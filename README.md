# tg-agent-bot

Telegram bot turned into an LLM agent: minimal harness, bounded agent loop,
exec tool, skills (course assignment 3). Implementation pending — see
`docs/spec/spec-v0.md`.

## Usage

```bash
<copy-pasteable run command>
```

## Setup

```bash
<setup commands>
```

## Testing

```bash
<test command>
```

## Build report

Built with AI agents under the lab workflow (spec-driven, one prompt = one
commit). Headline: <spec N tokens · M prompts · first-run: yes/no · $cost>.
Full report: [docs/reports/](docs/reports/), token accounting:
[docs/llm-usage.md](docs/llm-usage.md).
