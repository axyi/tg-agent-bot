# LLM usage

| # | Stage | Model | Tokens | Cost |
|---|-------|-------|--------|------|
| 1 | spec authoring (writer agent, 3 iterations incl. gate-chain and exec-recipe proof runs) | claude (lab session) | ~213k (harness-reported aggregate; in/out split not exposed) | — (flat-rate session) |
| 2 | spec review (reviewer agent, clean context, 3 passes) | claude (lab session) | ~270k (aggregate) | — |
| 3 | implementation step 1 — bootstrap: `.python-version`, `pyproject.toml`, `.gitignore`, `.env.example`, `uv lock`, `uv sync --locked` | claude-opus-5 | unknown | unknown |
| 4 | implementation step 2 — all test files, `conftest.py`, `fakes.py`; first `pytest` run observed failing | claude-opus-5 | unknown | unknown |
| 5 | implementation step 3 — `config.py` | claude-opus-5 | unknown | unknown |
| 6 | implementation step 4 — `storage.py` | claude-opus-5 | unknown | unknown |
| 7 | implementation step 5 — `tools.py` part 1 (`run_command`, `_Capture`, `_drain`) | claude-opus-5 | unknown | unknown |
| 8 | implementation step 6 — `tools.py` part 2 (skills, tool specs, dispatch) + `skills/*.md` | claude-opus-5 | unknown | unknown |
| 9 | implementation step 7 — `llm/base.py`, `llm/lmstudio.py`, `llm/openrouter.py`, `llm/__init__.py` | claude-opus-5 | unknown | unknown |
| 10 | implementation step 8 — `agent.py` | claude-opus-5 | unknown | unknown |
| 11 | implementation step 9 — `bot.py` (Telegram layer, poll loop, `--selftest`) | claude-opus-5 | unknown | unknown |
| 12 | implementation step 10 — `README.md`, prompt log, this table | claude-opus-5 | unknown | unknown |
| 13 | implementation step 11 — the four acceptance gates + code review | claude-opus-5 | unknown | unknown |
| **Σ** (rows 3–13, one continuous session) | | claude-opus-5 | in 14.78M (160 uncached + 260k cache-write + 14.52M cache-read), out 148.5k — measured from the local session transcript | ≈$12.60 (estimate at public API prices; actual billing: flat-rate subscription) |
| 14 | spec-v1 authoring (writer + reviewer agents, separate session) | claude-fable-5 (lab session) | ~unknown (separate session; not measured here) | — |
| 15 | v1 step 1 — preconditions: `.env` key-name checks, `docker pull python:3.13-slim`, LM Studio and OpenRouter model queries, `OPENROUTER_MODEL` appended; `.env.example`, `.gitignore` | claude-opus-5 | unknown | unknown |
| 16 | v1 step 2 — the four new test files plus the section-9.1 amendments; first `pytest` observed failing (6 collection errors) | claude-opus-5 | unknown | unknown |
| 17 | v1 step 3 — `config.py` (new variables, secret registration, sandbox placement) | claude-opus-5 | unknown | unknown |
| 18 | v1 step 4 — `storage.py` (schema v2 + migration, summaries, budget-aware loader, DB chmod) | claude-opus-5 | unknown | unknown |
| 19 | v1 step 5 — `tools.py` (docker argv/probe/runner, `_run_process`, `fetch_url`, audit, catalog) and both skills | claude-opus-5 | unknown | unknown |
| 20 | v1 step 6 — `llm/base.py`, `llm/lmstudio.py`, `llm/openrouter.py`, `llm/failover.py`, `llm/__init__.py` | claude-opus-5 | unknown | unknown |
| 21 | v1 step 7 — `agent.py` (repair rounds, truncation notice, token budget, interrupt, goals, summarizer) | claude-opus-5 | unknown | unknown |
| 22 | v1 step 8 — `bot.py` (pipeline order, rate limiter, commands, status message, send retry, `--selftest-live`) | claude-opus-5 | unknown | unknown |
| 23 | v1 step 9 — `README.md`, `AGENTS.md` | claude-opus-5 | unknown | unknown |
| 24 | v1 step 10 — five gates, Appendix-B acceptance probes, clean-context review, fix cycle 1/5, report | claude-opus-5 | unknown | unknown |
| **Σ** (rows 15–24, one continuous session + the review subagent) | | claude-opus-5 | in 35.51M (280 uncached + 692k cache-write + 34.82M cache-read), out 243.3k — measured from the local session transcripts | ≈$27.82 (estimate at public API prices; actual billing: flat-rate subscription) |
| 25 | v1.1 main session — preconditions, section-9.2 tests + section-9.1 amendments (observed the expected failures), the four mutation-check proofs, `config.py`/`storage.py`/`agent.py`/`tools.py`/`bot.py` in the order of section 8, five gates, Appendix-B live driver (C1–C7 + B1/B3/B4/B10 regression), documentation (`.env.example`, `.gitignore`, README, both spec doc-fixes, prompts 03/04/05, `report-v1.md` reconciliation), the review's `_record_sandbox_quota` completeness fix, the report, this table, and the final commit | claude-sonnet-5 | in 504 (uncached) + 534,939 (cache write) + 81,416,730 (cache read), out 229,845 — measured from the local Claude Code session transcript, per-request `usage` fields deduplicated by `requestId`, re-measured immediately before the commit | ≈$19.92 (estimate at public API prices: $2/$10 per MTok in/out, cache write ×1.25, cache read ×0.1; actual billing: flat-rate subscription) |
| 26 | v1.1 code-review subagent (`code-reviewer`, clean context, prompt `docs/prompts/06-code-review-v1.1.md`) | claude-sonnet-5 (subagent default) | 158,889 total (harness-reported aggregate; in/out/cache split not exposed to the parent session — unlike rows 1–24, the transcript backing this number is an async task-output file the harness instructs the parent session not to read) | not computed — the split needed for the cache-aware formula above is unavailable |
| **Σ** (rows 25–26, one `go` run) | | claude-sonnet-5 | 82,182,018 (row 25) + 158,889 aggregate (row 26) | ≈$19.92 + unknown (row 26) |
| 27 | v1.2 main session — precondition checks (incl. the LM Studio gate-5 exception), section-10.2 tests + section-10.1 amendments (observed red before green), `devtools/mutation_check.py` and its own test suite built before use, `config.py`/`storage.py`/`agent.py`/`tools.py`/`bot.py` in section-9 order, six gates (incl. one fix-and-rerun of the mutation gate itself — see report Fix cycles), Appendix-B live driver (D1–D8 + C1/C3/C4/C6 regression), documentation housekeeping (this table, `docs/plan.md`, both v1.1 report corrections, `AGENTS.md`'s gate list), the review, the report, and the final commits | claude-sonnet-5 | not computed — this run's harness did not expose a local session transcript to re-measure from at commit time (see note below); the per-column split was never reconstructed | ≈$33.11 (estimate at public API prices, measured after the fact from the local session transcript — see note below; actual billing: flat-rate subscription) |
| 28 | v1.2 code-review subagent (`code-reviewer`, clean context, prompt `docs/prompts/08-code-review-v1.2.md`) | claude-sonnet-5 (subagent default) | not computed — same async task-output constraint as row 26 | not computed |
| **Σ** (rows 27–28, one `go` run) | | claude-sonnet-5 | not computed | ≈$33.11 + unknown (row 28) |
| 29 | spec-v1.3 authoring — writer pass in the lab session plus two clean-context reviewer subagents (design review, then a consistency re-review after 25 fixes); executor of the spec is `claude-opus-5`, not yet run | claude-fable-5 (lab session; reviewers on the subagent default) | ≈258k subagent aggregate (≈134k design review + writer helpers, 123,557 consistency re-review; harness-reported, in/out split not exposed) + a main-session share that is not isolatable from the lab session transcript (that session also carried unrelated lab work) | — (flat-rate session) |
| 30 | v1.3 implementation run — the whole `go docs/spec/spec-v1.3.md` run across its four commits: stage A (v1.2 carry-over, observability, pricing, benchmark harness, dashboard, mutation entries), the baseline benchmark and the OpenRouter smoke, the token audit, stage C (optimizations O1–O6 incl. the O5 probe), the optimized run and the gate report, the six gates at C1/C3/C4, two clean-context reviews with one fix round each, and the C4 documentation | claude-opus-5 | unknown | unknown |
| 31 | v1.3 task subagents — 18 of the run's 19 prompt files (`docs/prompts/10-v13-TA1-carryover.md` … `27-v13-TC9-docs.md`; the 19th is row 30's own `09-go-spec-v1.3.md`), including the two `code-reviewer` clean-context reviews (`16-…`, `25-…`) and the audit subagent (`18-…`) | claude-opus-5 | unknown | unknown |
| **Σ** (rows 30–31, one `go` run) | | claude-opus-5 | unknown | unknown |
| 32 | v1.3 verify-run docs fixes (prompt 30): trimmed `docs/reports/tg-post-v1.3.md` under the 1500-char limit, added the `Model reason` bullet required by `standards/reporting.md` to all 21 v1.3 prompt files (`09-go-spec-v1.3.md`–`29-v13-TD2-tg-post.md`), and this row | claude-sonnet-5 | unknown | unknown |
| 33 | spec-v1.4 authoring — design in the lab session after `/verify-run` of v1.3, one fact-finding subagent over the v1.3 reports and bench files, one writer subagent for the draft, two markers resolved in the lab session (OpenRouter `reasoning` field, LM Studio model-level fallback), then four rounds of Codex cross-review (`gpt-5.6-sol`, 31 findings, all accepted; Appendix C of the spec) each applied by one clean-context subagent; executor of the spec is `claude-sonnet-5`, not yet run | claude-fable-5 (lab session; fact-finding on `claude-sonnet-5`, writer and the four fix passes on `claude-opus-5`; challenger OpenAI `gpt-5.6-sol`) | ≈1.11M subagent aggregate (≈120k fact-finding, ≈300k writer, ≈690k across the four fix passes; harness-reported, in/out split not exposed) + a main-session share that is not isolatable from the lab session transcript; Codex: 4 requests × ≈25k prompt tokens, output ≤ 16k each | — (flat-rate session; Codex API metered, amount not captured) |
| 34 | spec-v1.4 implementation run — the whole `go docs/spec/spec-v1.4.md` run, prompts 31–41, across T0–T10: preconditions, S01 root-cause repair (H1), bench harness readiness (BEN-03/04/05), the `baseline-v1.4` two-tree measurement, the RSN spike (all five candidates tried, RSN-06 STOP — no honored+shippable reasoning mechanism), REL-01's timeout/budget consistency check, default-selection documentation (STOP branch), mutation coverage for BEN-03/REL-01, the code review's two fixes, Appendix-B acceptance (E5/E8/E10 + D1/D3 regression), and this report/errata/post/table | claude-sonnet-5 (background session) | unknown — this harness does not expose a local session transcript to this session for re-measurement, unlike row 25's run; reconciled later via `tools/session-usage.py` outside this repository, the same way row 27 and rows 30–32 were left for the maintainer | unknown (estimate to follow the same after-the-fact reconciliation, at public API prices, once measured; actual billing: flat-rate subscription) |
| 35 | spec-v1.4 T9 code-review subagent (`code-reviewer`, clean context, prompt `docs/prompts/40-v14-t9-review.md`) | claude-sonnet-5 (subagent default) | 153,558 total (harness-reported aggregate over 62 tool uses; in/out/cache split not exposed to the parent session — same async task-output constraint rows 26/28 hit) | not computed — the split needed for the cache-aware formula is unavailable, as for rows 26/28 |
| **Σ** (rows 34–35, one `go` run) | | claude-sonnet-5 | unknown (row 34) + 153,558 aggregate (row 35) | unknown + unknown (row 35) |
| 36 | `/verify-run` docs-only fixes (prompt 43): added the `Model reason` bullet to `docs/prompts/40-v14-t9-review.md`, corrected `report-v1.4.md`'s gate-table commit count to 9→11 and its `llm-usage.md` contribution to rows 34–35, added the deferred lab-ledger row (applied by the lab as `3c12cc9`) to the same section, and this row | claude-sonnet-5 | unknown | unknown |
| 37 | spec-v1.5 authoring — lab session after `/verify-run` of v1.4: four fact-finding subagents (v1.4 outcome and tool-use evidence, ethinking/idp-concept standards inventory, ai-workflows-concept lessons, upstream version audit), one writer subagent, then three rounds of Codex cross-review (`gpt-5.6-sol`, 28 findings, all accepted; Appendix C of the spec) each applied by a clean-context subagent; executor of the spec is `claude-sonnet-5`, not yet run | claude-fable-5.1 (lab session; fact-finding on `claude-sonnet-5`, writer and the three fix passes on `claude-opus-5`; challenger OpenAI `gpt-5.6-sol`) | ≈1.14M subagent aggregate (≈325k fact-finding, ≈202k writer, ≈690k across the three fix passes; harness-reported, in/out split not exposed) + a main-session share that is not isolatable from the lab session transcript; Codex: 3 requests × ≈27k prompt tokens, output ≤ 16k each | — (flat-rate session; Codex API metered, amount not captured) |
| 38 | spec-v1.5 implementation run — the whole `go docs/spec/spec-v1.5.md` run, prompts 44–62, across T0–T18: `checks.py`'s YAML reader/schema/CC functions, five tool installs (gitleaks/semgrep/trivy/skylos/rtk, each its own atomic commit), scanner wiring (fail-closed, diff-scope partition, shadow mode), the `.githooks/` chain + `install_hooks.py` + `replay` (hooks activated live from T8 on, every later commit passing through them), `checks.py doctor`, the project-local RTK hook, prompt-format `lint-docs`, the three-profile wiring + wall-clock measurement + four `v15-*` mutations, the ruff 0.16.6 and Python 3.14 bumps, the sandbox image digest pin + byte-compared smoke, `AGENTS.md`/`docs/plan.md` sync, the T17 review's own fixes, and this report/post/table | claude-sonnet-5 (background session) | unknown — this harness does not expose a local session transcript to this session for re-measurement, same constraint as rows 27/30–34 | unknown (same after-the-fact reconciliation as row 34, once measured; actual billing: flat-rate subscription) |
| 39 | spec-v1.5 T11 prompt-backfill subagent (general-purpose, clean context: backfilled the 7-bullet header on the 29 historical prompt files `01`–`29` failing `lint-docs` check 1) | claude-sonnet-5 (subagent default) | unknown — the harness-reported aggregate was not captured into this table at the time (its own independent re-verification — a fresh check-1 sweep and the full test suite, run directly by the main session — is recorded in the T11 report section instead) | not computed |
| 40 | spec-v1.5 T16 `AGENTS.md`/`docs/plan.md` diff-review subagent (general-purpose, clean context) | claude-sonnet-5 (subagent default) | unknown — same as row 39, not captured into this table at the time | not computed |
| 41 | spec-v1.5 T17 code-review subagent (`code-reviewer`, clean context, prompt `docs/prompts/61-v15-t17-review.md`) | claude-sonnet-5 (subagent default) | 231,051 total (harness-reported aggregate over 50 tool uses, ~12.8 min; in/out/cache split not exposed to the parent session — same async task-output constraint as rows 26/28/35/44) | not computed — same reason as rows 26/28/35/44 |
| 42 | spec-v1.5 T19 final acceptance — the six gates, `checks.py run --profile full --since <base>`, `checks.py replay --range <base>..<implementation-tip>` (diagnosed both historical exceptions directly rather than assuming a cause), all 12 Appendix-B scenarios (2 driven live against the real repository: E6's trivy-binary-hidden run, E7's shadow/blocking-flip run), the REQ-V15-ACC-02 regression note, and the freeze commit (`docs/reports/report-v1.5.md`'s Final acceptance/attestation/RLM/Fix-cycles/Ledger/Verdict sections, `docs/plan.md`'s v1.5 → complete, this row, prompt 63) | claude-sonnet-5 (background session) | unknown — same constraint as row 38 | unknown (same after-the-fact reconciliation as row 38) |
| 43 | spec-v1.5 post-freeze corrections — two `advisor()` calls after T19 was believed done surfaced (a) `tg-post-v1.5.md`'s stale prompt count and missing T19 summary (fixed in `346a67b`) and (b) `346a67b`'s own commit message citing the wrong prompt (fixed by this row's own commit, prompt 64, which also updates the report's Deviations/Bugs/Ledger sections) | claude-sonnet-5 (background session) | unknown — same constraint as row 38 | unknown (same after-the-fact reconciliation as row 38) |
| 44 | spec-v1.5 post-freeze correction — a third `advisor()` call found the T17 review's own finding count was wrong (report said "5 findings," the enumeration and the reviewer's raw output both show 6: 4 🟡 + 2 🟢); fixed the Review section header, the RLM table's T17 row, `docs/plan.md`'s mention, and recorded `cd88b35`/`6fde12f`'s already-made commit messages as errata (prompt 65) | claude-sonnet-5 (background session) | unknown — same constraint as row 38 | unknown (same after-the-fact reconciliation as row 38) |
| **Σ** (rows 38–44, one `go` run + post-freeze, prompts 44–65) | | claude-sonnet-5 | unknown (row 38) + unknown (row 39) + unknown (row 40) + 231,051 aggregate (row 41) + unknown (row 42) + unknown (row 43) + unknown (row 44) | unknown + unknown + unknown + unknown + unknown + unknown + unknown |
| 45 | v1.5.1 patch D1 (CRITICAL) — root-caused and fixed test fixtures escaping into the enclosing repository's ref namespace via a leaked `GIT_DIR`/`GIT_WORK_TREE` (a real incident: `refs/heads/main` had been force-renamed onto a throwaway fixture commit, repaired manually before this session); empirically reproduced the incident's exact reflog line in an isolated scratch directory before writing the fix, then a regression test (red against the pre-fix fixtures, green after) (prompt `docs/prompts/66-v151-d1-fixture-git-env-isolation.md`, commit `5cc3909`) | claude-sonnet-5 (main session) | unknown — this harness does not expose a local session transcript to this session for re-measurement, same constraint as rows 38/42–44 | unknown (same after-the-fact reconciliation as row 38) |
| 46 | v1.5.1 patch D2 — measured `mutation-all`'s real wall clock directly (`time uv run --locked python devtools/mutation_check.py`: 21m12.932s, 72/72 killed) and `mutation-v15`'s (`--select v15-`: 1m39.497s, 4/4 killed); raised both gates' `timeout_seconds` in `config/quality_gates.yaml` to ~2x measured (1200s→2600s, 180s→220s), rule and measurement recorded in the config's own comment (prompt `docs/prompts/67-v151-d2-mutation-timeout.md`, commit `efc2df3`) | claude-sonnet-5 (main session) | unknown — same constraint as row 45 | unknown (same after-the-fact reconciliation as row 38) |
| 47 | v1.5.1 patch D3 (docs-only) — added the missing REQ-V15-ACC-03 freeze-exception re-verification sentence to `report-v1.5.md`'s Deviations items 6(b)/7 for post-freeze commits `6fde12f`/`85c5ad7` (said plainly that no such re-verification could be established as having run at the time, then performed a real one now via `checks.py replay`, both clean), and fixed `spec-v1.5.md`'s stale "43 mutation entries" prose to 72 in its two remaining spots (prompt `docs/prompts/68-v151-d3-freeze-evidence-and-errata.md`, commit `a6ca13c`) | claude-sonnet-5 (main session) | unknown — same constraint as row 45 | unknown (same after-the-fact reconciliation as row 38) |
| 48 | v1.5.1 patch closing — `docs/reports/report-v1.5.1.md` (what was fixed, why the freeze was broken and the user's explicit in-session authorisation, D1's red→green evidence, D2's measurement and rule, D3's fix, all six `AGENTS.md` gates in order including gate 5's recorded `lmstudio` failure, the `full` profile run, the `replay` delta, a ready-to-paste `economics.md` row, and an explicit note that no Telegram post is produced for a patch this small), this row and rows 45–47 (prompt `docs/prompts/69-v151-report-and-ledger.md`) | claude-sonnet-5 (main session) | unknown — same constraint as row 45 | unknown (same after-the-fact reconciliation as row 38) |
| 49 | v1.5.1 post-freeze-analogue correction, found by `advisor()` after row 48/commit `475d243` was believed done — (a) row 48's own "the user's explicit in-session authorisation" phrasing (and the matching claim in `report-v1.5.1.md`) asserted something this executor had no direct evidence for, only the patch brief's own text; not amended (`475d243` is an already hook-verified commit, and this row is itself immutable once committed) — corrected forward in `report-v1.5.1.md` to say the authorisation is relayed by the brief, not independently observed, matching the same honesty standard D3 itself applied to `6fde12f`/`85c5ad7`'s freeze evidence; (b) confirmed by grep that D1's fix scope covers every test file that shells out to `git`, not just `test_v15_standards.py`; (c) `spec-v1.5.md`'s §14 gate table carried a count fixed by D3 (72) sitting next to a timing left stale from 43 entries — both rows now carry v1.5.1's own measured figures alongside the original, each explicitly time-stamped; (d) re-ran `checks.py replay` to include `475d243` itself (the report/ledger commit) in the verified range, not stop one commit short of the true final HEAD; (e) softened the Verdict headline so an `AGENTS.md`-blocking gate-5 failure isn't summarised as a bare "PASS" (prompt `docs/prompts/70-v151-advisor-followup.md`) | claude-sonnet-5 (main session) | unknown — same constraint as row 45 | unknown (same after-the-fact reconciliation as row 38) |
| 50 | spec-v1.6.0 authoring — lab session after `/verify-run` of v1.5 and the v1.5.1 patch: scope aligned with the user (observability + tool quality + baseline + SemVer in 1.6.0; reasoning + cost gate deferred to 1.7.0), OTel GenAI semconv names verified against the semconv registry, one writer subagent (147 KB draft), one compression pass (135 KB), then three rounds of Codex cross-review (`gpt-5.6-sol`, 30 findings — 10 Critical, 18 High, 2 Medium — all accepted, 4 adapted; termination `round_limit`; Appendix C of the spec) each applied by a clean-context subagent; final spec 184 509 B, 103 MUST + 16 NON-GOAL, 19 tasks, 59 named tests, 10 mutation entries; prompt file `71-v160-spec-authoring.md`; executor of the spec is `claude-sonnet-5`, not yet run | claude-fable-5.1 (lab session; writer, compression and the three fix passes on `claude-opus-5`; challenger OpenAI `gpt-5.6-sol`) | ≈1.20M subagent aggregate (265k writer, 261k compression, 252k + 219k + 200k across the three fix passes; harness-reported, in/out split not exposed) + a main-session share that is not isolatable from the lab session transcript; Codex: 3 requests, prompt 39 665 / 47 958 / 51 864 tokens, completion 5 656 / 6 750 / 6 509 (of which reasoning 3 073 / 4 865 / 4 607) | — (flat-rate session; Codex API metered, amount not captured) |
| 51 | spec-v1.6.0 resume under errata 2–5, prompts 93–96: two source commits (`20f3200` S13 ceiling, `ca9c656` REQ-V160-BEN-08) each with its own `code-reviewer` clean-context pass, offline gates + mutation gate (83/83 killed) re-verified, PRE-03/PRE-04 resolved live against `qwen/qwen3.8-27b` on LM Studio, `smoke-v160` reproduced across two invocations, the `baseline-v1.6.0` measurement run as five invocations (53/54 pass) — STOPPED before T16's commit on S18's `summary_exists` failure (reasoning-budget exhaustion, same instrument family as S15), operator decision pending; report's new "Resume under errata 2–5" section | claude-sonnet-5 (background session) | unknown — this harness does not expose a local session transcript to this session for re-measurement, same constraint as rows 38/45–50; live LM Studio inference this session alone ran ≈2.5 hours of wall-clock across the smoke and baseline measurements | unknown (same after-the-fact reconciliation as row 38); $0.00 marginal — all inference local to LM Studio |
| 52 | spec-v1.6.0 T0–T15 implementation run — prompts 72–92 (`72-go-spec-v1.6.0.md` through `92-v160-run-closed-at-t15.md`), covering the whole first attempt end to end: preconditions, tracing spans (T1) and storage schema 4 (T2), agent span wiring (T3), metrics aggregates (T4), dashboard render + refactor (T5) and its server + CLI wiring (T6/T7), the truncated-summary retry (T8) and repeat-call refusal (T9), six new bench scenarios + `BENCH_SCHEMA` 2 (T10/T11), version bump and docs catch-up (T12), ten `v160-*` mutation entries (T13), the clean-context code review (T14), and T15's live preflight, erratum 2's disclosure and `smoke-v160`'s S13 blocker, ending at "Run closed at T15" — **known gap closed at prompt 101**: measured directly from that execution's own local Claude Code session transcript (`aee4c17e-2b90-40ba-9a01-cfc50b6cb1e6.jsonl`, 2026-09-04T17:43:32Z–2026-09-06T06:46:28Z, one continuous session spanning two context-window compactions), the same methodology row 25 established — per-request `usage` fields aggregated per unique `requestId` (1100 distinct requests; a requestId spans several JSONL lines, one per content block of the same response, all carrying an identical usage snapshot, confirmed before trusting the aggregate); excludes the four `general-purpose`-subagent delegations (T10–T13) recorded above, whose own token totals live in async task-output files this session was instructed not to read, the same constraint rows 26/28/35/41 record | claude-sonnet-5 (background session) | in 516,238,033 (2,210 uncached + 3,776,373 cache-write + 512,459,450 cache-read), out 888,818 — measured from the local session transcript, deduplicated by `requestId`, as above; excludes T10–T13's own subagent totals | ≈$120.83 (estimate at public API prices: $2/$10 per MTok in/out, cache write ×1.25, cache read ×0.1 — same formula and rate as row 25; actual billing: flat-rate subscription) |
| 53 | spec-v1.6.0 resume, prompts 97–98: operator authorised erratum 6 (S18's `summary_exists` non-blocking, same reasoning-budget mechanism as S15) after an `AskUserQuestion` exchange; disclosed in place (`97-v160-erratum6-s18.md`); the already-measured 54-run baseline (no re-run) merged into one canonical document via `bench.summarize()`, `docs/assets/bench/baseline-v1.6.0.json` and `dashboard-v1.6.0.html` committed, the informational S01–S12 comparison against `baseline-v1.4.json` produced via the Python API and recorded in the report (T16, `98-v160-t16-baseline.md`) | claude-sonnet-5 (background session) | unknown — same constraint as row 51; no further live LM Studio inference in this row (the merge, dashboard render and comparison are all offline) | $0.00 — no inference this row |
| 54 | spec-v1.6.0 T17 — provisional report, prompt 99 (`99-v160-t17-provisional-report.md`): closed the four remaining REQ-V160-RPT-02 items (measured test count, the two §6 VERIFY markers against the live semconv registry, dashboard evidence, scanner summary, fix-cycle count) and wrote `docs/reports/tg-post-v1.6.0.md` (RU, 1477 chars) | claude-sonnet-5 (background session) | unknown — this harness does not expose a local session transcript to this session for re-measurement, same constraint as row 53 | unknown (same after-the-fact reconciliation as row 38) |
| 55 | spec-v1.6.0 T18 — final acceptance, prompt 100 (`100-v160-t18-final-acceptance.md`): the six verbatim gates re-run on tip `726771f`, both `checks.py run --profile full` candidates, `checks.py replay --range d7e1d395…726771f` (28/28 commits clean), all 14 Appendix B scenarios, the REQ-V160-ACC-02 regression check with concrete evidence, and the evidence-only commit `0d33af4` recording `<implementation-tip>`/`<base>`/the replay output and the tag's **intended** name only, never its creation | claude-sonnet-5 (background session) | unknown — same constraint as row 51 | unknown (same after-the-fact reconciliation as row 38) |
| 56 | spec-v1.6.0 post-freeze `/verify-run` docs corrections, prompt 101 (`101-v160-verify-run-fixes.md`, this row): tag-creation evidence (when, commit, annotated message, the operator's `AskUserQuestion` approval, all quoted from this session's own tool-call record) added to `report-v1.6.0.md`; S18 repeat 3's root cause corrected (attempt 1 truncated, attempt 2 hit the 600 s transport timeout, read from `baseline-v1.6.0.json`'s embedded `llm_calls`); the erratum-6 three-option list restored to the order/labels `AskUserQuestion` actually used; a new "Deviations — compiled" section closing the RPT-02 item 13 promise T17 left open; `tg-post-v1.6.0.md`'s tag/prompt-count update; and this table's rows 52 (the 72–92 backfill) and 54–55 | claude-sonnet-5 (background session) | unknown — this session's own transcript was not re-measured before this commit the way row 25's was, same constraint as row 38 | unknown (same after-the-fact reconciliation as row 38); $0.00 marginal — no live LM Studio or bot inference this row (docs-only, no gates run) |
| 57 | spec-v1.7.0 authoring — lab session after v1.6.0 closed (tag `v1.6.0`, prompt 101): scope from the user's 2026-09-04 decision (reasoning policy per call purpose on LM Studio Bionic 1.1.x, summary wall-clock budget, cost gate −30 % against the frozen `baseline-v1.6.0`, S15/S18 back in the blocking gate, three carried items), one writer subagent (112 KB draft, 67 MUST) with a verification pass over 157 `file:line` citations (25 fixed) that also found `bench.comparability()` rejecting the frozen baseline against itself (v1.3 stage-C rule) and `timeout_s=1800` locked, then three rounds of Codex cross-review (`gpt-5.6-sol`, 28 findings — 9 Critical, 16 High, 3 Medium — 27 accepted, 5 adapted, 1 rejected as contradicting the standing EC-04; termination `round_limit`; Appendix C of the spec) each applied by a clean-context subagent; final spec 225 225 B, 69 MUST + 15 NON-GOAL, 15 tasks (3 conditional exits + T-STOP), 25 named tests, 9 mutation entries, 4 `[[VERIFY]]` markers; prompt file `102-v170-spec-authoring.md`; executor of the spec is `claude-sonnet-5`, not yet run | claude-fable-5.1 (lab session; writer, verification and the three fix passes on `claude-opus-5`; challenger OpenAI `gpt-5.6-sol`) | ≈1.07M subagent aggregate (260k writer, 180k verification, 224k + 191k + 211k across the three fix passes; harness-reported, in/out split not exposed) + a main-session share that is not isolatable from the lab session transcript; Codex: 3 requests, prompt 34 326 / 44 545 / 54 511 tokens, completion 4 928 / 5 425 / 5 569 (of which reasoning 2 876 / 3 549 / 3 584) | — (flat-rate session; Codex API metered, amount not captured) |
| **Σ** (rows 45–49, v1.5.1 patch, prompts 66–70) | | claude-sonnet-5 | unknown + unknown + unknown + unknown + unknown | unknown + unknown + unknown + unknown + unknown |
| 58 | spec-v1.7.0 T0–T10 implementation, prompts 103–115 (`103-go-spec-v1.7.0.md` through `115-v170-t10-code-review.md`): preconditions and the T0 report skeleton; T1's live preflight establishing the six BEN-01 instrument values against `192.168.0.145`/`Bionic v1.1.1`/`qwen/qwen3.8-27b`/context `42496` and the TTFT-shape probe (found `stats: {}` on the OpenAI-compatible route — RSN-07 trigger 1); T2's scratch harness and T3's live stage-A pairs (candidate **c**, assistant-prefill, honored and shippable for `summary` only — `tool-round`/`final` both `none`); a T4 blocker (`storage.SCHEMA_VERSION` 4→5 breaking two unlisted tests under REQ-V170-EC-03, resolved) plus the core types and migration; T5's `complete()` wiring and the five call sites carrying `reasoning`/`timeout_s`; T6's summary wall-clock budget; T7's `bench.py` comparability/BEN/CAR-01 work; T8's docs and gate-config catch-up; a T9 blocker (the gate-matrix table missing from `spec-v1.7.0.md`, resolved by an operator-authorised spec edit disclosed via both sha256 values) plus nine `v170-*` mutation entries; and T10's clean-context `code-reviewer` pass (two 🔴 findings — four tests pinned the pre-T12 default via the wrong oracle; `T-V170-POL-07` missing — and one 🟡 — the mandated three-file test split not yet done — all fixed and re-verified) | claude-sonnet-5 (background session) | unknown — this harness does not expose a local session transcript to this session for re-measurement, same constraint as rows 51/53/56 | unknown (same after-the-fact reconciliation as row 38); $0.00 marginal for the T1/T3 live LM Studio inference (reference-priced, local, no real money) |
| 59 | spec-v1.7.0 T11–T12, prompts 116–121 (`116-v170-t11-stage-c-candidates.md` through `121-v170-report-correction.md`): T11's Stage C candidate benchmark, run only after the operator's explicit separate check-in for the multi-hour live undertaking — C1 (`by-purpose`/`tool-round`, one invocation, 54/54, ≈76 min) and C2 (`off`, three invocations after two `S15` timeout aborts, reassembled per BEN-04, 51/54) both measured against `baseline-v1.6.0`; the wire-identity finding that C1/C2/C3 resolve to byte-identical requests this run (confirmed from `reasoning_requested`/`reasoning_honored` on every `llm_calls` row), which also surfaced a T8 documentation defect (`LLM_REASONING_ON_PURPOSES`'s sense inverted in `.env.example`/`README.md`); verdict **FAIL, cost gate** (`docs/reports/bench-v1.7.0.md`); then T12's selection commit shipping C1 as the default, two blockers discovered and resolved under freeze with the operator's explicit authorisation via `AskUserQuestion` (the ACC-03 hunk checker scoped to added lines only after 7+ tested wording attempts proved the added-and-removed scoping unsatisfiable; a second "T4-style" erratum reading two more pre-existing tests' expected default from `Config`'s own field instead of a literal), and a transient 1Password SSH-agent outage that briefly broke every git-commit-dependent test (confirmed external via a scratch-directory reproduction, not a regression) | claude-sonnet-5 (background session) | unknown — same constraint as row 58; T11's own live LM Studio inference is separately measured and reference-priced inside `docs/assets/bench/cand-v170-by-purpose-tool.json` ($0.17189) and `cand-v170-off.json` ($0.14612) | unknown (same after-the-fact reconciliation as row 38); $0.00 marginal for T11's live LM Studio inference (reference-priced, local, no real money) |
| 60 | spec-v1.7.0 T13, prompt 122 (`122-v170-t13-report-closure.md`): closed the remaining REQ-V170-RPT-03 items the per-task sections had not yet carried to completion — the consolidated gates table, mutation summary, scanner summary (disclosing all three skylos NG-12 figures — 19 spec, 8 T10-filtered, 13 full-profile), fix-cycle count (0 of 5, derived from REQ-V170-EC-01's own cycle definition), measured test count (1016 → 1133, `AGENTS.md` confirmed still correct); the Benchmark-affecting-changes, RLM-delegation and Deviations running sections extended through T12 (also recovering one previously-undocumented T11 deviation — the instrument-metadata CLI-flag omission on both candidate documents, patched pre-commit); the Ledger row filled in; `docs/reports/tg-post-v1.7.0.md` written (1487 chars) | claude-sonnet-5 (background session) | unknown — same constraint as rows 58–59 | unknown (same after-the-fact reconciliation as row 38); $0.00 marginal — no live LM Studio or bot inference this row (docs-only, no gates re-run) |
| 61 | spec-v1.7.0 T14, prompt 123 (`123-v170-t14-final-acceptance.md`): final acceptance — the six verbatim gates re-run against `<implementation-tip>` (`48e9db2`), gate 5 needing one re-pin after the GPU box's floating IP moved a 4th time (`172.16.50.233`, same disposition as T1/T5/T10); `checks.py run --profile full --since <base>` (15/15) and `checks.py replay --range <base>..<implementation-tip>` (21/21); the Appendix B table (12 PASS, E13 recorded not applicable on trigger 1 — `stats.time_to_first_token` absent); the REQ-V170-ACC-02 regression check (`tools.py`/`dashboard_server.py`/`dashboard_render.py` confirmed byte-unchanged this release); two background security-review notifications on `tools.py`/`bot.py`, both verified false positives — races with `mutation_check.py`'s own in-place mutation cycling, confirmed against the catalogue and against committed `HEAD`; the disclosure that `T-V170-ACC-03`'s selection-commit half still skips (the permanent prompt-120 ambiguity from T12) rather than passing as spec:2317 anticipated; landed as two commits (`0c3ae39`, the prompt file alone, then `ff0f2a9`, the evidence-only commit touching `docs/reports/*` only) — **REQ-V170-BEN-07 verdict stands FAIL, cost gate: no `v1.7.0` tag created, `git tag -l` unchanged, nothing pushed**; then a documentation-only correction (REQ-V170-ACC-03 exception 1), prompt 124, fixing this table's own T14 gap and `tg-post-v1.7.0.md`'s stale commit/prompt count | claude-sonnet-5 (background session) | unknown — same constraint as rows 58–60 | unknown (same after-the-fact reconciliation as row 38); $0.00 marginal — no live LM Studio or bot inference beyond gate 5's own live check (docs-only otherwise) |
| 62 | spec-v1.7.0 operator acceptance, prompt 125 (`125-v170-operator-acceptance.md`): the operator accepted the release under its standing cost-gate FAIL and authorised the tag; an "Operator decision" subsection added to `docs/reports/report-v1.7.0.md` under the verdict it overrides, the tag sentence corrected in `docs/reports/tg-post-v1.7.0.md`, no measured number touched. **Row added retroactively 2026-09-09** — prompt 125 shipped without one, a gap `/verify-run` item 3 would have raised on the next run | claude-opus-5 (lab session) | unknown — same harness constraint as rows 58–61 | unknown (same after-the-fact reconciliation as row 38); $0.00 marginal — docs only, no inference |
| 63 | spec-v1.8.0 authoring, prompt 126 (`126-v180-spec-authoring.md`): brief → draft → citation audit → pre-round rulings → three Codex cross-review rounds, across four commits (`58d4553`, `e537335`, `a8a3db9`, `ece29cc`). Challenger **OpenAI Codex `gpt-5.6-sol`**, 3 rounds, **50 findings, 50 accepted (14 adapted), 0 rejected**, termination `round_limit`. Citation audit: 124 citation tokens opened, 18 corrected, 150 asserted names checked against source. Final: 51 MUST, 24 tests, 11 tasks, 14 scenarios, 6 mutation entries; spec 103,612 B + delta 20,149 B | claude-opus-5 (lab session; drafting, audit and applying subagents) | subagents 1,264,000 aggregate as reported by the harness (explore 62,935 · draft 225,934 · citation audit 160,321 · pre-round apply 261,680 · round-1 apply 188,135 · round-2 apply 205,562 · round-3 apply 159,378); Codex per round — r1 23,827 in / 6,028 out (4,096 reasoning), r2 31,346 / 5,769 (4,366), r3 32,974 / 6,591 (5,592). Lab-session main context: unknown, same constraint as rows 58–62 | unknown for the lab session; Codex ≈$0.60 at public API prices for 88,147 in / 18,388 out — an estimate, marked as such; $0.00 to LM Studio (no bot inference this row) |

| 64 | spec-v1.8.0 `go` run, prompts 127–138 (`127-go-spec-v1.8.0.md` through `138-v180-t9-version-bump.md`): T0 preconditions (gate 5 re-pinned to the live GPU box, `192.168.0.145`, after the two other known IPs timed out; floor re-measured 1133, no drift); T1 `AGENTS.md` Context discipline + ownership-zone rule; T2 delete-on-success status + `AgentOutcome`/`run_agent_outcome`, plus an operator-authorised erratum extending REQ-V180-EC-03's four-site test-amendment list by two (`tests/test_pricing.py`, `tests/test_v1_guardrails.py`, structurally broken by CHAT-02/-04's own explicit MUSTs); T3 the typing indicator, which also fixed one pre-existing `tests/test_bench.py` SIGINT-simulation race (flagged as a process deviation — bundled into T3's own commit rather than escalated first, unlike every other cross-file break this run); T4 the dashboard visual rework to the frozen `--ink`/`--ground`/`--plate`/`--rule`/`--dim`/`--signal` palette (no pre-existing test broken); T5 the four conversation storage readers plus two render builders (by-turn cursor paging, the seeded byte-budget accumulator); T6 the four `/conversations` routes, redaction/truncation on both sinks, and six new mutation entries with two real (not estimated) measured gate timeouts — its background subagent stalled once mid-run on its own backgrounded mutation-timing measurement, leaving one mutation (`v180-typing-ceiling-removed`) applied and unreverted in the working tree; the orchestrator caught it via `git status`/`git diff` (a background security-review plugin independently, and correctly, flagged the same transient state as a false positive), reverted it by hand, then the harness's own agent lifecycle resumed the same subagent to finish and commit; T7 the doc/config catch-up (test count 1133→1217, mutation count 92→98), whose own report-skeleton gap (a missing Ledger-row section, the orchestrator's T0 omission, not T7's) was caught by T7's `lint-docs` run and fixed directly afterward; T8 the required clean-context `code-reviewer` review (eight compliance items verified, three findings: an undisclosed `reading_strip_section` wiring gap and an unguarded `Thread.start()` fixed, the already-disclosed `/traces` duration-bar detail formally waived — no `MUST`-test binds it) plus a second operator-authorised test-amendment erratum (`tests/test_v170_bench.py`'s v1.7.0-named `report_path` self-check, structurally broken by T7's own required REQ-V180-RPT-01 repoint) and the full six-gate-plus-`checks.py run --profile full` re-run, all green (98/98 mutations killed twice, once per required invocation); T9 the version bump (`pyproject.toml` + `uv.lock`, `1.7.0`→`1.8.0`, `T-V180-VER-01` confirmed red-before/green-after via `git stash`) | claude-sonnet-5 (interactive session, delegating each task to a fresh background subagent per `AGENTS.md`'s Context discipline, one `code-reviewer` review) | subagents' own harness-reported aggregates, summed: T1 97,742 · T2 220,625 · T2-erratum 71,327 · T3 150,060 · T4 186,325 · T5 172,131 · T6 320,037 + 374,666 (two runs, the stall and its resumption) · T7 89,318 · T8-review 174,661 · T8-fixes 111,131 = **1,968,023** aggregate across 11 subagent invocations; the interactive orchestrator session's own token count is not self-measurable by this harness (same constraint as every `background session` row above) | $0 marginal API cost — Claude Code subscription-metered session (`/usage`, not per-token billed); $0.00 to LM Studio/OpenRouter — no live bot inference this run beyond gate 5's own zero-token live checks (`--selftest-live` "spends no inference tokens and sends no Telegram message" per `AGENTS.md`) |
| 65 | spec-v1.8.0 T10, prompt 139 (`139-v180-t10-final-acceptance.md`): final acceptance — the six verbatim gates re-run against `<implementation-tip>` `30207af` (gate 5 needed one more GPU-box re-pin, `192.168.0.145` → `172.16.50.233`, same known-set disposition as T0), mutation gate 98/98 killed; `checks.py run --profile full --since 28e6169` all 15 gates PASS; `checks.py replay --range 28e6169..30207af` 12/12 commits clean, no hook bypass anywhere in the run; Appendix B 14/14 driven by the already-green suite; one evidence-only commit touching `docs/reports/` and nothing else, then the annotated tag `v1.8.0`. Row added retroactively by prompt 140 — the run closed without it, the same REQ-V180-RPT-04 gap row 62 records for v1.7.0's prompt 125. | claude-sonnet-5 (interactive session; T10 is *artefacts only* per `standards/workflow.md` §5.1, so no subagent was spawned and the work ran in the orchestrator's own context) | unknown — commands-only orchestrator work with no subagent to report an aggregate; the interactive session's own per-request counts are not self-measurable by this harness | $0 marginal — Claude Code subscription-metered session, not per-token billed; $0.00 live inference (gate 5's checks spend no model tokens) |
| 66 | spec-v1.8.0 post-run, prompt 140 (`140-v180-verify-run-fixes.md`): `/verify-run` against the closed run returned FAIL on 3 of 8 checklist items — two of them the same REQ-V180-RPT-04 violation (no usage row for prompt 139; the report's Ledger row still provisional after T10 tagged), one the absent `economics.md` row. This prompt closes the documentation half: rows 65 and 66 here, the de-provisionalised Ledger row in the report, and one drift found while verifying rather than by the checklist — `AGENTS.md` still sized gate 3 at T7's 1217-test snapshot, three short of the 1220 the release actually collects. The `economics.md` paste is a lab-repo commit, outside this prompt. Checklist item 8 (delegation) passed clean at 9 delegated / 3 exempt / 0 unexplained — the first run since the task-brief-by-file seam landed. | claude-opus-5 (lab session) for the fixes; the verification itself ran in a clean-context `claude-sonnet-5` subagent, per `.claude/skills/verify-run/SKILL.md` | verifier subagent's own harness-reported total: 137,994 across 61 tool uses; the lab session's own count is not self-measurable by this harness | $0 marginal — subscription-metered; $0.00 live inference beyond gate 5's own zero-token checks |
| 67 | spec-v1.9.0 authoring, prompt 141 (`141-v190-spec-authoring.md`): brief → draft → citation audit → three Codex cross-review rounds, across four commits (`060a5e7`, `c820650`, `57dc746`, `457612d`). Challenger **OpenAI Codex `gpt-5.6-sol`**, 3 rounds, **30 findings, 30 accepted (12 adapted), 0 rejected** (one clause of R1-8 rejected), plus 4 lab-audit items; termination `round_limit` — round 3 still returned 1 Critical (the lab's own `BM25` corruption from round 2) and 6 High. Citation audit: 280 citations checked, 14 corrected. Final: 70 MUST, 62 test ids, 14 tasks, 16 scenarios, 7 mutation entries; spec 174,186 B + delta 30,222 B | claude-fable-5-1 (lab session; drafting and citation-audit subagents) and claude-opus-5 (applying subagents) | subagents 1,244,941 aggregate as reported by the harness (explore 100,088 + 111,681 · draft 263,722 · citation audit 141,602 · round-1 apply 197,023 · round-2 apply 198,849 · round-3 apply 231,976); Codex per round — r1 39,250 in / 5,875 out (3,796 reasoning), r2 46,167 / 4,793 (3,153), r3 51,733 / 4,853 (3,128). Lab-session main context: unknown, same constraint as rows 58–63 | unknown for the lab session; Codex ≈$0.75 at public API prices for 137,150 in / 15,521 out — an estimate scaled from row 63, marked as such; $0.00 to LM Studio (no bot inference this row) |
| 68 | spec-v1.9.0 `go` run, prompts 142–158 (`142-go-spec-v1.9.0.md` through `158-v190-t12-version-bump.md`): T0 preconditions (the operator's `EMBEDDING_MODEL`/`EMBEDDING_DIM` pair resolved live against the GPU box via a seven-step reversible `sqlite-vec` preflight rather than asked for blind — `text-embedding-nomic-embed-text-v1.5`/768; `v190-baseline` bench, S13 aborted at timeout); T1 the RAG storage schema (5→6) and migration (+ an operator-ratified REQ-V190-EC-03 extension to three more version-literal tests; a real `add_vectors` 2-tuple-vs-3-column bug found and fixed pre-commit); T2 the embeddings client and config (+ an operator-ratified RET-08 erratum, two more `test_v1_guardrails.py` tests needing a default embedding pair); T3 document parsing and chunking (+ a disclosed `T-V190-SEC-04` spec-id collision across two requirements, left unedited by operator decision; + a test-strengthening erratum on the overlap-clamp and corrupted-PDF tests); T4 the indexing pipeline (+ DOC-02's between-page budget check, found missing and filled within scope; + an operator-ratified zero-chunk `EmptyDocumentError` guard for a silently unsearchable document); T5 retrieval — vector/BM25/RRF/rerank/`Searcher` (+ an orchestrator-direct widening of SEC-05's AST walk to `rag.py`); T6 the fourth tool, dispatch, prompt line, attribution (+ an operator-ratified `tools_exposed` literal bump 3→4; one infra 403 retry, no partial commit); T7 the Telegram document flow, `/documents`, `/delete` (+ a real `getFile`/`file_path`-ordering bug found and fixed pre-commit, closing SEC-04's handler half); T8 the retrieval evaluation, gate 7 (three operator-ratified reranker-parameter attempts — `_RERANK_MAX_TOKENS` 128→1024→2048, `_RERANK_TIMEOUT_S` 20.0→120.0 — converging on a diagnosed genuine model-latency stochasticity in the deployed thinking model, not a code defect; recall@5=1.000 on every mode across all 6 live runs; disposition deferred to T13); T9 seven `v190-*` mutation entries and the gate-matrix repoint (105/105 killed twice; one infra rate-limit mid-task, orchestrator finished directly, `timeout_seconds` re-measured); T10 docs/config catch-up (README's `## Documents (RAG)` section, + a third self-precedented EC-03 repoint of `test_v170_bench.py`'s `report_path` self-check); T11 the `v190-candidate` benchmark (both baseline and candidate hit `bench.py`'s own abort-stops-the-run limitation, delta hand-assembled) plus the clean-context review (2 🟡 findings — one fixed, an envelope worst-case-arithmetic comment; one formally waived, the operator-ratified `rag.py` deviations) and the full `checks.py run --profile full` suite (15/16 PASS, gate 7 red and disclosed) with one process-hygiene incident cleanly recovered; T12 (this row) the version bump — `pyproject.toml`/`uv.lock` 1.8.0→1.9.0, `T-V190-VER-01` proven red-before/green-after — plus two more operator-ratified EC-03-class repoints this task's own required edit exposed: `tests/test_v190_agents.py`'s count-bearing-lines test (renamed, its own T10-era comment already named T12 as the point of change) and `tests/test_v180_version.py` (repointed from the live tree to the frozen `v1.8.0` git-tag blob, per REQ-V190-EC-03's no-deletion rule — establishing the convention every future version-pin test will need at its own retirement), README/AGENTS.md version and count-line updates, this table, the report's Ledger row, and `tg-post-v1.9.0.md` | claude-sonnet-5 (interactive session, delegating each task to a fresh general-purpose subagent per `AGENTS.md`'s Context discipline, one `code-reviewer` review at T11) | subagents' own harness-reported aggregates — see `docs/reports/report-v1.9.0.md` for the per-task breakdown; the interactive orchestrator session's own token count is not self-measurable by this harness | $0 marginal — Claude Code subscription-metered session, not per-token billed; live LM Studio inference (T0's `v190-baseline` bench, T8's gate-7 reranker attempts) is reference-priced, not real money; no OpenRouter inference beyond gate 5/selftest-live's zero-token reachability checks |
| 69 | spec-v1.9.0 post-run, prompt 159 (`159-v190-verify-run-fixes.md`): `/verify-run` against the closed run returned FAIL on 3 of 8 checklist items. Item 1 (gates) is gate 7's disclosed, operator-accepted known limitation reproduced independently by the verifier — `rag_eval.py` exit 2 on the reranker completion contract, `recall@5=1.000` on every mode — and carries into v1.10.0's scope rather than this prompt. Item 6 (no `economics.md` row) and item 8 (delegation record) are documentation, closed here: the report's Per-task delegation record covered T0–T11 only (12 of 14 tasks, `REQ-V190-EC-07` item 6), T9's row read "matched map" although the orchestrator finished that task in main context writing files gates run (§5.1's *writes source files* trigger, no closed-list exemption — now recorded as a deviation, not rephrased into one), RPT-02 item 4 claimed a `v190-T11.md` brief `git log --all` shows was never written, and the Ledger row was still a T12 snapshot. Final counts: **11 delegated / 2 exempt / 1 unexplained**. The `economics.md` paste is a lab-repo commit, outside this prompt. | claude-opus-5 (lab session) for the fixes; the verification itself ran in a clean-context `claude-sonnet-5` subagent, per `.claude/skills/verify-run/SKILL.md` | verifier subagent's own harness-reported total: 196,413 across 93 tool uses; the lab session's own count is not self-measurable by this harness | $0 marginal — subscription-metered; $0.00 live inference beyond the verifier's own gate 5 and gate 7 re-runs against the local LM Studio box |
| 70 | v1.9.1 T1, prompt 160 (`160-v191-rerank-contract.md`): fixed gate 7 forward — `response_format` passthrough (`llm/base.py`, both provider adapters, `llm/failover.py`), the rerank call now requests a JSON schema sized to the real candidate count (`rag.py`), `_RERANK_MAX_TOKENS`/`_RERANK_TIMEOUT_S` brought to measured reality (2048→128, 120.0→30.0), `LLM_RERANK_MODEL` routing mirroring `LLM_SUMMARY_MODEL`'s shape (`config.py`, `llm/__init__.py`, `bot.py`, `devtools/rag_eval.py`), gate 7's own `timeout_seconds` re-measured 600→580 (2x a real green run, 287.388s), two new `v191-*` mutation entries (both killed), and the two README spots asserting gate 7 "currently red" as ongoing fact corrected to match reality (with their test repointed) — `docs/reports/report-v1.9.0.md`'s own historical record of gate 7 shipping red is untouched. All seven gates green: pytest 1584 passed/1 skipped; mutation 107/107 killed (60m15.388s); gate 7 hybrid recall@5=1.000, real 4m47.388s. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; live LM Studio/OpenRouter inference during gates 5 and 7 is reference-priced, not separately billed |
| 71 | v1.9.1 T1 review finding, prompt 161 (`161-v191-review-finding.md`): clean-context review of `b959a63` returned one 🔴 — `tests/test_v190_retrieval.py`'s rerank-schema assertion compared `rag._rerank_response_format(3)` against itself, so a bug inside that function would keep the assertion green. Rewritten against independent literals (schema type, `order.items.maximum`, `order.maxItems`), narrowed to what the call site owes (a schema is sent, sized to 3 candidates), the full shape staying pinned in `tests/test_v191_rerank_contract.py`. Proven, not assumed: `"maximum": n` mutated to `n - 1` in `rag.py` makes the rewritten test fail (`assert 2 == 3`); reverted, it passes — before the rewrite the same mutation left it green. Test-only, single edit under every delegation threshold, no subagent spawned. | claude-opus-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session; no live inference this row (offline test-file edit and `pytest`/`ruff` re-runs only) |
| 72 | v1.9.1 T2, prompt 162 (`162-v191-t2-version-bump.md`): the release stamp — `pyproject.toml`/`uv.lock` 1.9.0→1.9.1, `T-V191-VER-01` proven red-before/green-after (`AssertionError: assert '1.9.0' == '1.9.1'` before, green after); `tests/test_v190_version.py` repointed to the frozen `v1.9.0` git-tag blob per REQ-V190-EC-03, the same treatment `tests/test_v180_version.py` received at v1.9.0 T12; README's Versioning table gains the v1.9.1 row, v1.9.0's row losing its now-false "this release" label; a self-found deviation (not in the brief) repointing `config/quality_gates.yaml`'s `lint-docs.report_path` 1.9.0→1.9.1 per REQ-V190-RPT-01's own stated convention, with both pinning tests (`tests/test_v170_bench.py`, `tests/test_v190_agents.py`) updated and renamed to match. T2's own first authoritative gate run turned up a real gate-7 red (three consecutive attempts, each failing a different subset of the ten answerable items) that needed a code change out of a version-bump task's scope — stopped and reported per its own brief's Stop clause rather than fixed in place; T3 (prompt 163) fixed it forward. T2 resumed after T3 landed: `AGENTS.md`'s gate-3/gate-6 count lines and `tests/test_v190_agents.py`'s pinning test moved to the release's real, final, post-T3 figures (1593 tests, 108 mutation entries); `docs/reports/report-v1.9.1.md` finished (the measured-evidence table, the clean-context-review record, a new T3 section covering the root cause and both of T3's own disclosures — the withdrawn `mistral-nemo` swap and the measured `_RERANK_TIMEOUT_S=15.0` basis — the per-task delegation record now naming T3 too, the seven-gate table with exit codes and wall clocks, the Ledger row); `docs/reports/tg-post-v1.9.1.md` written; this table's own rows 71–73. All seven gates re-run verbatim on the final tree (post-T3) as the authoritative run; tag `v1.9.1` created only after all seven were green. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; live LM Studio/OpenRouter inference during gates 5 and 7 is reference-priced, not separately billed |
| 73 | v1.9.1 T3, prompt 163 (`163-v191-rerank-retry.md`): gate 7's real failure mode, found by T2's own authoritative run — `llm/base.py:401` already classifies an HTTP 429 as `LLMError(retryable=True)`, but `rag.rerank()` caught `LLMError` and returned `None` without ever reading that flag, so a transient upstream error degraded silently to RRF order and took the gate down with it (measured: a 10-request burst against `google/gemma-3-12b-it`, T1's routed model, scored 1/10 ok, 9× HTTP 429 — a single-upstream model under a back-to-back burst). Fix: `rerank()` retries only a `retryable` `LLMError`, bounded `_RERANK_MAX_ATTEMPTS=3` with backoff `0.5s, 1.5s` (module constants), never retries a non-retryable error or an unparsable reply, logs every retry and every successful retry with the attempt number; `.env.example`'s recommended `LLM_RERANK_MODEL` moves to `openrouter:mistralai/mistral-small-24b-instruct-2501` (10/10 on the same burst, best measured recall 9/10 among survivors); one new mutation entry `v191-rerank-retry-dropped`, killed. Same-day amendment, evidence-driven: a proposed swap to `mistral-nemo` was withdrawn once two void gate-7 runs (one overlapping a live `mutation_check.py` mutation of `rag.py` itself, one contaminated by burst probes against the same upstream) were excluded — three clean sequential runs, `rag.py` sha `dbb834ae03af3597` verified unchanged before/after each, all exit 0, zero 429s; those same three clean runs showed one item's rerank succeeding only on attempt 3 after two 10.0s timeouts, the measured basis for `_RERANK_TIMEOUT_S` 10.0→15.0 and for the successful-retry log line. All seven gates green on T3's own tree: pytest 1591 passed; mutation 108/108 killed (61m28s); gate 7 hybrid+rerank recall@5=1.000, all 10 answerable items `rerank_succeeded=True` (151s). | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; live inference during the burst diagnosis, gate 5 and gate 7 is reference-priced, not separately billed |

Notes: rows 1–2 are the authoring cost of the specification, recorded per
the lab reporting standard; runtime data and secrets are never logged here.
Rows 3–13 are the implementation run (`go docs/spec/spec-v0.md`, prompt
`docs/prompts/01-go-spec-v0.md`), one row per stage of the spec's section 4
implementation order. The run executed as one continuous agent session; the
harness does not expose per-request input/output token counts or money cost to
the agent, so the per-step cells stay `unknown`. The Σ row was measured
afterwards from the local Claude Code session transcript (per-request `usage`
fields, deduplicated by request id); cost estimated at Anthropic's public API
price list (claude-opus-5: $5/$25 per MTok in/out, cache write ×1.25, cache
read ×0.1) — the session actually ran on a flat-rate subscription. The table keeps
its original five columns — the spec's REQ-EC-11 names a six-column variant
(`Tokens in` / `Tokens out`), but the instruction to keep the existing columns
takes precedence over the parenthetical.

Rows 15–24 are the spec-v1 implementation run (`go docs/spec/spec-v1.md`, prompt
`docs/prompts/03-go-spec-v1.md`), one row per stage of that spec's section-8
implementation order. As in the v0 run the harness exposes no per-request
counts to the agent, so the per-step cells stay `unknown`; the Σ row was measured
afterwards from the local Claude Code transcripts — the main session (108
requests) plus the `code-reviewer` subagent's own transcript (29 requests) —
keeping the final streaming `usage` row per request id. Cost is estimated at
Anthropic's public price list for claude-opus-5 ($5/$25 per MTok in/out, cache
write ×1.25, cache read ×0.1). Inference the *bot* itself spent during the
Appendix-B probes is separate and negligible: a handful of LM Studio calls
(local, free) and two OpenRouter calls on `google/gemini-2.5-flash-lite` at
$0.10/$0.40 per 1M tokens — well under a cent.

Rows 25–26 are the spec-v1.1 implementation run (`go docs/spec/spec-v1.1.md`,
prompt `docs/prompts/05-go-spec-v1.1.md`), covering the whole patch end to
end: tests, implementation, five gates (one of which was additionally re-run
with `docker` removed from `PATH` to confirm no test shells out to a real
daemon), the Appendix-B live driver script, the `code-reviewer` subagent's
review, its one fix cycle (1/5), and this report. Unlike the v0 and v1 runs,
this harness exposes the full per-request `usage` block directly in the
local session transcript, so row 25 needed no separate after-the-fact
reconstruction — the numbers above are a direct aggregation of that file
(249 requests as of the re-measurement taken immediately before this commit),
not an estimate. That re-measurement is itself the final action before the
commit records it, so row 25 necessarily excludes the handful of tokens this
commit step itself will spend — a residual, unavoidable undercount rather than
a stale one. The subagent call is asynchronous and its
transcript lives in a task-output file this session is explicitly instructed
not to read (to avoid pulling tool-call noise into context), so row 26 uses
only the aggregate the harness reported back in the task-completion
notification. Inference the *bot* itself spent during the Appendix-B driver
is separate and effectively free: LM Studio is local, and no OpenRouter call
was made at all — the throwaway `OPENROUTER_API_KEY` was a synthetic canary
that was deliberately never validated (`LLM_PROVIDER=lmstudio`,
`LLM_FAILOVER=off`).

Rows 27–28 are the spec-v1.2 implementation run (`go docs/spec/spec-v1.2.md`,
prompt `docs/prompts/07-go-spec-v1.2.md`), covering the security-audit patch
end to end: the section-10.2 tests and section-10.1 amendments, the
`devtools/mutation_check.py` gate built before it verified a single fix, the
five security fixes of section 5, six gates including a fix-and-rerun of the
mutation gate itself (see `report-v1.2.md`'s Fix cycles — not counted against
the 5-cycle repair budget, since it happened before the gates were run as the
reported sequence), the Appendix-B live driver (D1–D8 plus the C1/C3/C4/C6
v1.1 regression), documentation housekeeping, the `code-reviewer` subagent's
review, and the report. Unlike row 25, this session's **token split** stays
`not computed` rather than reconstructed or estimated: this run did not
re-measure a local transcript immediately before the commit the way row 25's
run did, and reporting an unmeasured number as if it were the same kind of
figure would misrepresent it. The **cost** cell is no longer `not computed`
(REQ-V13-CO-07): ≈$33.11, measured after the fact from the local session
transcript at the same public API prices as row 25, with that source named in
the cell — actual billing remains the flat-rate subscription. Inference the
*bot* itself spent during the
Appendix-B driver is separate and effectively free: LM Studio is local for
the ordinary-message check in D2, and no OpenRouter call was made — D1's and
C1's registered secret was a synthetic canary never sent through the LLM
client, and D2's ordinary-turn check used a scripted `FakeLLM`, not a live
provider call.

Row 29 is the authoring cost of `docs/spec/spec-v1.3.md`, recorded before
the `go` run so that the implementation rows that follow start from a known
baseline. It is an aggregate estimate, not a measurement of the kind rows
15–25 give: the writing happened in the lab session's main context, which
`tools/session-usage.py` cannot split by task, and the two reviewer
subagents only report their totals. No inference was spent by the bot
itself; the only live checks were `pytest --collect-only` (326 tests at
`1ecc35e`) and a `len()` of `agent.SYSTEM_PROMPT`, both offline.

Rows 30–31 are the spec-v1.3 implementation run (`go docs/spec/spec-v1.3.md`,
prompt `docs/prompts/09-go-spec-v1.3.md`), covering the token-economy patch end
to end — the four commits `69ebc75`, `f0572c8`, `c11f590` and the C4
documentation commit — with **19 prompt files** logged for this run
(`09-go-spec-v1.3.md` through `27-v13-TC9-docs.md`). The prompt count is the
only directly observable figure: every token and cost cell is the literal
`unknown`, never an estimate and never a number without a named source
(REQ-V13-RPT-06). The executor has no API to its own session usage, and this
run's harness displayed no usage or cost line to it — unlike row 25, where the
session transcript was available for a direct re-measurement, there is nothing
here to cite. The measured values are filled in after the run by the maintainer
from the lab's session transcripts (`tools/session-usage.py`, outside this
repository) and recorded in `economics.md`; `docs/reports/report-v1.3.md`'s
"Cumulative: v1 → v1.3" section copies whatever this table says, `unknown`
cells included. Inference the *bot* itself spent is measured and reported
separately in `docs/reports/report-v1.3.md`: the two full benchmark runs at
$0.09675 and $0.084729 and the O5 probe at $0.001876 are all
reference-priced ESTIMATES over free local LM Studio inference
(REQ-V13-PRC-03), and the only real money spent was the OpenRouter smoke's
$0.000212 on `google/gemini-2.5-flash-lite` — one S02 run, capped at
`--max-cost-usd 0.50`.

Row 34 is the spec-v1.4 implementation run (`go docs/spec/spec-v1.4.md`,
prompt `docs/prompts/31-go-spec-v1.4.md`), covering the whole patch end to
end across 11 commits (`30c7a16`, `f6e634d`, `51ac747`, `818bbde`, `485fcc5`,
`e5fc230`, `718e4eb`, `3fe860a`, `9a32cda`, `64aa5da`, plus this
`advisor()`-follow-up correction commit — report/errata/post/ledger
accuracy fixes, `docs/prompts/42-…`): as in rows 27/30–32, this harness exposed no per-request
usage or cost line to the executor, so every cell stays `unknown` rather
than an invented estimate — reconciled later by the maintainer from the
lab's own session tooling, outside this repository, per RPT-05's
disposition (this run's own T0 deviation 4: `economics.md` sits outside
the repository root, so this run's contribution to it is recorded here
and left for hand reconciliation, never written by the executor). Row 35
is the one subagent this run spawned, the T9 `code-reviewer` review: the
harness reported its 153,558-token aggregate in the task-completion
notification (same shape as rows 26/28), with no in/out/cache split
exposed. Inference the *bot* itself spent during Appendix-B (E5, E10, and
the D1/D3 unchanged-code regression check) is zero: E5 drove
`config.load_config` directly with no LLM call in the path; E10 was a
static file scan; D1/D3 relied on the existing, unchanged automated test
and mutation suites rather than a fresh live drive.
| 74 | v1.9.2 T1, prompt 164 (`164-v192-static-analysis.md`): whole-tree static analysis, all findings fixed — every skylos finding (27) individually decided against a four-step procedure (7 deleted: 2 unused functions with no caller anywhere, 5 unused variables including `dashboard_server.py`'s `_STATIC_ROUTES` (documented dead across three prior releases and now cleaned up on explicit operator request) and `storage.py`'s `_MIGRATION_2_TO_3` (added by an amend after the operator ruled the whole-tree cleanup order supersedes its own "kept, unlisted edit" note — see the row-8 erratum in `docs/reports/report-v1.9.2.md` §A); 20 kept and suppressed inline, naming the interface each protects — signal handlers, an `LLMClient` Protocol stub, stdlib `http.server` overrides, an argparse dispatch convention, an `HTMLParser` override, a `SpanSink` Protocol; 0 left unsuppressed); semgrep repointed to exclude `.semgrep/` from its own scan target (it was flagging its own vendored rule pack's example vulnerable snippets); 17 ruff bug-class hits individually reviewed (3 fixed — a mutable class-attribute default, two `zip()` sites given `strict=True`; 14 dismissed with reasons — parameterized SQL placeholders, a docker tmpfs mount spec, logger-guard `except: pass`, module-level state globals); `RUF100`/`B` added to the project's selected rule set after fixing the 15 `B905`/`B007` hits that surfaced (10 sliding-window `zip()` pairs at `strict=False`, 3 same-length pairs at `strict=True`, one stray unused loop variable) and 11 now-dead `# noqa` directives; a proposal table for the remaining ~5460 non-adopted hits (`S101`, `PLR2004`, `TRY003`, `ARG*`, `BLE`, `DTZ`, and the rest) with a one-line verdict each. All acceptance checks green on the final, amended commit (`6c9a904`): `ruff check .` 0; `ruff format --check .` unchanged at 75 files (deferred to prompt 165); `pytest` 1593 collected, all passed, ~73s; `bot.py --selftest` OK; `skylos . --gate --strict` 0 findings; `semgrep` 0 findings under `.semgrep/` at any severity, 1 total (the already-reviewed `storage.py:425` on this commit — `storage.py:439` pre-amend, `:487` after prompt 165's reformat — dismissed); `checks.py lint-docs` 0; a throwaway drift script confirmed 108/108 mutation `find` strings still match exactly once. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (offline static-analysis fixes and gate re-runs only) |
| 75 | v1.9.2 T1 §B, prompt 165 (`165-v192-reformat.md`): closed REQ-V15-NG-04 — whole-tree `ruff format .` (63 non-`docs/` `.py` files; 11 `docs/*.md` files it also touched were reverted and `docs/**` is now excluded in `[tool.ruff.format]`, formalising spec-v1.5's own already-decided policy). Stopped mid-task on two unresolved items, both settled by an explicit operator ruling before this prompt finished: `devtools/bench_scenarios.py`'s reformat was reverted and permanently excluded (REQ-V13-BEN-12 — `scenarios_sha256()` hashes its raw bytes, pinned in 15 committed `docs/assets/bench/*.json` artefacts that are never edited to match new bytes); `storage.py`'s `_MIGRATION_2_TO_3` (skylos finding #8, left "kept, unsuppressed" in prompt 164 because the flagged line opens a triple-quoted SQL string) was deleted under §A.3 with (a)(b)(c) evidence, and commit 1 (`f421621`) was amended to `6c9a904` to carry it, moving the post-fix skylos count from 1 to 0. Nine mutation `find`/`replace` pairs re-derived against the reformatted source (same mutation semantics, only byte layout changed) across `tools.py` (×2), `agent.py` (×2), `devtools/bench.py` (×3), `storage.py`, `llm/failover.py`; one incidental fix the reformat forced (a lambda in `tests/test_v11_patch.py` whose signature ruff cannot wrap, converted to a named nested function). `ruff-format-all` added (`config/quality_gates.yaml`) — the same no-runner-change shape as `ruff-check-all` next to diff-scoped `ruff-check` — wired into `pre-push`/`full` in place of the old diff-scoped `ruff-format`, which now stays only in `pre-commit`; the gate matrix in `docs/spec/spec-v1.9.0-delta-1.md` and `tests/test_v15_standards.py`'s label map both updated to match (added, not replaced). Acceptance: `ruff check .` exit 0; `ruff format --check .` exit 0 (92 files already formatted, 0 would reformat); `pytest` 1593 collected, all passed, ~71–72s; drift script 108/108 before and after; all nine `--only` reruns killed. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (offline reformat, mutation re-derivation, and gate re-runs only) |
| 76 | v1.9.2 T1, prompt 166 (`166-v192-t1-review-findings.md`): closed nine clean-context review findings against `6c9a904`+`d25d664` in one commit, without rewriting either — `docs/llm-usage.md` row 74 rewritten to match `6c9a904`'s actual content (7 deleted, 20 suppressed, 0 unsuppressed, skylos 0; 🔴); an erratum added to `docs/reports/report-v1.9.2.md` §A row 8 naming both the stale pre-amend commit message and `docs/spec/spec-v1.7.0.md:2413`'s REQ-V170-NG-14 non-goal, explaining why the operator's explicit whole-tree cleanup order — not a side effect — supersedes it (🟠 ×2); `docs/spec/spec-v1.9.0-delta-1.md`'s citations refreshed to the current tree (`tests/test_v15_standards.py:1718-1732`, `:1689-1715`; 🟠); `ruff-format-all` added to the `pre-commit` profile alongside `ruff-format`, whose own comment now explains the real reason `blocking_paths` must stay — `devtools/checks.py:892`'s `replay` reads it for historical commits — with the gate-matrix table and its test mapping both updated to match (🟠); `storage.py:439`→`:487` citation fixed in report §C.1/§C.2 (🟡); a disposition line added to report §B noting the brief's own doc-update instruction was moot (no such "format is shadow" line ever existed in AGENTS.md or README) (🟡); a delegation-record note on both commits' accurate `Claude Sonnet 5` trailers (🟡); one sentence added to report §A's suppression paragraph naming the eight functions (`_handle_signal`, `cmd_doctor`, `cmd_lint_docs`, `log_message`, `log_error`, `handle_starttag`, `handle_startendtag`, `NullSink.write`) whose `# skylos: ignore` sits on the `def` line itself, also hiding them from skylos's unused-function check (🟡). Acceptance green: `checks.py lint-docs` exit 0; `pytest tests/test_v15_standards.py` exit 0; `ruff check .` exit 0; `ruff format --check .` exit 0; `checks.py run --profile pre-commit` exit 0. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (documentation-only corrections and gate re-runs) |
| 77 | v1.9.2 T2 §1, prompt 167 (`167-v192-mutation-order.md`): gate 6's `default_runner` now orders the explicit test-file list it hands `pytest -x` by relevance to the mutated module (`ordered_test_files`, five tiers — version prefix, same-named module file, direct importers, one-hop importers, everything else — a permutation of every test file by construction, asserted in the function) instead of relying on pytest's default alphabetical order; `run_one` passes `mutation` through to the runner (`default_runner(mutation)`); a once-per-invocation collect-count guard in `main()` (`_shrink_counts`) closes the silent-shrink hole an explicit file list opens (REQ-V13-CO-06 / REQ-V15-GATE-04), with its own mutation entry `v192-mutation-order-shrink-unchecked` and a killer test that fakes both counts and mocks `run_all` rather than shelling out. Measured before (old alphabetical order, `c21ffb3`) vs. after (this commit), each `--select` subset run alone, sequential, nothing concurrent: v15- 98.33s→14.04s (4/4 both), v160- 590.26s→101.19s (11/11 both), v170- 565.75s→25.46s (9/9 both), v180- 476.95s→117.09s (6/6 both), v190- 495.91s→23.41s (7/7 both) — total 2227.2s→281.19s across the five subsets (~7.9x). False-kill guard: the unmutated tree run once under each of 18 distinct-mutated-path orderings (more than the brief's "about a dozen" estimate — this tree has 18 distinct paths across 109 entries), all 18 exit 0. Drift script: 109/109 matched, 0 drifted, before and after. `pytest` wall ×3 on the final tree: 79.71s, 80.44s, 80.66s (1598 collected, up from 1593 — five new `tests/test_mutation_check.py` tests). `config/quality_gates.yaml`'s five `mutation-v*` `timeout_seconds` re-measured by the file's own 2x rule from the after numbers (v15 220→30, v160 1190→210, v170 1070→60, v180 810→240, v190 1020→50); `mutation-all` untouched (T3's scope). Deviation recorded: the brief's own commit-trailer instruction (`docs/spec/task-briefs/v192-T2.md` §6) names `Claude Opus 5`; the actual executor is `claude-sonnet-5`, and the commit trailer below is accurate to that rather than the brief's literal text. Acceptance green: `ruff check .` 0; `ruff format --check .` 0; `pytest` 0 (above); `bot.py --selftest` OK; `checks.py lint-docs` 0; `mutation_check.py --only v192-mutation-order-shrink-unchecked` killed; `mutation_check.py --select v190-` killed 7/7 in 23.41s. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (offline mutation-runner reordering, real subprocess pytest/mutation re-runs for measurement, no LLM calls) |
| 78 | v1.9.2 T2 §3/§4, prompt 168 (`168-v192-pytest-time.md`): pytest-xdist 3.8.0 adopted as a dev dependency (`addopts = "-q -n auto"` in `pyproject.toml`, the brief's own measured plateau at 8 workers on this 16-core box); the mutation runner (`devtools/mutation_check.py:default_runner`) opts out with `-n 0` (added only in this commit, once xdist is actually installed — verified empirically that `-n 0` errors "unrecognized arguments" a commit earlier, before the dependency existed). Isolation proof: five consecutive `pytest -q` runs (23.42s/24.80s/24.57s/25.23s/24.83s, all exit 0) plus one `--dist loadfile` run (38.82s, exit 0), `git status --short` showing only this run's own in-progress edits after each — no test-leaked files (v1.5.1 D1 precedent). §3.1: `live_server`'s `serve_forever(poll_interval=0.01)` at the three call sites the brief names (`tests/test_v160_dashboard.py:705,859`, `tests/test_v180_conversations.py:479`), fixture-only. §3.2: `test_t_ex_04` (`EXEC_KILL_GRACE_S` 5.0→0.5 via monkeypatch, elapsed bound 12.0→4.0, measured 6.00s→1.51s) is the one patched down; `test_t_ex_05` (`EXEC_DRAIN_GRACE_S`) is kept unpatched at the real production 2.0s value (measured 4.04s, unchanged) so that constant stays exercised at its shipped value — the choice and reason are recorded in both tests' own comments. `config/quality_gates.yaml`'s `pytest` gate `timeout_seconds` re-measured by the file's 2x rule from the five isolation-run numbers: worst case 25.23s → 60s (down from 120s). §4 duplicates: three cheap scans (REQ-id frequency across `tests/*.py`, spot-checked the highest-frequency id `REQ-V13-TOO-02` — 10 mentions across 5 files, each a distinct sub-case, not a duplicate assertion; a test-name-family heuristic for manual-parametrize candidates — zero families of 4+ near-identical names beyond what already uses `@pytest.mark.parametrize`; the brief's own top-10 `--durations` list cross-checked — each of the ~10 real-tool/timeout tests covers a distinct mechanism). 0 candidates removed, 0 parametrize-merges found — the suite's time is in section 1's ten tests, not in duplicates. Operator-facing estimate for a future full `mutation-all` run under this task's reordering (T3's own re-measurement, not run here): the five measured `--select` subsets (37 of 109 entries) averaged 7.60s/entry after reordering; scaled to all 108 pre-T2 entries, ≈820s (~13.7min), versus the last directly measured `mutation-all` wall (T9, 105 entries, 3953.309s ≈ 66min) scaled the same way to 108 entries (≈4066s ≈ 67.8min) — a rough ~5x projected speedup, caveated that the 71 unmeasured entries (mostly `v13-`, 34 of them) may not track the measured average. Acceptance green: `uv sync --locked` 0 (pytest-xdist installed); `ruff check .` 0; `ruff format --check .` 0; `pytest` 0, 1598 collected (unchanged); `bot.py --selftest` OK; `checks.py lint-docs` 0; drift script 109/109 matched, 0 drifted; `mutation_check.py --only v192-mutation-order-shrink-unchecked` killed; `mutation_check.py --select v190-` killed 7/7 in 22.71s. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (offline xdist adoption, fixture/grace fixes, real subprocess pytest/mutation re-runs for measurement, no LLM calls) |
| 79 | v1.9.2 T2, prompt 169 (`169-v192-t2-report-sections.md`): closed a paperwork gap discovered by this run's own pre-completion review (advisor call) — `docs/spec/task-briefs/v192-T2.md` §6 names a report section per commit ("T2 — gate 6" for prompt 167; "T2 — gate 3" and "T2 — duplicates" plus the T2 delegation-record line for prompt 168), but neither `92b667c` nor `c3a38ea` touched `docs/reports/report-v1.9.2.md`. Added all three sections plus the delegation-record line, transcribing content already verified in rows 77/78 (no new measurement); a fourth subsection ("Deviation: commit trailer model") records the brief's `Claude Opus 5` trailer text against the actual `claude-sonnet-5` executor, mirrored from row 77. Precedent: T1's own prompt 166 (row 76) — a follow-up prompt/commit fixing a discovered paperwork gap rather than amending closed commits; recorded here as a deviation from the brief's literal "two commits, two prompt files" count (section 6). Acceptance: `checks.py lint-docs` exit 0; no code/test/config path touched. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (documentation-only transcription) |
| 80 | v1.9.2 T2 review, prompt 170 (`170-v192-t2-review-findings.md`): closed a clean-context review's findings against `92b667c`+`c3a38ea`+`260c7e1` (no 🔴, two 🟠, six 🟡) in one commit, without rewriting any of the three. 🟠 Finding 1 (shrink guard fails open): `_collect_count` now passes `-qq` explicitly (never relying on `addopts` to reach that verbosity) and returns `(count, returncode)`; `main()`'s guard requires both returncodes `== 0` and both counts `> 0` before comparing them; new mutation entry `v192-mutation-order-shrink-zero-accepted` (drops the `> 0` condition) with its own killer test (fakes `(0,0,0,0)`, mocks `run_all`), plus a direct unit test for the non-zero-returncode case. 🟠 Finding 2 (three timeouts couldn't report a survivor): measured one full single-process suite run directly (`pytest -q -n 0`, 66.22s/65.33s, close to the review's ~69s estimate); corrected sizing rule written once above `mutation-v15` in `config/quality_gates.yaml` (2x killed-path wall + 70s survivor floor, rounded up to 10s), re-sizing `mutation-v15` 30s→100s, `mutation-v160` 210s→280s, `mutation-v170` 60s→130s, `mutation-v180` 240s→310s, `mutation-v190` 50s→120s; `mutation-all` left as-is with a note that T3 re-measures under the rule; the SIGKILL-traps-only-SIGINT/SIGTERM hazard noted as a v1.10.0 item, `checks.py` untouched. 🟡 Finding 6 (ordering missed import shapes): `_imports` now also matches `from <pkg> import <sub>` (word-bounded) and indented imports; `_module_name` maps `pkg/__init__.py`→`pkg`; tier 4 now scans `llm/*.py` and `devtools/*.py`. Tiers re-printed for `v13-bench-gate-threshold` (`devtools.bench`) and `v13-routing-agent-too` (`llm`, was `llm.__init__`) — both now find their real importers (`test_dashboard.py`, `test_v14_patch.py`, `test_v160_bench.py`, `test_v170_bench.py` / `test_agent.py`, `test_bench.py`, `test_failover.py`, `test_history_stub.py`, `test_llm.py`) via tier 3, none of which were found before. `--select v13-` (34 entries) re-measured once, alone: **34/34 killed, real=210.542s** (6.19s/entry, faster than the five-subset sample's 7.60s/entry average), replacing the report's earlier rough ≈820s full-run projection. 🟡 Finding 7 (shrink guard ran before id/prefix validation): moved after both checks, so `--only typo` fails instantly again. 🟡 Finding 3 (report claimed no repointing needed): reworded, naming the three now-stale count-bearing citations, deferred to T3. 🟡 Finding 4 (`mutation-all` comment) and Finding 5 (pytest gate comment naming the 16-core box, with 4-worker 26.9s / 8-worker plateau reference points) added. 🟡 Finding 8 (duplicates section reproducibility): added the exact `grep` commands for scans (a)/(b) and named `pytest -q --durations=50` as the source for (c)'s manual cross-check. `--select v15-` re-measured once: 4/4 killed, real=13.95s (unchanged from before, confirming the import-matching fix didn't touch this subset). Acceptance green: `ruff check .` 0; `ruff format --check .` 0; `pytest` 0, 1600 collected (two new tests); `checks.py lint-docs` 0; drift script 110/110 matched, 0 drifted; both new/updated mutation entries killed; both `--select` re-runs killed n/n with wall recorded. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (offline code fixes, real subprocess pytest/mutation re-runs for measurement, no LLM calls) |
| 81 | v1.9.2 T3, prompt 171 (`171-v192-t3-version-bump.md`): closed the patch — `pyproject.toml`'s `project.version` 1.9.1→1.9.2, `uv.lock` regenerated; `tests/test_v192_version.py` (`T-V192-VER-01`) red before the bump (`AssertionError: assert '1.9.1' == '1.9.2'`), green after; `tests/test_v191_version.py` repointed to the frozen `v1.9.1` git-tag blob (`git show v1.9.1:pyproject.toml`), the same convention `tests/test_v180_version.py`/`tests/test_v190_version.py` already carry, never deleted (REQ-V190-EC-03). Count-bearing lines moved to the tree's real final numbers, measured after every other edit: `AGENTS.md`'s gate-3 line 1593→1601, gate-6 line 108→110 (narrative extended to name v1.9.2 T2's two new mutation entries); `tests/test_v190_agents.py`'s own count-bearing test renamed and repointed to assert 1601/110; README's release table gained a `v1.9.2` row, the `v1.9.1` row's "this release" clause dropped; `config/quality_gates.yaml`'s `lint-docs.report_path` repointed `report-v1.9.1.md`→`report-v1.9.2.md` (REQ-V190-RPT-01) with both its pinning tests (`tests/test_v190_agents.py`, `tests/test_v170_bench.py`) renamed/repointed to match, found via pre-flight grep, not named in the brief. `docs/reports/report-v1.9.2.md` finished with the consolidated seven-gate table, the gate-6 before/after (61m39s at 108 entries on v1.9.1 → 680.49s at 110 entries this run, ~5.44x), a cross-reference to both clean-context reviews (not duplicated), the complete per-task delegation record, four disclosures (commit-trailer model; the pre-existing gate-6 SIGKILL-on-timeout hazard, a v1.10.0 item; the tightest mutation-* timeouts confirmed against this run's total, since an unselected `mutation-all` run prints no per-subset wall; the pre-push five-subsets-vs-`mutation-all` tradeoff, both numbers written, decided by neither this task nor the report), the open tail from v1.9.1 (one rerank call succeeding only on attempt 3 — recurs in this run's gate-7 log too, gate 7 still green; the advisory conversation smoke, red in v1.9.1 T2's run, passed in this one), and the Ledger row. `docs/reports/tg-post-v1.9.2.md` (1315 chars) and this row close the reporting. All seven gates run verbatim, in order, on the final tree, nothing else on the box during gate 6, gate 6 and gate 7 never concurrent: `uv sync --locked` 0 (0.021s); `ruff check .` 0 (0.035s); `pytest` 0 (22.912s, 1600 passed/1 skipped, 1601 collected); `bot.py --selftest` 0 (0.542s); `bot.py --selftest-live` 0 (1.732s, all seven live checks OK including `lmstudio`); `mutation_check.py` (no `--select`) 0 (680.49s, 110/110 killed, 0 survived/errored/drifted); `rag_eval.py` 0 (186.998s, hybrid recall@5=1.000, hybrid+rerank recall@5=1.000, PASS). `mutation-all.timeout_seconds` re-sized by the survivor-safe rule from this run's own measured wall: 7910s→1440s (2×680.49s + 70s = 1430.98s, rounded up). Annotated tag `v1.9.2` created on the release commit after all seven were confirmed green. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; live inference on gate 7 (the rerank call plus the advisory conversation-aware smoke) at reference OpenRouter prices, no real spend tracked or exposed to this session |
| 82 | v1.9.2 T3 erratum, prompt 172 (`172-v192-t3-erratum.md`): a self-found paperwork correction to `c431987`'s own `docs/reports/report-v1.9.2.md`, surfaced by the `advisor()` call made before reporting T3 done. (1) The Ledger row's Bugs column had transcribed the T3 brief's literal finding tally without checking it against the two review briefs it summarizes — counting `v192-T1-review.md` (items 1–9) and `v192-T2-review.md` (items 1–8) directly gives 9 (1🔴/4🟠/4🟡) and 8 (0🔴/3🟠/5🟡), matching `c21ffb3`'s own commit message ("nine findings") and this report's T2 delegation record ("eight findings"); the brief's own arithmetic was one 🟡 over on each side. Corrected in the review summary and the Ledger row, with an erratum note above the row naming the discrepancy — same erratum-not-rewrite convention `c21ffb3` already used. (2) The seven-gate table's "final tree" framing didn't disclose that five things landed in `c431987` after gate 7 finished, using gate 6/7's own results as input: `config/quality_gates.yaml`'s `mutation-all.timeout_seconds` (7910→1440), the whole T3 report section, `tg-post-v1.9.2.md`, `llm-usage.md` row 81, and the Ledger row — added as disclosure (e), naming what was re-run afterward on the tree as committed (`ruff check .`, `ruff format --check .`, `pytest` 1601 collected, `checks.py lint-docs`, all exit 0) versus what wasn't (gates 4/5/6/7; re-running an 11m20s mutation pass to validate a comment-only edit was judged not worth it). Also fixed disclosure (c)'s false claim that gate 6 "uses `mutation-all`'s own single timeout" — it doesn't, since `AGENTS.md`'s literal invocation bypasses `checks.py`'s gate executor entirely, so no `quality_gates.yaml` timeout applied to this run at all; added one sentence naming that 1440s carries much less slack against this box's own documented shared/contended variance (5040s/4690s/5620s/7910s history) than 7910s did, interacting with disclosure (b)'s SIGKILL-on-timeout hazard. `c431987` not amended; the `v1.9.2` tag moved to this commit (still unpushed). Acceptance green: `ruff check .` 0; `ruff format --check .` 0; `pytest` 0, 1601 collected (unchanged); `checks.py lint-docs` 0. | claude-sonnet-5 (Claude Code) | harness does not expose per-request token counts to this session; not self-measurable | $0 marginal — Claude Code subscription-metered session, not per-token billed; no live inference this row (documentation-only reconciliation, no gate re-run beyond the four listed) |
