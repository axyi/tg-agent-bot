# Prompt 208 — v1.10.1 T5: clean-context review (REV-01)

- **Date:** 2026-09-17
- **Executor model:** claude-sonnet-5
- **Model reason:** `code-reviewer` subagent (`.claude/agents/code-reviewer.md`,
  `model: sonnet`), own clean context, never the writing context (REV-01).
- **Harness:** Claude Code (background session, subagent)
- **Stage:** T5
- **Owner of:** review findings only (no source edit — the reviewer never
  modifies files); fixes land in a separate orchestrator commit
- **REQ ids:** REQ-V1101-REV-01

## Goal

Review commits `1d96ca0..HEAD` (T0 through T4: CFG-02, EMB-01/02,
G5-01/02, GC-01, RT-01…05, RUN-01, PRM-01/02) against the standard
checklist plus REV-01's nine spec-specific items.

## Constraints

Read-only. `docs/spec/spec-v1.10.1.md` is the contract for this review.
Item 8 (mutation `find` strings, `mutation-all`'s count) is only
partially checkable at this point — the six `v1101-*` mutation entries
land at T6, after this review; note what's checkable now (the gate-matrix
repoint from T2) and what to defer.

## Acceptance

Findings reported by severity with `file:line`, fixed or waived with a
reason by the orchestrator in the same task.

## Stop

N/A — a review does not stop the run; findings are triaged and closed by
the orchestrator.
