# Prompt 231 — v1.10.4 T3: clean-context review

- **Date:** 2026-09-18
- **Executor model:** claude-sonnet-5
- **Model reason:** independent review in a clean context, before the
  live gates that matter (`REQ-V1104-REV-01`); the pinned `code-reviewer`
  subagent (`model: sonnet`).
- **Harness:** Claude Code (background session, delegated subagent)
- **Stage:** T3
- **Owner of:** `docs/reports/report-v1.10.4.md` (`## T3` section) —
  read-only otherwise; any source-writing fix lands under a separate
  brief `docs/spec/task-briefs/v1104-T3.md`
- **REQ ids:** REQ-V1104-REV-01

## Goal

Review commits `b689195..09cca94` (T0-T2) against `docs/spec/spec-v1.10.4.md`
and REV-01's five checklist items in a clean context, independent of the
sessions that wrote T1 and T2. Confirm: (1) every NG-01-listed
production/evaluation-instrument file is byte-equal to `f3ce1a5`, and
`devtools/mutation_check.py` differs only by Appendix D's five entries
and rationale comment, `config/quality_gates.yaml` differs only by
MUT-02's hunks and PIN-02's `report_path`; (2) every rewritten pin
asserts presence, contiguity and order — no `tail ==`, whole-file "is
now" count, release-name anchor, frozen-matrix equality or "no later
row" assertion anywhere in `tests/` (NG-10); every renamed test keeps
its intent; no test deleted; (3) every T0 inventory hit of EC-02's parts
(i)-(iii) and (v) is classified, no unclassified hit survived; (4) the
isolation proofs name `T-V1103-LINT-09` for entry 4 and the inner
predicate for entry 2; (5) no new dependency, no secret value anywhere,
no live call in any test, `git rev-parse stash@{0}` still equals
`e3c6e3ff…`.

## Constraints

Read-only review. Findings are fixed or waived with a reason; a fix that
writes source is delegated by brief `docs/spec/task-briefs/v1104-T3.md`
— never self-fixed in the review's own context. Never self-review in the
writing context (this review must be independent of the T1/T2 sessions).

## Acceptance

Every REV-01 item PASS or a disclosed, reasoned waiver; any finding
requiring a source change is either fixed via a delegated brief (this
task, if the fix is small and mechanical) or reported back to the
orchestrator for triage. `## T3` section appended to
`docs/reports/report-v1.10.4.md`.

## Stop

A finding whose fix would touch an NG-01-listed instrument file, widen
scope beyond the spec, or require more than a small mechanical change is
reported back to the orchestrator, not fixed unilaterally.
