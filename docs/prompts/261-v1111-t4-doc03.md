# Prompt 261 — v1.11.1 T4 first commit: DOC-03, the v1.11.0 paperwork corrected

- **Date:** 2026-09-24
- **Executor model:** claude-sonnet-5
- **Model reason:** *artefacts only* per §5.1/EC-03 — five precise text
  edits to two already-shipped report files, no test, no bookkeeping;
  not a source-writing task, so no delegation.
- **Harness:** Claude Code (background session, orchestrator, main
  context)
- **Stage:** T4 (first commit)
- **Owner of:** `docs/reports/report-v1.11.0.md`,
  `docs/reports/tg-post-v1.11.0.md` exclusively — no other path, no
  test, no bookkeeping (this prompt file and its `docs/llm-usage.md`
  row land in T4's second commit per EC-03, not here)
- **REQ ids:** REQ-V1111-DOC-03

## Goal

Correct five factual errors in the already-shipped v1.11.0 paperwork,
discovered while authoring spec-v1.11.1: (1) `report-v1.11.0.md:1945`'s
`| 17 (236-252) |` undercounted the true prompt range/count, corrected
to `| 19 (236-254) |`; (2) `tg-post-v1.11.0.md:23`'s matching `промпты
236–252` corrected to `236–254`; (3) the `## T2` section
(`report-v1.11.0.md:276`) gains a disclosed-EC-05-deviation paragraph
about prompt 239 being cited by three commits; (4) the T7 Phase A
bullet (`:1700`) replaces a stale clause about the `quality_gates.yaml`
comment fix's attribution with the accurate bundling disclosure; (5)
the T7 review's finding-2 waiver (`:1534`) corrects a citation typo,
`:2222` → `:2229` (the live tree's actual second `cancel_reason` write
site).

## Constraints

*Artefacts only* — exactly `docs/reports/report-v1.11.0.md` and
`docs/reports/tg-post-v1.11.0.md` in this commit's diff, nothing else
(EC-03's first-commit path restriction). No source, test or config file
touched. `tg-post-v1.11.0.md`'s `wc -m` stays ≤ 1500. `.env`, `data/`,
`bot.db`, `sandbox/`, `exec_audit.jsonl` never opened. `--no-verify`
never used.

## Acceptance

Each of the five sites reads exactly as spec `:541-563` specifies;
`git show --stat` on the commit names exactly the two files;
`tg-post-v1.11.0.md`'s character count ≤ 1500; gates 1–4 and
`devtools/checks.py lint-docs` exit 0 (the latter now walks the edited
`report-v1.11.0.md`, since `config/quality_gates.yaml`'s `report_path`
still names it); `T-V1111-DOC-03` (landing in T4's second commit) is
green by then per EC-02's stated-carve-out timing.

## Stop

A site's live text differing from the spec's quoted `was` by more than
a trivial rewrap would be a spec ambiguity — the stop route; none
occurred, all five sites matched the spec's citations exactly.
