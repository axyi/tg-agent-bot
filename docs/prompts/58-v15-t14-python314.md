# Prompt 58 — spec-v1.5 T14: Python 3.14 bump

- **Date:** 2026-09-04
- **Executor model:** claude-sonnet-5 (Claude Code)
- **Model reason:** REQ-V15-DEP-01 fully specifies the four lines to
  move and the exclusion list (no ruff-pin change, sandbox image is
  T15's); the only judgment call was verifying 3.14.7 is really
  installed and matches the host before pinning it, then running and
  waiting out the full offline gate set including the unselected
  72-entry mutation gate twice (once standalone, once inside `full`)
- **Harness:** Claude Code
- **Stage:** T14
- **Owner of:** `.python-version`, `pyproject.toml` (`requires-python`,
  `[tool.ruff] target-version`), `uv.lock` (regenerated), `AGENTS.md`
  (Stack line), `README.md` (uv/Python line), `docs/reports/
  report-v1.5.md` (T14 section, RLM row), `docs/prompts/
  58-v15-t14-python314.md` (new)
- **REQ ids:** REQ-V15-DEP-01, REQ-V15-DEP-02, REQ-V15-DEP-05

## Goal

Bump the project's own runtime to Python 3.14: `.python-version`,
`requires-python`, `[tool.ruff] target-version`, regenerate `uv.lock`,
update `AGENTS.md` and `README.md`'s runtime mentions, then run the
full gate set (all six verbatim commands plus every new gate via
`checks.py run --profile full --since <base>`) and confirm it is green
on the new interpreter.

## Constraints

- No ruff-pin change here — T13 already owns both ruff pins.
- The sandbox image (`README.md`'s `docker pull` line,
  `config.py`'s `DEFAULT_DOCKER_IMAGE`, its test fixtures) is T15's,
  not this task's — left untouched.
- Acceptance is the full gate set green on 3.14, not merely the test
  suite passing — a runtime bump can break what no unit test covers.

## Acceptance

- `.python-version` → `3.14`; `requires-python` →
  `">=3.13,<3.15"`; `target-version` → `"py313"`; `uv.lock`
  regenerated; `AGENTS.md`/`README.md` say 3.14.
- All six verbatim gates green on 3.14, including the unselected
  mutation gate (all entries `killed`) and `--selftest-live`.
- `checks.py run --profile full --since <base>` green except the one
  expected, sequencing-dependent `lint-docs` failure (the report's
  ledger row, still a placeholder until T18).
- Test count > 728.

## Stop

If any gate fails on 3.14 for a reason connected to the interpreter
bump itself (not the known ledger-row placeholder), stop and fix the
actual defect within the 5-cycle repair budget before claiming the
bump complete.
