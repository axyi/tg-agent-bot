# Prompt 259 — v1.11.1 T2: table widths, the /sessions empty state, DOC_LIMIT_REPLY, one batch constant

- **Date:** 2026-09-24
- **Executor model:** claude-sonnet-5
- **Model reason:** a bounded, well-scoped implementation task (five small
  source edits plus their catalogued test-pin rewrites) delegated to a
  subagent per `AGENTS.md`'s context-discipline rule (a task that writes
  source code a gate compiles/imports/runs).
- **Harness:** Claude Code (subagent)
- **Stage:** T2
- **Owner of:** `bot.py`, `documents.py`, `README.md`,
  `tests/test_v1111_tab.py` (new), `tests/test_v1110_doc.py`,
  `tests/test_v1110_ing.py`, `tests/test_v190_agents.py`,
  `tests/test_v1110_ver.py`, this prompt file,
  `docs/reports/report-v1.11.1.md`, `docs/llm-usage.md`
- **REQ ids:** REQ-V1111-TAB-01, REQ-V1111-TAB-02, REQ-V1111-TAB-03,
  REQ-V1111-TAB-04, REQ-V1111-TAB-05

## Goal

Implement the five TAB requirements per `docs/spec/task-briefs/v1111-T2.md`
and `docs/spec/spec-v1.11.1.md` sec.3.2: `/documents`' `#` column widens to
5 units (ids render whole to 99,999) and `file` narrows to 22
(`bot.py:2391`'s `max_width`); `/model`'s status table widens its `field`
column to 16 (no label ever truncates) and narrows `value` to 40
(`bot.py:1555`'s `max_width`, plus the docstring correction at
`bot.py:1528-1534`); a new `SESSIONS_EMPTY_REPLY` constant
(`bot.py:141`) and `_handle_sessions`' new empty-state branch
(`bot.py:2470-2472`) send it on the plain path before any table;
`DOC_LIMIT_REPLY` (`bot.py:109`) names both `/delete` forms; and
`documents.py`'s own `EMBED_BATCH_SIZE` constant is removed in favour of
importing `llm.embeddings.BATCH_SIZE` (`documents.py:30-31`, the five call
sites at `:552-558`). The five catalogued test-pin sites
(`docs/spec/task-briefs/v1111-T0-pin-inventory.md`) are rewritten in
place. README's `/documents` sample, `/model` prose, `/sessions` empty
wording, the `## Error behaviour` table (a new row for TAB-03, the
existing DOC_LIMIT_REPLY row updated for TAB-04) and the embeddings
paragraph are updated to match.

## Constraints

Never open/print/grep `.env`, `data/`, `bot.db`, `sandbox/`,
`exec_audit.jsonl`. No LAN address or secret literal anywhere. Zero new
dependencies (`from llm.embeddings import BATCH_SIZE` is an internal,
same-repo import). `tests/test_v1111_tab.py`'s six tests are written
first, red-checked against the pre-change tree, then the source edits
land. `--no-verify` never.

## Acceptance

`tests/test_v1111_tab.py::test_t_v1111_tab_01_documents_id_width_five_round_trip`,
`::test_t_v1111_tab_02_readme_documents_sample_byte_equal`,
`::test_t_v1111_tab_03_model_status_label_whole`,
`::test_t_v1111_tab_04_sessions_empty_plain`,
`::test_t_v1111_tab_05_doc_limit_reply_names_both_forms`,
`::test_t_v1111_tab_06_single_batch_constant` all pass; the full suite
green (`uv run --locked pytest`); gates 1-4
(`uv sync --locked`, `uv run --locked ruff check .`, `uv run --locked
pytest`, `uv run --locked python bot.py --selftest`) and `uv run --locked
python devtools/checks.py lint-docs` all exit 0.

## Stop

None triggered — a bounded implementation task with a pre-catalogued pin
inventory; no spec ambiguity, no exhausted repair budget.
