# Prompt 197 — v1.10.0 T5: the runner, offline

- **Date:** 2026-09-14
- **Executor model:** claude-sonnet-5 (delegated subagent, general-purpose)
- **Model reason:** large but precisely specified implementation task
  mirroring an existing sibling module (`devtools/rag_eval.py`); no
  reasoning-tier need since every mechanism has a template.
- **Harness:** Claude Code (background session, delegated via Agent tool)
- **Stage:** T5
- **Owner of:** `devtools/agent_eval.py` (extended), `tests/test_v1100_runner.py`
- **REQ ids:** REQ-V1100-RUN-01, -02; REQ-V1100-JDG-02, -03, -04;
  REQ-V1100-LAT-01, -02, -03; REQ-V1100-ERR-01; REQ-V1100-SEC-01

## Goal

Build `run()`, `main()`, `RecordingLLM`, `RequestRecorder`,
`_refusing_runner`, `--select`/`--print-dependencies`, the judge call and
`parse_judge_reply` (byte-exact spec-block copy), the latency table and
TTFT probe, and the full error matrix — entirely offline, exactly as
`docs/spec/task-briefs/v1100-T5.md` specifies.

## Constraints

Extend only `devtools/agent_eval.py`; new file `tests/test_v1100_runner.py`.
No live call anywhere in this task. Gates 1-4 only.

## Acceptance

Gates 1-4 exit 0; the `# BEGIN/END SPEC JUDGE PROTOCOL` block is
byte-identical to the extracted spec text; `T-V1100-RUN-01…12`,
`T-V1100-JDG-02…09`, `T-V1100-LAT-01…04`, `T-V1100-ERR-01`,
`T-V1100-SEC-01` all pass offline.

## Stop

If the judge-protocol byte-exact requirement conflicts with making the
module importable (e.g. missing imports), fix by adding imports *outside*
the marked block, never by editing text inside it.
