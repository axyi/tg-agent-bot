# Prompt 264 — v1.11.1 T0 resumed: re-check, gates 1–5, measurements, pin inventory

- **Date:** 2026-09-24
- **Executor model:** claude-sonnet-5
- **Model reason:** commands-only orchestration for the re-checks/gates/
  measurements (T0's own §10.1 exemptions: *commands only*, *artefacts
  only* for this prompt and the report update); the pin-inventory
  construction and `tests/test_v1111_pin.py` are delegated to one
  subagent per EC-03/EC-04.
- **Harness:** Claude Code (background session)
- **Stage:** T0 (resumed from the blocked pass, prompt 256, commit `5780562`)
- **Owner of:** this prompt file, `docs/reports/report-v1.11.1.md`,
  `docs/llm-usage.md` (row 168), `docs/spec/task-briefs/v1111-T0-nodeids.txt`
- **REQ ids:** REQ-V1111-EC-02, REQ-V1111-EC-03, REQ-V1111-EC-04,
  REQ-V1111-PIN-01, REQ-V1111-REV-03

## Goal

Resume `spec-v1.11.1.md`'s T0 after the operator cleared the `bot_state`
override rows and re-issued `go` with a new LM Studio address. Prompt 256
already landed the structural-precondition record and the blocked-run
outcome (commit `5780562`); this prompt re-checks the two conditions that
actually blocked (the override count, LM Studio reachability), resolves a
spec self-conflict this same blocked-run commit created (EC-04's four-path
allowlist vs. REV-03's "re-issue `go`" instruction — see Constraints),
then completes T0: gates 1–5, the floor/node-id/mutation-count
measurements, and the delegated pin inventory.

## Constraints

**Disclosed amendment, operator-confirmed.** The sorted
`git diff --name-only v1.11.0..HEAD` now carries six paths, not EC-04's
four — the two extras (`docs/prompts/256-go-spec-v1.11.1.md`,
`docs/reports/report-v1.11.1.md`) are T0's own sanctioned artefacts from
the blocked pass, not foreign drift. Literally this is ERR-01 row 14 (a
precondition mismatch, no repair cycle, stop route before T0) — but
REV-03's blocked-run clause directs re-issuing `go`, and no `go` re-issue
after any blocked run could ever satisfy row 14's four-path literal again.
This is a spec self-conflict under EC-02's spec-ambiguity clause, surfaced
to the operator via `AskUserQuestion` before any further command ran; the
operator chose to resume T0 treating the two extra paths as expected
consequences of the sanctioned blocked-run commit, not a fresh
precondition mismatch. Recorded here, not silently resolved.

Same `.env`/`data/`/`bot.db`/`sandbox/`/`exec_audit.jsonl` boundary as
prompt 256; the `sed -i` for `LMSTUDIO_BASE_URL` runs without any
follow-up read/grep/print of `.env` (prompt 256's `grep -c` verification
is disclosed as a violation, not repeated). No source file written this
prompt; only the pin-inventory construction (PIN-01) is delegated, one
subagent, brief `v1111-T0.md`, writing the distinct
`v1111-T0-pin-inventory.md` and `tests/test_v1111_pin.py`.

## Acceptance

Override count reads `0`; LM Studio reachable with the pinned model in
its catalogue; gates 1–5 exit 0; collected count `>= 2384` (recorded as
`floor`); `len(MUTATIONS) == 152`; the delegated subagent lands
`docs/spec/task-briefs/v1111-T0-pin-inventory.md` and
`tests/test_v1111_pin.py`, both `T-V1111-PIN-01`/`-02` green;
`git diff --exit-code` clean after the commit.

## Stop

An unreachable LM Studio or a non-zero override count at this stage is
again a blocked run (ERR-01 row 6), same disposition as prompt 256. A
count below 2384 on collection is a base-tree/precondition mismatch — the
stop route (REV-03), no repair cycle.
