# Prompt 262 — v1.11.1 T4 second commit: DOC-01/DOC-02, README/plan/AGENTS drift

- **Date:** 2026-09-24
- **Executor model:** claude-sonnet-5
- **Model reason:** a bounded, multi-file documentation task (three README
  edits, a `docs/plan.md` banner, one `AGENTS.md` paragraph) plus its own
  test file, delegated to a subagent per `AGENTS.md`'s context-discipline
  rule (more than one file to touch, a task that writes a file a gate
  imports/runs).
- **Harness:** Claude Code (subagent)
- **Stage:** T4 (second commit)
- **Owner of:** `README.md`, `docs/plan.md`, `AGENTS.md`,
  `tests/test_v1111_doc.py` (new), this prompt file,
  `docs/llm-usage.md`, `docs/reports/report-v1.11.1.md`; also stages
  T4's first commit's own untracked artefact,
  `docs/prompts/261-v1111-t4-doc03.md`, unmodified, per EC-03
- **REQ ids:** REQ-V1111-DOC-01, REQ-V1111-DOC-02

## Goal

Implement REQ-V1111-DOC-01 and REQ-V1111-DOC-02 per
`docs/spec/task-briefs/v1111-T4.md` and `docs/spec/spec-v1.11.1.md:479-531`:
three README edits (a `/model` catalogue-cap row and a decimal/binary
sizing sentence in `## Limits`; a `/documents` empty row and a Telegram
400 row in `## Error behaviour`, `/sessions`' own empty row already
landed by T2 and not duplicated; a rewritten tag-creation sentence in
`## Versioning`), `docs/plan.md`'s seven-line superseded banner, and
`AGENTS.md`'s `## Secrets` RFC1918-is-not-a-secret paragraph, all in
`tests/test_v1111_doc.py::test_t_v1111_doc_01_readme_limits_error_rows_versioning`
/`::test_t_v1111_doc_02_plan_banner_and_agents_ruling`. This commit also
records `T-V1111-DOC-03`'s already-green result (T4's first commit,
`9d8dc0e`, EC-02's stated carve-out timing) and bundles that first
commit's own bookkeeping (its prompt file, already on disk untracked,
and its `docs/llm-usage.md` row) per EC-03.

## Constraints

Never open/print/grep `.env`, `data/`, `bot.db`, `sandbox/`,
`exec_audit.jsonl`. No LAN address or secret literal anywhere. Zero new
dependencies. This commit's diff touches exactly nine paths: `README.md`,
`docs/plan.md`, `AGENTS.md`, `tests/test_v1111_doc.py`,
`docs/prompts/261-v1111-t4-doc03.md` (staged unmodified),
`docs/prompts/262-v1111-t4-doc.md` (this file), `docs/llm-usage.md`,
`docs/spec/task-briefs/v1111-T4.md`, `docs/reports/report-v1.11.1.md`.
`--no-verify` never.

## Acceptance

`tests/test_v1111_doc.py::test_t_v1111_doc_01_readme_limits_error_rows_versioning`,
`::test_t_v1111_doc_02_plan_banner_and_agents_ruling`,
`::test_t_v1111_doc_03_v1110_report_and_post_corrected` all pass; full
suite green (`uv run --locked pytest`); gates 1-4 (`uv sync --locked`,
`uv run --locked ruff check .`, `uv run --locked pytest`,
`uv run --locked python bot.py --selftest`) and
`uv run --locked python devtools/checks.py lint-docs` all exit 0;
`checks._lint_report_delegation(Path("docs/reports/report-v1.11.1.md"))`
returns `[]`.

## Stop

None triggered. One disclosed deviation, not a stop: the authoring
order was edit-then-test, not genuine authorship-order test-first for
DOC-01/DOC-02 (neither is among EC-02's four named carve-out ids) —
the README/`docs/plan.md`/`AGENTS.md` edits landed first, then
`tests/test_v1111_doc.py` was written against the changed tree, then
`git stash push --keep-index -- README.md docs/plan.md AGENTS.md`
temporarily reverted the three files and the module re-run confirmed
red for the right reason (DOC-01: `ValueError: substring not found` on
the `/model` catalogue-cap lookup; DOC-02: the banner-lines comparison
failed at index 0) while DOC-03 stayed green throughout; the stash was
then popped and all three tests re-confirmed green. Recorded in the
report rather than overstated, the same disclosure category T1 used.
Four disclosed line-drift amendments (README section headings and the
`Telegram 429 on send` row, all <= 3 lines against spec citations) and
one stale-prompt-number correction (spec `:499`/`:530`/`:535` cite
prompts 261/260 for this task's two commits; T3's own renumbering
makes it 262/261) are recorded in the report, not the spec. Pre-commit
`advisor` self-check caught two gaps before this commit: the test-first
section originally understated the edit-then-test order; this task's
own `5587fd1` gitleaks-tree row was first mis-appended to T1's table by
false analogy with `ebc2822`'s placement there, then moved to a new
table inside T3's own section, matching the brief's "T3's own table" /
"T4's own table" pairing.
