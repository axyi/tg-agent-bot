# Prompt 225 — v1103 T5: clean-context review (REV-01)

- **Date:** 2026-09-18
- **Executor model:** claude-sonnet-5
- **Model reason:** code-reviewer subagent, clean isolated context,
  never the writing context (REV-01).
- **Harness:** Claude Code (`code-reviewer` subagent, `model: sonnet`)
- **Stage:** T5
- **Owner of:** review only — no file writes
- **REQ ids:** REQ-V1103-REV-01, REQ-V1103-REV-03

## Goal

Review every change T1-T4 made (commit range `636a281..HEAD`) against
`docs/spec/spec-v1.10.3.md` and the standard/test-independence
checklists, plus REV-01's seven spec-specific items, before T6's live
gate run.

## Constraints

Read-only — no file edits. Do not run gates 5, 6, 7, or 8 (no live
LLM/network calls, no mutation run). Gates 1-4 may be re-run to verify
claims if useful, but are not required (already proven green in each
task's commit).

## Acceptance

Findings closed or waived with reasons; verdict recorded (approve or
request changes) in `docs/reports/report-v1.10.3.md`'s `## T5` section.

## Stop

N/A — review always completes and reports; it does not itself block on
a repair budget (that's the orchestrator's decision after reading the
findings).
