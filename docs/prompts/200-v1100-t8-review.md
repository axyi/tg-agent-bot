# Prompt 200 — v1.10.0 T8: clean-context review

- **Date:** 2026-09-14
- **Executor model:** claude-sonnet-5 (`code-reviewer` subagent, clean
  context)
- **Model reason:** the repo's own code-reviewer agent, run separately from
  the writing context per REV-01's "never self-review" rule.
- **Harness:** Claude Code (background session, delegated via Agent tool)
- **Stage:** T8
- **Owner of:** none (read-only review); fixes it finds land in a
  follow-up commit
- **REQ ids:** REQ-V1100-REV-01

## Goal

Review commits `c84d739..HEAD` (T0 through T7 of spec-v1.10.0) against the
standard checklist plus REV-01's nine spec-specific items, in a clean
context with no memory of having written any of this code.

## Constraints

Read-only. No fix is applied inside the review itself — findings are
reported, then closed or waived in a separate follow-up task.

## Acceptance

Every finding classified by severity; the nine REV-01-specific checks
explicitly addressed one by one, not just the generic checklist.

## Stop

N/A — a review always completes and reports; it does not "fail".
