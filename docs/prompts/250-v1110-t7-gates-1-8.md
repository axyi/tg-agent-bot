# Prompt 250 — v1.11.0 T7 Phase E/F: gates 1-8, report-only commit

- **Date:** 2026-09-23
- **Executor model:** claude-sonnet-5
- **Model reason:** commands-only orchestration (T7's own §14.1
  exemption: *commands only* for the gate run and the report-only
  commit) — no delegation.
- **Harness:** Claude Code (background session, orchestrator)
- **Stage:** T7 (Phase E/F)
- **Owner of:** `docs/reports/report-v1.11.0.md` (`## T7`, Phase E/F),
  `docs/llm-usage.md` (row 161)
- **REQ ids:** REQ-V1110-REV-02

## Goal

Run all eight gate commands of `AGENTS.md:150-158` verbatim, in order,
against `tested_tree = 2c3c5aab5370d6ce951009766b879a8e7ef4b116`
(T7's fix commit) with an empty `git status --porcelain` recorded first:
gates 1-5, gate 6 (`mutation_check.py`'s direct `mutation-all` run,
alone, nothing else touching the tree), `doctor`, `lint-docs`, then gate
8 exactly once as the last live action, with the MOD-05 override count
re-read at `0` immediately before it. Record gate 6's wall `W` and apply
MUT-01's timeout-recalibration rule if `W > 1300`. Then make the
report-only commit.

## Constraints

Gates 6, 7 and 8 never run in parallel with one another or with any
other gate (EC-07) — each was launched alone, its own background job,
with no other tool call touching the repository until it completed
(one self-caught exception: a manual bite-check of the T7-fix commit
briefly touched `bot.py` while an earlier gate-3 background run was
still in flight, producing one spurious "1 failed" result; re-run
cleanly with nothing else active, confirmed green — recorded here for
the honest record, not hidden). This report-only commit touches only
`docs/reports/*` and `docs/llm-usage.md` — no source, test or config
file.

## Acceptance

All eight gates green, in order, none rerun: gate 3 2376/2376, gate 6
152/152 killed (W=961.6s, under the 1300s recalibration threshold — no
`timeout_seconds` hunk needed), gate 7 `hybrid recall@5=1.000` with the
two advisory conversation-aware smoke checks disclosed non-blocking,
gate 8 injection 5/5, hallucination 4/4, memory 3/3, judge mean 0.913 —
all floors met. `git status --porcelain` empty and `HEAD` unchanged
after gate 8. `doctor` and `lint-docs` both green.

## Stop

Not applicable — every gate passed; no stop route, no repair cycle spent
this phase.
