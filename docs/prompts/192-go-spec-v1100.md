# Prompt 192 — go spec-v1.10.0 (T0 preflight)

- **Date:** 2026-09-13
- **Executor model:** claude-sonnet-5
- **Model reason:** default interactive session model; the task is standard
  spec-driven implementation and orchestration work, no reasoning-tier need.
- **Harness:** Claude Code (background session)
- **Stage:** T0
- **Owner of:** `docs/prompts/192-go-spec-v1100.md`, `docs/reports/report-v1.10.0.md`
  (skeleton), this task's preflight artefacts
- **REQ ids:** REQ-V1100-EC-01, REQ-V1100-EC-05, REQ-V1100-EVAL-01 (timeout
  measurement), REQ-V1100-REV-04 (Stage 0)

## Goal

Execute `docs/spec/spec-v1.10.0.md`'s T0: run the five Stage 0 preflight
checks in order (judge route resolved and distinct from the chat model;
LM Studio lists the configured chat model; one timed plain chat turn on the
production route; one strict-schema judge call using §8's two labelled
spec-block fences verbatim; `uv lock` no-op), then the seven existing gates
on the unchanged tree (gate 8 not applicable yet), record `<base>`, install
hooks and confirm `doctor` green, re-measure the collected test count,
compute EVAL-01's gate-8 `timeout_seconds` from the measured chat-turn wall,
and write the `report-v1.10.0.md` skeleton with `## Operator inputs`.

## Constraints

Commands only — no source or test file is written in this task (EC-04's
*commands only* exemption). `.env` is never opened by the executor; only
env-variable **names**, model ids and `describe()` pairs are recorded, never
`OPENROUTER_API_KEY` or `TELEGRAM_BOT_TOKEN` values. Since `LLM_JUDGE_MODEL`
(CFG-01) and the `purpose="judge"` branch (`llm/__init__.py`) do not exist
on the unchanged tree, Stage 0 checks 1 and 4 construct the routed judge
client directly through `parse_routed_model` + `llm._client_for` (the same
primitives CFG-01 will wire into `build_llm_client` at T3) rather than
literally through a not-yet-existing `purpose="judge"` call — recorded here
as a resolved sequencing note, not a spec deviation in substance. No
`--no-verify`. Gates 6, 7 and 8 never run concurrently (one GPU box).

## Acceptance

All five Stage 0 checks pass (non-empty, distinct judge route; `/models`
lists `qwen/qwen3.8-27b`; chat turn timed and under `cfg.llm_timeout_s`; the
judge call parses cleanly with `parse_judge_reply`; `uv lock` produces no
diff); gates 1–7 exit 0 on the unchanged tree; `docs/reports/report-v1.10.0.md`
skeleton committed with `## Operator inputs` (resolved judge model, source
`.env`, both `describe()` pairs) and a complete ledger-row block.

## Stop

Any Stage 0 check fails → Stage 0 stop route (REV-04): no source or test
file written, no bump, no tag, report finalised as a blocked run.
