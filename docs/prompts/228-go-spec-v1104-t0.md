# Prompt 228 — v1.10.4 T0: preflight, Stage 0's seven checks, gates 1–5+7, EC-02 inventory

- **Date:** 2026-09-18
- **Executor model:** claude-sonnet-5
- **Model reason:** commands-only orchestration task (T0's own §10.1
  exemption: *commands only*); no delegation.
- **Harness:** Claude Code (background session)
- **Stage:** T0
- **Owner of:** `docs/prompts/228-go-spec-v1104-t0.md`,
  `docs/reports/report-v1.10.4.md` (skeleton), `data/run-v1104.db`
  (created fresh by the storage preflight, proved empty), `docs/llm-usage.md`
  (row 139)
- **REQ ids:** REQ-V1104-EC-01, REQ-V1104-EC-02, REQ-V1104-EC-04,
  REQ-V1103-INS-01 (by reference), REQ-V1104-GATE-01, REQ-V1104-REV-03
  (Stage 0), REQ-V1104-RPT-01

## Goal

Run `spec-v1.10.4.md`'s Stage 0 preflight — the exit-status preconditions
(spec committed and unmodified, `Status: ready for \`go\`` present once,
prompt 227 and `docs/llm-usage.md` row 138 in `HEAD`, `pyproject.toml`
still `1.9.5`, no `v1.10.0`…`v1.10.4` tag, `stash@{0}` identity-pinned to
`e3c6e3ff…` with the post-T1-shaped two-path `git apply --check` proved on
the unchanged `f3ce1a5`-based tree), then the seven checks in order
(offline checks 1/2/7 against the already-synced locked environment, check
6 with its judge≠chat assertion and single INS-01 fallback), then gates
1–5 and 7 on the unchanged tree (gates 6 and 8 not run), re-measure the
test-collection floor and `len(MUTATIONS)`, run EC-02's five-part
fail-closed inventory (the frozen-list grep, the reference grep, the
absence/end-of-list grep, the broad pass, every hit classified) and
reconcile it against EC-02's 16-row amendment table and the "verified
unaffected" list, and write the report skeleton with `## Operator
inputs`, the attempt log, `## T0 — preflight` with T0's own delegation
bullet, and a ledger-row block.

## Constraints

Commands only — no source, test or config file written this task. The
executor never opens `.env` directly, never prints a secret value (only
`bool(openrouter_api_key)` and `describe()` pairs), and never touches the
operator's own database — `data/run-v1104.db` is the run's own fresh file,
already pointed to by `.env`'s `DB_PATH` (an operator-side precondition
satisfied before this prompt started), proved empty (absent, so
`db_empty=True` by construction) before any network call. The stash is
read only (`git rev-parse`, `git stash show -p | git apply --check`),
never `pop`/`apply`/`drop`. `--no-verify` never used.

## Acceptance

Every exit-status precondition holds. All seven Stage 0 checks pass (no
blocker); `db_empty=True` recorded; `uv lock --offline` produces no diff
and `git diff --stat f3ce1a5 -- pyproject.toml uv.lock` is empty; the
re-measured collection count is 2285 (the floor); `len(MUTATIONS) ==
139`; gates 1–4 green (pytest exit 0, matching the 2285 floor); gate 5
all-green (`config`/`db`/`docker`/`telegram`/`embeddings`/`openrouter`
OK, `lmstudio` SKIP); gate 7 green (`hybrid: recall@5=1.000`, first
invocation clean, no transient re-invocation needed; the advisory
conversation-aware smoke fails are non-blocking, per NG-07-equivalent
prior releases). EC-02's five-part T0 inventory hit list is reconciled
against the spec's 16-row amendment table and verified-unaffected list,
every hit classified (ERR-01 row 14).

## Stop

Any exit-status precondition failing, any Stage 0 check failing, or any
gate-5 line other than the disclosed `lmstudio SKIP` going red (`db` in
particular), is a Stage 0 blocker: the run stops and finalises per REV-03
Stage 0 — no source or test file written, no bump, no tag, the evidence
commit the run's only commit.
