# Prompt 50 — spec-v1.5 T6: install rtk 0.47.0

- **Date:** 2026-09-04
- **Executor model:** claude-sonnet-5 (Claude Code)
- **Model reason:** a pinned-version binary install; no design decision
- **Harness:** Claude Code
- **Stage:** T6
- **Owner of:** `config/quality_gates.yaml` (rtk pin only),
  `docs/reports/report-v1.5.md` (T6 provenance row),
  `docs/prompts/50-v15-t6-rtk.md` (new)
- **REQ ids:** REQ-V15-DEP-02, REQ-V15-DEP-03, REQ-V15-DEP-05, REQ-V15-RTK-03

## Goal

Install rtk 0.47.0 as a GitHub release asset with a verified SHA-256, move
only the `tools.rtk.version` pin, warn-only per REQ-V15-RTK-03 (already
wired into `doctor.warn_only_tools` at T1).

## Constraints

- Release asset + checksum only (REQ-V15-DEP-03).
- This commit touches only the rtk pin — no gate logic, no other tool
  (REQ-V15-DEP-05).
- rtk telemetry consent must remain `never asked` / disabled after the
  upgrade (`AGENTS.md` § RTK, `CLAUDE.md`'s RTK block) — never enable it.

## Acceptance

- `rtk --version` prints `rtk 0.47.0`.
- SHA-256 of the downloaded asset matches the published checksums file,
  recorded in the report.
- `rtk telemetry status` still shows `consent: never asked` after the
  upgrade.
- `uv run --locked ruff check .` and `uv run --locked pytest
  tests/test_v15_standards.py` both exit 0.
- All five external tools (gitleaks, semgrep, trivy, skylos, rtk) are now
  at their REQ-V15-DEP-02 targets — recorded in the report.

## Stop

If the upgrade silently changes telemetry consent, stop and revert —
telemetry must never be enabled under any circumstance.
