# Prompt 260 — v1.11.1 T3: dispatch-level error-row tests, the secrets-registry restore fixture

- **Date:** 2026-09-24
- **Executor model:** claude-sonnet-5
- **Model reason:** a bounded, test-only task (six dispatch-level tests
  plus one small conftest fixture) delegated to a subagent per
  `AGENTS.md`'s context-discipline rule (a task that writes source files
  a gate compiles/imports/runs).
- **Harness:** Claude Code (subagent)
- **Stage:** T3
- **Owner of:** `tests/test_v1111_tst.py` (new), `tests/conftest.py`,
  this prompt file, `docs/reports/report-v1.11.1.md`,
  `docs/llm-usage.md`
- **REQ ids:** REQ-V1111-TST-01, REQ-V1111-TST-02

## Goal

Implement REQ-V1111-TST-01 and REQ-V1111-TST-02 per
`docs/spec/task-briefs/v1111-T3.md` and `docs/spec/spec-v1.11.1.md`
sec.3.3: six new tests in a new module, `tests/test_v1111_tst.py`, one
per v1.11.0 ERR-01 row 11-16 (`spec-v1.11.0.md:1138-1143`), each driving
`bot.process_update` through `tests/test_v1110_err.py`'s own
`process`/`text_update` helpers with a hand-written update dict,
asserting the reply byte-equal to the row's string and that string's
presence in README's `## Error behaviour` section; the frozen
`tests/test_v1110_err.py::test_t_v1110_err_01_error_matrix_strings`
stays byte-unchanged. `tests/conftest.py` gains a context manager
`secrets_registry_snapshot()` (save `set(config._secrets)`, yield,
clear+update the same set object, never rebind it) and an autouse
fixture `restore_secrets_registry` wrapping every test in it, ordered
after `no_real_bind`; `T-V1111-TST-07` proves the fixture itself via a
nested `subprocess.run([... "-m", "pytest", ...])` over a two-test
module written to `tmp_path` plus a `tmp_path/conftest.py` importing the
project's fixture by name; `T-V1111-TST-08` is verified by command (not
a function), both results recorded in the report.

## Constraints

Never open/print/grep `.env`, `data/`, `bot.db`, `sandbox/`,
`exec_audit.jsonl`. No LAN address or secret literal anywhere -- only
the repo's existing fake `VALUE-abcdefgh12`-style sentinel, never a
credential-shaped real value (this task touches the secrets registry
directly). Zero new dependencies. No test file other than
`tests/conftest.py` changes for the TST-02 item -- the leaking test is
never hunted, the fixture makes ordering irrelevant. `--no-verify`
never.

## Acceptance

`tests/test_v1111_tst.py::test_t_v1111_tst_01_err_row_11_session_usage`,
`::test_t_v1111_tst_02_err_row_12_session_unknown`,
`::test_t_v1111_tst_03_err_row_13_delete_not_found`,
`::test_t_v1111_tst_04_err_row_14_delete_usage`,
`::test_t_v1111_tst_05_err_row_15_stale_callback`,
`::test_t_v1111_tst_06_err_row_16_model_unknown_and_usage`,
`::test_t_v1111_tst_07_secrets_registry_restored` all pass; the full
suite green (`uv run --locked pytest`); TST-08's two commands both exit
0 (`uv run --locked pytest
"tests/test_v1103_red_team.py::test_t_v1103_rt_06_leak_shape_fixture_still_fails_on_clause_c_only"
-o addopts=""`, and `uv run --locked pytest -p xdist -n 4 -o addopts=""
-q` twice); gates 1-4 (`uv sync --locked`, `uv run --locked ruff check
.`, `uv run --locked pytest`, `uv run --locked python bot.py
--selftest`) and `uv run --locked python devtools/checks.py lint-docs`
all exit 0.

## Stop

None triggered. The spec's own `[[VERIFY]]` grep
(`grep -n "_secrets" config.py tests/conftest.py tests/test_v11_patch.py`)
confirmed the registry's name, location and mutate-in-place contract
exactly as cited, so the `cited -> actual` decision rule at
spec `:444-448` was never invoked; the nested-run `[[VERIFY]]` at
spec `:456-464` confirmed the plain `from tests.conftest import
restore_secrets_registry` import path works on the first try, so
neither the `pytest_plugins` fallback nor the permanent
`xdist_group`-marked pair was needed. One disclosed deviation, not a
stop: TST-02/-07's authoring order was not genuinely test-first (the
conftest.py fixture was written before the test file, after a manual
`/tmp` prototype); this is recorded in the report rather than
overstated, distinct from TST-01's six row tests, which needed no
source change to red-check against at all. `T-V1111-TST-07` was,
however, separately shown genuinely red/green against the finished
tree for the exact failure mode spec `:449` names (a non-`autouse`
fixture fails the nested run), by temporarily removing `autouse=True`
and restoring it -- see the report.

Prompt-renumbering disclosure, flagged forward per the brief: the
static per-task map broke once T0's resume/follow-up took 264/265 out
of order and T1's follow-up took 258; this prompt confirms **260** for
T3 and flags that T4 now takes **261+262** (its two commits, was
260+261), T5 takes **263** (was 262), and T6 must skip 264/265 and take
**266** (was 263).

Pre-commit `advisor` self-check caught six real gaps before this
commit (the missing renumbering disclosure, row 12's then-vacuous
rollback assertion, a key-shaped test placeholder, TST-07 never having
been run red for its own target case, two conflated `ImportError`
messages, and one stale line-range citation) -- all fixed; see the
report's "Pre-commit self-check" subsection.
