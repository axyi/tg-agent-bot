# Prompt 257 — v1.11.1 T1: OUT-01/OUT-02, the 400-only fallback and three docstrings

- **Date:** 2026-09-24
- **Executor model:** claude-sonnet-5
- **Model reason:** a bounded, spec-cited implementation task (brief
  `v1111-T1.md`) delegated per §10.1's **yes** cell — a source-writing task
  under the repo's context-discipline rule, not commands/artefacts only.
- **Harness:** Claude Code (subagent, general-purpose)
- **Stage:** T1
- **Owner of:** `bot.py`, `tables.py`, `tests/test_v1111_out.py` (created),
  `docs/reports/report-v1.11.1.md`, `docs/llm-usage.md` (row 170),
  `docs/prompts/257-v1111-t1-out.md`
- **REQ ids:** REQ-V1111-OUT-01, REQ-V1111-OUT-02

## Goal

Narrow the `<pre>` table path's one-time plain-text fallback so it fires
only on an HTTP 400 from the Bot API, not on any non-fatal failure as
today. `TelegramError` gains a `status: int | None = None` field;
`TelegramClient.call` sets it from `response.status_code` on every
response-driven raise and leaves it `None` on the transport raise;
`_call_with_retry` stays byte-unchanged. `send_pre`/`edit_pre` switch
their fallback predicate from `exc.fatal` to `exc.status == 400` (401/404
never equal 400, so the old fatal check is subsumed). Three stale
docstrings — `send_pre`, `edit_pre`, `tables.fit_lines`, `IngestJob` — are
rewritten to say what the code now does, per spec `:274-311`'s verbatim
text.

## Constraints

Test-first (EC-02): `tests/test_v1111_out.py`'s five tests written and
observed red (via a temporary `git stash` of the source edits) before the
source changes landed. `_call_with_retry` (`bot.py:274-288` on this tree)
byte-unchanged from `2431034`. `tests/test_v1110_out.py:359-422`'s four
`T-V1110-OUT-05` cases stay green unamended. No new dependency; README
untouched (that edit is T2/T4's). `.env`, `data/`, `bot.db`, `sandbox/`,
`exec_audit.jsonl` never opened/printed/grepped; no LAN address or secret
literal written anywhere. `--no-verify` never used.

## Acceptance

`T-V1111-OUT-01…05` green (`tests/test_v1111_out.py`, 5 passed);
`T-V1110-OUT-05`'s four cases green unamended; gates 1–4
(`uv sync --locked`, `ruff check .`, `pytest`, `bot.py --selftest`) and
`devtools/checks.py lint-docs` all exit 0; one commit, conventional type,
body names this prompt file, trailer copied from `git log -3`.

## Stop

A test failing to go red for the right reason against the pre-change
tree, a gate staying red past the fix, or a spec ambiguity in OUT-01/-02's
wording would each be a stop-and-report condition; none occurred this
task.
