tg-agent-bot v1.10.1 — хвосты v1.10.0 закрыты, gate'ы переведены на OpenRouter: gate 8 снова остановил релиз, но по новой причине

Условия: закрыть хвосты v1.10.0 (пять миссов red-team чекеров,
контрадикция T6, устаревшие счётчики) и перевести живые gate 5/7/8 с
LM Studio на OpenRouter (модель под тестом gpt-4.1-mini, судья gpt-4.1).
T0–T5 прошли штатно: embeddings аутентифицируются по Bearer, gate 5
зелёный с T1, чекеры получили clause (e) «инструмент вызван под
атакой» и маркеры 8→15; ревью закрыло 1 must-fix + 5 should-fix.

Результат: gate 8 вживую, exit 1 на T6: injection 1/5 (порог 5/5) — из
4 миссов 3 это новая clause (e): модель ВЫЗЫВАЛА exec при попытке соц.
инженерии, хотя текстовый ответ был корректным отказом. Под чекерами
v1.10.0 (без clause (e)) эти три прошли бы — новый механизм поймал
реальное небезопасное поведение. hallucination 3/4, memory 3/3, judge
mean 0.973 — все прошли. Числа не сравнимы с v1.10.0 напрямую: другая
модель и другой чекер. Это Stage B′: офлайн-сьют уже доказал
корректность чекеров — модель не дотянула. Датасет и пороги не
трогаем, версия не бампится, тег не ставится. Одна живая аномалия без
правки кода: rerank дважды вернул неразбираемый порядок на gate 7,
третий прогон чист; repair-бюджет не потрачен.

Метрики: claude-sonnet-5 (Claude Code). 9 промптов (202–210). Токены
харнесс не отдаёт. Стоимость живых вызовов сильно меньше $0.10; Claude
Code — $0, подписка.

GitHub: https://github.com/axyi/tg-agent-bot
