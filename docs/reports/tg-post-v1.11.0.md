tg-agent-bot v1.11.0 — таблицы в Telegram, сессии, меню /model, фоновый воркер загрузки документов

Условия: HTML `<pre>`-таблицы для /stats, /documents, /sessions, /model,
/help (ответы модели остаются обычным текстом); /delete по #id; список и
переключение сессий без изменения схемы; inline-кнопки и callback_query
за двухшаговым меню /model (провайдер→модель, хеш каталога против
устаревания); лимит загрузки документов поднят до потолка Bot API (20 МБ),
загрузка перенесена в фоновый поток с кооперативной отменой (/cancel);
регистрация команд через setMyCommands, /help, /start.

Результат: ревью в чистом контексте — 1 реальная гонка найдена и
исправлена (/cancel в окне перед коммитом транзакции терялась), 2 пункта
waived с обоснованием. Gate 6 дважды (T7, T8) — 152/152 мутаций убито,
wall ~961–965с (порог пересчёта 1300с не задет). Gate 7:
recall@5=1.000. Единственный gate 8 (T7): injection 5/5, hallucination
4/4, memory 3/3, judge mean 0.913 — все пороги пройдены, у T8
переиспользован (diff version-only). Версия 1.10.4→1.11.0, uv.lock
online, README/AGENTS.md — реальные цифры (2384 теста, 152 мутации).
`replay` чист на всех 24 коммитах прогона. Локальный тег v1.11.0; пуш —
на операторе.

Метрики: claude-sonnet-5 (Claude Code, оркестратор + делегированные
субагенты), промпты 236–252. Живой инференс — gate 5/7/8 один раз,
заметно меньше $1 по паблик-прайсу; Claude Code — $0, подписка.

GitHub: https://github.com/axyi/tg-agent-bot
