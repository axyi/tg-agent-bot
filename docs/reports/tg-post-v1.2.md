# Telegram-пост v1.2

---

**Патч закрыл находки двух независимых аудитов v1.1: security-проба +
83-мутационный compliance-ревью**

Условия: только названное аудитами, без рефакторинга v1.1. Проба нашла 8
находок (id/имя tool-call модели шли в БД напрямую, квота sandbox не видела
нечитаемый каталог, SSRF через `127.1`, symlink вместо `/etc/resolv.conf`,
второй инстанс убивал первый); ревью намутировало код — 23 мутации выжили
в тестах.

claude-sonnet-5 (Claude Code), один промпт `go docs/spec/spec-v1.2.md`:
свои id tool-call вместо доверия модели; tri-state скан квоты; 3-слойный
SSRF-allowlist (форма + резолв при старте + резолв на каждый
запрос/редирект); жёсткое создание resolv-файла; reap контейнеров по
владельцу-процессу; редактирование в audit-хуке; плюс постоянный gate —
`devtools/mutation_check.py` на stdlib.

Метрики:
• тестов 326 (было 251), 6 гейтов вместо 5
• mutation-gate 31/31 убито — после починки мета-теста, топившего гейт по
  своей же bookkeeping, а не по факту находки
• Appendix B: 12 сценариев (8 новых + 4 регрессии v1.1) на реальном Docker,
  все PASS, скриптом, без сообщений в Telegram
• ревью: 3 блокера и 2 непокрытых мутацией строки — закрыты; 1 риск
  задокументирован, не закрыт
• токены/стоимость: не посчитаны в этом прогоне

Урок: находка без mutation-теста — ненайденная находка.

https://github.com/axyi/tg-agent-bot
