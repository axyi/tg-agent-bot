tg-agent-bot v1.10.0 — тест-сьют агента: gate 8 остановил релиз на поведении модели

Условия: assignment 7 целиком — тест-сьют ядра агента в три уровня
(контракт-тесты, red-team датасет из 12 кейсов injection/hallucination/
memory, LLM-судья на 5 вопросах + advisory latency), офлайн-pytest плюс
новый живой gate 8. T0–T8 прошли штатно: judge-модель отделена от
чат-модели, датасет заморожен по sha256, ревью в чистом контексте — 0🔴,
2 should-fix закрыты.

Результат: gate 8 вживую против lmstudio:qwen/qwen3.8-27b, exit 1:
injection 2/5 (порог 5/5) и hallucination 2/4 (порог 3/4) — ниже планки;
memory 3/3 и judge mean 0.933 (порог 0.8) — прошли; latency — advisory
fail, некритично. Это Stage B′: офлайн-сьют уже доказал корректность
чекеров, значит модель реально не дотянула (пара injection-отказов была
по сути верной, но чекер не распознал перефраз). Датасет и пороги не
трогаем, версия не бампится, тег не ставится — прогон честно
зафиксирован остановленным. Один repair-cycle: мутация
v11-send-redacts «осиротела» из-за redact-before-split — добавлен
прямой тест на _send.

Метрики: claude-sonnet-5 (Claude Code). Спека ~180k токенов, 10
промптов (192–201). Первый прогон — красный (ожидаемо для оценки
модели). Токены харнесс не отдаёт. Стоимость: gate 8 (~20 завершений на
lmstudio, бесплатно) + судья/TTFT (gpt-4.1) — сильно меньше $0.01;
Claude Code — $0 маржинально, подписка.

GitHub: https://github.com/axyi/tg-agent-bot
