tg-agent-bot v1.9.4 — патч: редакция в логах, честный gate-7-смок, именованный завис mutation

Условия: оператор — «Поехали 1.9.4 по всем трём предложенным пунктам и
прочим хвостам» (три пункта v1.9.3-ревью + PTH/RUF043-хвост).

Результат: T1 — секреты маскируются на уровне логирования
(`RedactingFormatter` на всех точках входа, включая traceback и цепочки
исключений); старые `redact()`-вызовы сохранены. T2 — смок gate 7
получил третий, context-proof ход (2-й ход было нельзя провалить
осмысленно); чат-маршрут смока измерим через опциональный
`LLM_EVAL_CHAT_MODEL` — ни одна из тестовых OpenRouter-моделей не ищет по
документам без принуждения, продакшн-маршрут остаётся дефолтом. T3 —
зависшая мутация завершается по таймауту 180s и репортится `errored` с
её id вместо безымянного падения гейта. T4 — последние строки
ruff-таблицы (`PTH*`, `RUF043`) приняты, `PLW0603` → «никогда». T5 —
версия 1.9.3→1.9.4, счётные строки на реальных цифрах (1634 теста, 119
мутаций), все 7 гейтов зелёные, тег v1.9.4. Ревью (opus): 0🔴/1🟠/7🟡,
закрыто одним коммитом. На v1.10: измерить подкачку модели в LM Studio
напрямую; prompt смока не всегда принуждает модель искать по документам.

Метрики: исполнитель claude-sonnet-5 (Claude Code; ревью —
claude-opus-5), 7 промптов (181–187). Токены харнесс не отдаёт.
Стоимость: ~$0 маржинально — подписка; живой инференс по референсным
ценам (< $0.10 суммарно), реальных денег сверх лимитов не потрачено.

GitHub: https://github.com/axyi/tg-agent-bot
