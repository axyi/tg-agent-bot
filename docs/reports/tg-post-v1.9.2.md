tg-agent-bot v1.9.2 — патч: статанализ, mutation-реордер + xdist, версия

Условия: оператор заказал полный глубокий анализ всего кода (skylos,
trivy, semgrep и др., не только диф) с исправлением всех находок;
отдельно — gate 6 (мутации) полз 61m39s, gate 3 (pytest) шёл серийно.

Результат: T1 — skylos 27→0 (7 удалено, 20 подавлено с обоснованием, 0
без пометки), 75 файлов переформатированы whole-tree, semgrep больше не
сканирует свой же rule-pack. T2 — mutation-раннер теперь упорядочивает
тестовые файлы по релевантности к мутации: gate 6 61m39s→11m20s на
110 записях (~5.4x), pytest под xdist ~70s→~23s (~3x). 0 тестов удалено:
version-pin тесты никогда не удаляются, только перепривязываются к
замороженному git-тегу (REQ-V190-EC-03) — так и в этом патче. Оба
ревью (T1, T2) закрыты одним коммитом каждое, без переписывания
проверенных коммитов. T3 — версия 1.9.1→1.9.2 (red-before/green-after),
все счётные строки на реальных цифрах (1601 тест, 110 мутаций), все 7
гейтов зелёные на финальном дереве, тег v1.9.2.

Метрики: исполнитель claude-sonnet-5 (Claude Code; оба ревью —
claude-opus-5), 8 промптов (164–171). Токены харнесс не отдаёт — не
измерено. Стоимость: $0 — подписка; живой инференс на гейтах 5/7 по
референсным ценам, реальных денег не потрачено.

GitHub: https://github.com/axyi/tg-agent-bot
