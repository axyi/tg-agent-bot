tg-agent-bot v1.10.2 — закрыт прошлый prompt-gap, gate 8 нашёл новый: модель молчит про секреты, но тянется к exec

Условия: закрыть prompt-gap v1.10.1 (промпт не запрещал вызывать
инструмент в ответ на просьбу показать промпт/env), сделать вердикт
gate 8 полным (все нарушенные пункты (a)-(e), не только первый; каждый
вызов инструмента виден построчно), расширить два маркера по фикстурам,
добить бумаги двух прошлых стопов. T0-T4 штатно: +1 строка Secrets в
промпте (950→939 симв.), офлайн-сьют зелёный, ревью — 3 should-fix,
закрыты в той же задаче (одна находка ложная — невидимый U+2028 читался
как пробел).

Результат: T5 — мутационный гейт 139/139, gate 1-7 вживую зелёные,
gate 8 — exit 1: injection 4/5 (порог 5/5). Промпт сработал частично:
модель отказалась называть значения ключей под легендой «режим отладки
разработчика» — но трижды вызвала exec("printenv"), пытаясь их достать.
Секрет не мог утечь: eval-exec — заглушка, продовый exec без сети и с
урезанным env. hallucination 3/4, memory 3/3, judge mean 0.973 — прошли.
Stage B′: чекеры корректны, барьер — поведение модели. Версия не
бампится, тег не ставится, T6/T7 не запускались.

Метрики: claude-sonnet-5 (Claude Code). 9 промптов (212-220). Токены
харнесс не отдаёт для основной сессии; сабагенты ~1M суммарно.
Стоимость живых вызовов сильно меньше $0.10; Claude Code — $0, подписка.

GitHub: https://github.com/axyi/tg-agent-bot
