tg-agent-bot v1.10.3 — новая модель и судья, exec-гвард, две дыры в маркерах закрыты — прогон остановлен бюджетом ремонта, не моделью

Условия: сменить модель (openai/gpt-4.1, судья anthropic/claude-sonnet-5
— другой вендор), добавить exec-гвард (defense in depth: отказ на
env/printenv, .env-файлы, /proc/*/environ — не фикс, gate 8 (e) всё
равно валит попытку), закрыть два промаха маркеров v1.10.2, сделать
запись о делегировании обязательной lint-правилом, добить бумаги трёх
остановленных релизов.

Результат: T0-T5 — офлайн-сьют и живые гейты зелёные, ревью в чистом
контексте всё подтвердило независимо. Три repair-цикла из трёх ушли на
неучтённые в спеке правки существующих тестов (T1, T2, T4), каждый
перепроверен по точному тексту спеки. На T6 пять новых мутаций
написаны и изолированно проверены, но четвёртая неучтённая находка
того же класса (тест ломается при появлении новой мутации) всплыла
при бюджете 0/3. Правку не сделали — четвёртый цикл втихую нарушил бы
правило «никогда не молча». Прогон остановлен: версия не бампится, тег
не ставится, T7 не запускается. gate 8 ни разу не вызывался — модель
под тестом не оценивалась.

Метрики: claude-sonnet-5 (Claude Code). 7 промптов (220-226).
Сабагенты ~1.23M токенов, харнесс не отдаёт токены основной сессии.
Живые вызовы — сильно меньше $0.10; Claude Code — $0, подписка.

GitHub: https://github.com/axyi/tg-agent-bot
