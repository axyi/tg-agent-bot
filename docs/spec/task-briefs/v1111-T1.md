# Brief v1111-T1 — OUT-01/OUT-02, delegated

Spec: `docs/spec/spec-v1.11.1.md:233-312` (REQ-V1111-OUT-01, -02),
test table rows `:678-682` (`T-V1111-OUT-01…05`), error matrix rows 3-4
(`:617-618`). Base tree: `2431034` (tag `v1.11.0`), current `HEAD` is
`ffdca15` (T0 complete). Read the cited spec ranges yourself for exact
wording — this brief is a pointer, not a copy.

**Test-first (EC-02)**: write `tests/test_v1111_out.py` first (five
tests, exact names/assertions at spec `:678-682`), watch them fail for
the right reason, then implement.

**OUT-01**: `TelegramError` (`bot.py:176-188` ±5) gains `status: int |
None = None` stored as `self.status`. `TelegramClient.call`
(`:219-263` ±5) sets it from `response.status_code` on every raise that
follows a response (401/404 fatal, 429, and the other response-driven
raises), leaves it `None` on the transport raise. `_call_with_retry`
(`:265-280` ±5) stays **byte-unchanged**. `send_pre` (`:2540-2564` ±5)
and `edit_pre` (`:2567-2585` ±5): the one-time plain resend fires only
when `exc.status == 400`; every other `TelegramError` (fatal, 429 past
budget, 5xx, `status is None`) is logged via the existing ERROR line,
`None` returned, no resend; a failure of the resend itself is logged
once by the same line, `None` returned, never a second fallback. All
fallback attempts omit `parse_mode`. The `fatal` check is subsumed
(401/404 never equal 400) — read the existing code before deciding
whether to delete or just bypass it.

**OUT-02**: three docstring replacements, verbatim text at spec
`:274-311` — (a) `bot.send_pre`'s docstring (the exact block quoted in
the spec) and `edit_pre`'s "400-only rule" phrasing; (b)
`tables.fit_lines`'s docstring drops the `bot.py:1190-1197` /
`` `_fit` `` parenthetical; (c) `IngestJob`'s docstring first sentence,
exact text at spec `:300-308`, other paragraphs/read/write sites
unchanged.

**Unchanged**: at-most-once semantics, `_pre_text` order, the payload
pin; `tests/test_v1110_out.py:359-422`'s four `T-V1110-OUT-05` cases
must stay green **unamended**. README's `## Error behaviour` gains one
row (400 only) — that edit belongs to T2/T4 per §10's task split, do
not touch README here unless a test you write requires it (it
shouldn't — DOC rows are T4's).

**Commit + gates**: one commit, prompt 257. Before this commit's own
gates, record `ffdca15`'s own `gitleaks-tree` result (exit 0, already
scanned by the orchestrator, late per the established lag — the
orchestrator will supply the exact wording) in
`docs/reports/report-v1.11.1.md`'s T0 section, plus this commit's T1
section (gates 1-4 output, `lint-docs`, the delegation-record bullet
`- T1 | delegated: yes | to: ... | brief: docs/spec/task-briefs/v1111-T1.md
| map vs actual: ...`, per the grammar in `devtools/checks.py:1604`). Run
gates 1-4 (`uv sync --locked`, `ruff check .`, `pytest`, `bot.py
--selftest`) and `devtools/checks.py lint-docs`, all green, before
committing. Do not run the standalone `gitleaks-tree` block yourself —
return control to the orchestrator for that (it runs after your commit
lands, same lag pattern as T0).

**Constraints**: never open/print/grep `.env`, `data/`, `bot.db`,
`sandbox/`, `exec_audit.jsonl`; no LAN address or secret literal
anywhere; zero new dependencies; `docs/llm-usage.md` gets one new row
(next free number) summarizing this task. Commit message: conventional
type, ≤72-char header, body names `docs/prompts/257-v1111-t1-out.md`
(write that prompt file too, using `docs/prompts/TEMPLATE.md`'s shape),
ends with the same Co-Authored-By/Claude-Session trailer as recent
commits in `git log -3`. `--no-verify` never.

Return a summary: files touched, the five new tests' pass/fail, the
`T-V1110-OUT-05` regression check result, gate outputs, commit sha.
Never paste full file contents.
