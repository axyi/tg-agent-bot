# Task brief v1104-T2 — land the five `v1103-*` mutation entries (MUT-01, MUT-02)

Repo: `/home/akh/aihome/coders-su/projects/tg-agent-bot`. Spec:
`docs/spec/spec-v1.10.4.md` §3 (`REQ-V1104-MUT-01`, lines **255-369**;
`REQ-V1104-MUT-02`, lines **371-396**), §4 error-matrix rows 1, 1b, 2, 3
(lines **450-453**), §1 row 2b (line **97**), Appendix D (lines
**1085-1281** — the byte-exact two-path stash diff). Report:
`docs/reports/report-v1.10.4.md` (append `## T2`, replacing the
placeholder — T1's `## T1` section must already be present; do not touch
it).

Run this **after T1 is committed**. Base for this task: T1's commit
(`git log -1`).

## Goal, in strict order — test-first, then check, then apply

1. **First**, land `T-V1104-MUT-01` through `-05` and `T-V1104-ERR-01`
   (§6.1 table, spec lines 500-504, 521) in `tests/test_v1104_gates.py`
   (the file T1 already created — add to it, don't recreate) and run them
   against the **post-T1, pre-apply** tree. Record the result: `-MUT-01`,
   `-02`, `-03` are expected **red** (the five entries/gate don't exist
   yet); `-MUT-04`, `-MUT-05`, `T-V1104-ERR-01` are expected **green
   structurally already** (they don't depend on the five entries being
   present — read spec lines 306-317 for exactly why each one is red or
   green at this point, and record it that way, never claim red when the
   spec says structurally green).
2. **Only then**, the stash identity check and apply. `git rev-parse
   stash@{0}` — if it still equals `e3c6e3ff3bee60bff183ae056621d4dc984cd5a3`
   (T0 already confirmed this and T1 does not touch the stash), re-run the
   exact two-path check on the **post-T1** tree:
   ```bash
   git stash show -p stash@{0} | git apply --check --include='devtools/mutation_check.py' --include='config/quality_gates.yaml' -
   git stash show -p stash@{0} | git apply --include='devtools/mutation_check.py' --include='config/quality_gates.yaml' -
   ```
   Exit 0 on `--check` is the id-match route (§3, spec lines 322-329) —
   apply for real with the second command. If the stash is absent, has a
   different id, **or** `--check` fails on the post-T1 tree despite the
   id matching, read ERR-01 rows 1 and 1b (spec lines 450-451) and
   Appendix D (spec lines 1085-1281) carefully: row 1 (absent/other-id) —
   extract Appendix D's fenced diff into a file, verify its `sha256sum`
   equals `a54e5faedeaa239759d768cec8b6636982e517cafed03281a441949e440d9e11`
   (the fenced content, first `diff --git` line to the last line before
   the closing fence, each line newline-terminated), `git apply --check`
   on the extracted file (exit 0 required), then `git apply` it — same
   two paths, same bytes. Row 1b (id matches but `--check` fails on the
   post-T1 tree) — **never retry the same patch blindly**: this is a
   construction defect, disclose it in the report, and repair only by a
   byte-exact reconstruction of Appendix D's specified post-image hunks
   (spend one repair cycle, recorded). **Never** `git stash pop`,
   `apply` (bare), or `drop` — read-only stash access only (NG-03).
   **The stash's three test hunks are NOT applied** — they carry frozen
   shapes T1 already retired; only the two non-test paths
   (`devtools/mutation_check.py`, `config/quality_gates.yaml`) go in.
3. **After applying** (either route), before any isolation proof, verify
   the post-image: `git hash-object devtools/mutation_check.py` must
   equal `d09909d…` (Appendix D's stated `index` post-image — the file
   differs from `f3ce1a5` only by the five entries and the rationale
   comment); the yaml's `mutation-subsets` line, `mutation-v1103` block
   and `mutation-all` comment must equal Appendix D's post-image content
   (not the whole-file hash — T1 already repointed `report_path`
   elsewhere in the same file). A mismatch is ERR-01 row 2's class — stop
   and disclose, don't paper over it.
4. **Rewrite the `mutation-all` sentence** (MUT-02(c), spec lines 381-395):
   the stash's own new paragraph reads "is now 144" at this point via
   Appendix D's applied hunk already — confirm it reads exactly the
   fragment `` MUTATIONS)` is now 144`` on one comment line (spec lines
   386-391 give the sentence verbatim). If Appendix D's applied text
   doesn't already match this (it should — the fallback diff already
   carries the target text per its own comment at spec lines
   1162-1164), fix it to match exactly; the v1.10.3 predecessor sentence
   ("closed at 139") must NOT be the one in the final block — check
   which sentence Appendix D's diff actually inserts (its hunk at spec
   lines 1156-1164 shows BOTH the "closed at 139" line staying AND a new
   "is now 144" paragraph being appended — read it closely, that's the
   intended final shape, not a contradiction).
5. **Add EC-02 row 2b**: `tests/test_v1100_gates.py`'s rewritten
   release-groups test (T1 already converted it to per-group assertions)
   gets **one more** appended assertion — a fourth group `_V1103_IDS`
   (the five ids of §3, in order), same shape as the other three groups
   T1 added. Append only — do not edit T1's three existing group
   assertions.
6. **Rerun the six T2 tests green** (`-MUT-01` through `-05`,
   `T-V1104-ERR-01`) now against the post-apply tree.
7. **Per-entry isolation proofs** (`REQ-V1103-GATE-02`'s two-proof rule,
   referenced at spec lines 357-366): for each of the five entries —
   - the mutated module still imports (exit 0) after the `find`→`replace`
     substitution;
   - **collect the exact pytest node id(s)** for its named killer test
     (read the killer column in the table at spec lines 266-272 — note
     entry 4's real killer is `T-V1103-LINT-09`, NOT `-06`/`-07` which
     the old (wrong) table named — this is already corrected in this
     spec's table, just don't second-guess it back);
   - run pytest with **only those explicit node ids, never `-k`**;
   - assert at least one selected node fails for the expected reason,
     **record the selected count** — a collection miss or any extra
     selected node is a construction defect (ERR-01 row 3), disclose and
     repair (repair cycle).
   Record for each entry: the §3 row, the import-proof exit, the
   collection command, the exact node ids, the selected count, the
   pytest command run, its result, the failing assertion text.

`len(devtools.mutation_check.MUTATIONS)` becomes **144** after this
task.

## The five `find`/`replace` pairs — verbatim from spec §3 (lines 277-293)

Do not retype these from memory or re-derive them — copy them character
for character from `docs/spec/spec-v1.10.4.md` lines 277-293 (or from
Appendix D's applied diff, which carries the identical bytes) into
`devtools/mutation_check.py`'s `MUTATIONS` list, in the shape
`{id, path, find, replace, why}` (five keys each), placed immediately
after the `v1102-hal-gap-marker-dropped` entry (spec-pinned location,
`devtools/mutation_check.py:1897-1922` on the pre-apply tree) — this
placement is exactly what Appendix D's diff hunk already does; if you
took the stash-apply route (not the Appendix D-extraction fallback
route) this step is already done for you by the apply itself, you are
only verifying it, not re-authoring it.

## Non-goals (NG-02, NG-03)

No `v1104-*` mutation entry — this release adds no new mutation, only
lands five already-authored ones. No `git stash pop`/`apply`/`drop`
anywhere, ever, this task.

## Acceptance

- `uv run --locked ruff check .` clean.
- The pre-apply test record captured exactly as step 1 describes (three
  red, three green-structurally, recorded as such — never claimed red
  when structurally green).
- Six T2 tests green after apply; five isolation proofs all recorded
  with exact node ids and selected counts.
- `git hash-object devtools/mutation_check.py` == `d09909d…` (post-image
  check passed).
- `len(mc.MUTATIONS) == 144`.
- Full `uv run --locked pytest -q` green.
- Gates 1-4 only. Do **NOT** run `bot.py --selftest-live`,
  `devtools/rag_eval.py`, `devtools/agent_eval.py`, or
  `devtools/mutation_check.py` (the CLI itself, full or any `--select`)
  this task — gate 6's calibration and run are T4's, not yours; you only
  edit the registry and yaml, you do not execute the mutation runner as
  a gate.
- `doctor` green.
- On the id-match route, `git rev-parse stash@{0}` still equals
  `e3c6e3ff…` after this task (never dropped); on the fallback route,
  record the observed stash state and leave every stash untouched.

## Delegation record

Report back the exact five-cell bullet:
`- T2 | delegated: yes | to: <your agent identity> | brief:
docs/spec/task-briefs/v1104-T2.md | map vs actual: <...>`

## Reading map (§10.1)

`devtools/mutation_check.py:40-61` (registry shape), tail only
(`:1895-1926` pre-apply), `:2304-2325` (`run_one`, the DRIFTED path);
`config/quality_gates.yaml:26-31`, `:574-590`, `:696-716`; the five
`find` lines only (spec §3); `tests/test_v1100_gates.py` (the group test
T1 rewrote); `tests/test_v1104_gates.py` (T1's file, yours to extend).

## Commit

One commit, this task only. Conventional commit header, body references
`(prompt: docs/prompts/230-v1104-t2-mutations.md)`.
