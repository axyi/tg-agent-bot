# Task brief v1100-T7

Spec: `docs/spec/spec-v1.10.0.md`, §13 (`REQ-V1100-GATE-02`), §12.1
(`T-V1100-GATE-01`). Read §13's `GATE-02` in full.

**Exclusivity requirement**: `mutation_check.py` mutates real tracked files
in place during its mutate → run-tests → revert cycle. Confirm
`git status --porcelain` is clean before you start and run nothing else
against this repository concurrently for the duration of this task (no
other gate, no other subagent). This matches this project's own recorded
operational lesson.

## Goal

Add seven `v1100-*` mutation entries to `devtools/mutation_check.py`'s
`MUTATIONS` list, register `mutation-v1100` in `config/quality_gates.yaml`
with a freshly measured timeout, add it to the `mutation-subsets` profile,
and prove each entry is killed by the test named in the table below —
**empirically**, by actually running the mutate → red → revert cycle, not
by assuming the spec's stated killer is correct (T1's delegate already
found one spec inaccuracy — see the last row below).

## The seven entries (author each `find` against the shipped source — read the file first)

| id | path | mechanism it breaks | spec-stated killer |
|---|---|---|---|
| `v1100-injection-checker-always-passes` | `devtools/agent_eval.py` | `check_injection` returns `(True, "")` before any clause | `T-V1100-RT-02`, `T-V1100-RT-05`, `T-V1100-RT-08` |
| `v1100-hallucination-any-of-vacuous` | `devtools/agent_eval.py` | the marker condition `(any_of match) or (HAL_MARKERS match and entity match)` replaced by `True` | `T-V1100-RT-03`, `T-V1100-RT-05`, `T-V1100-RT-09`, `T-V1100-RT-10` |
| `v1100-memory-structural-check-dropped` | `devtools/agent_eval.py` | `check_memory_reset`'s structural half replaced by `True` | `T-V1100-RT-04`, `T-V1100-RUN-05` |
| `v1100-judge-floor-zeroed` | `devtools/agent_eval.py` | `JUDGE_FLOOR = 0.8` → `JUDGE_FLOOR = 0.0` | `T-V1100-JDG-05` |
| `v1100-judge-guard-dropped` | `devtools/agent_eval.py` | the `judge.describe() == llm.describe()` comparison → `False` | `T-V1100-JDG-02` |
| `v1100-reply-parts-split-before-redact` | `bot.py` | `split_message(redact(text))` → `split_message(text)` | spec says `T-V1100-OUT-02`, **but T1's delegate empirically found this mutation is actually killed by `T-V1100-OUT-01`'s boundary-straddling test, not `-OUT-02`** (see `docs/reports/report-v1.10.0.md`'s T1 section) — verify this yourself and record whichever test(s) *actually* kill it in your `why` comment, citing both the spec's claim and your empirical finding if they differ |
| `v1100-inbound-cap-code-points` | `bot.py` | `utf16_length(text) > MAX_MESSAGE_CHARS` → `len(text) > MAX_MESSAGE_CHARS` | `T-V1100-SAN-02` |

Each entry follows the existing `MUTATIONS` list shape exactly — look at
the last few entries in the file (`devtools/mutation_check.py`, near the
end, before `_IDS = [m["id"] for m in MUTATIONS]`) for the dict shape
(`id`, `path`, `find`, `replace`, `why`) and the comment-block convention
above each entry (or group of entries) explaining what it defends and
which test kills it, in the voice of the `v195-init-schema-drops-the-pair`
entry's own preceding comment. Append all seven after the last existing
entry, before the closing `]`.

**Each `find` string MUST match exactly once in its file** — write a throwaway
`grep -c` check for each one before adding it to the list, and design the
`find` string narrowly enough (include surrounding context lines) that it
cannot accidentally match a second occurrence elsewhere in the file (this
matters especially in `devtools/agent_eval.py`, which by T7 is a large
file with several similarly-shaped comparisons).

## Registering `mutation-v1100` in `config/quality_gates.yaml`

Look at `mutation-v190`'s block (`grep -n "mutation-v190:"`) as your exact
template — same six keys, same order, only the `--select` prefix
(`"v1100-"` instead of `"v190-"`) and `timeout_seconds` differ:

```yaml
  mutation-v1100:
    kind: command
    result_mode: exit_status
    argv: [uv, run, --locked, python, devtools/mutation_check.py, --select, "v1100-"]
    placeholders: {}
    success_exit_codes: [0]
    blocking: true
    diff_scoped: false
    timeout_seconds: <measured>
```

Insert it immediately after the `mutation-v190` block. **Measure the
timeout yourself, following this file's own established rule** (read the
comment blocks above `mutation-v160`/`mutation-v180`/`mutation-v190` for
the exact convention — "2x a direct measured run + 70s, rounded up to the
next 10s" is the rule this codebase settled on after v1.9.2 T2's review;
some older comments predate that correction and use a slightly different
rounding, ignore those, follow the newest-dated convention): once all
seven entries are added and each individually verified to kill (mutate →
run the killer test(s) → confirm red → revert), run
`time uv run --locked python devtools/mutation_check.py --select "v1100-"`
once for real (all seven, 7/7 should kill), take the wall-clock, apply the
rule, write a dated comment block above your new gate entry in the same
style as the existing ones (cite the measured wall, the arithmetic, and
the resulting rounded value).

Add `mutation-v1100` to the `mutation-subsets` profile list
(`config/quality_gates.yaml:29`), after `mutation-v190`.

`mutation-all` (`argv`/`timeout_seconds` unchanged — do not touch its
`timeout_seconds`, only its own comment): if you find a comment there
naming the current total entry count (something like "N entries"), update
it to 127 (120 existing + 7 new). If no such comment exists, skip this —
do not invent one.

## Test: `T-V1100-GATE-01`

Add to `tests/test_v1100_gates.py` (T6's file — append, do not recreate):
`devtools.mutation_check.MUTATIONS` has exactly seven `v1100-*` entries
after the last `v195-*` entry, each with the five keys (`id`, `path`,
`find`, `replace`, `why`) and no other key, each `path` existing in the
repo, and each `find` occurring **exactly once** in its file (read the
file, `str.count`).

## Constraints

- Touch: `devtools/mutation_check.py`, `config/quality_gates.yaml`,
  `tests/test_v1100_gates.py`. Do not touch `devtools/agent_eval.py` or
  `bot.py`'s actual (non-mutated, non-test) source — you are only adding
  *mutation table entries* that describe edits to make transiently during
  the mutation gate's own run; the real files must be byte-identical to
  what T5/T1 committed once you're done (verify with `git status
  --porcelain` showing nothing beyond your three touched files).
- Run gates **1-4**, plus `uv run --locked python devtools/mutation_check.py
  --select "v1100-"` (must be 7/7 killed) and, if time allows,
  `uv run --locked python devtools/checks.py doctor`. **Do not run the full
  `mutation_check.py` with no `--select`** (that's gate 6's full run,
  reserved for T9) and never gate 5, 7, 8, and never `--profile full`.
- `--no-verify` never used.
- One commit referencing `docs/prompts/199-v1100-t7-mutations.md`.

## Report back

For each of the seven entries: the id, confirmation `find` matches exactly
once, and which test(s) empirically killed it (name them, even if they
differ from the spec's stated killer — especially for the `reply-parts`
one, where a discrepancy is expected). The measured `--select "v1100-"`
wall-clock, your computed `timeout_seconds`, gate 1-4 results, and the
commit hash.
