# Task brief v1101-T4

Spec: `docs/spec/spec-v1.10.1.md`, §9 `REQ-V1101-PRM-01`, `REQ-V1101-PRM-02`,
§2 `REQ-V1101-NG-07`. Read those in full before writing anything — this
brief carries only the load-bearing bits already resolved, not a
substitute for the spec text.

## Goal

Two literal changes so OpenRouter models reliably call `search_documents`
before answering from memory (the carried v1.9.4/v1.10.0 tail: models
answer from general knowledge instead of searching). Measure gate 7's
advisory conversation-aware smoke **twice** — once on the unchanged tree,
once after the edit — to see whether it actually helped. The verdict stays
advisory either way (NG-07): you are not trying to make it pass, you are
measuring and recording whatever it does.

## Current source, exact (as of commit `526e19d`)

`agent.py:124-129`, the comment above `SKILLS_HEADER`:
```python
# REQ-V13-PFX-01: the cacheable prefix, compressed to imperative English and
# kept under 550 characters (measured with `{skill_lines}` removed). Every
# statement here is load-bearing and pinned by `tests/test_prefix.py`; the tool
# catalog documents the tools, so this prompt never repeats their signatures.
```
The "kept under 550 characters" clause is already stale (the real cap is
`PROMPT_LIMIT`, currently 700 in `tests/test_prefix.py:29`) — correct it
to name `PROMPT_LIMIT` instead of a bare number, e.g. "kept within
`PROMPT_LIMIT` (`tests/test_prefix.py`)".

`agent.py`'s `SYSTEM_PROMPT` (the multi-line string starting around line
132), the docs line — today two source lines joined by `\`:
```python
Docs: search_documents finds user files; answer from returned passages only, \
cite Source: <filename> (page N); else say the docs lack it.
```
Replace with this **exact** text (203 characters, ASCII, one rendered
line — you may still wrap it with `\` continuations in source, same style
as its neighbours):
```
Docs: when the user has uploaded files, call search_documents BEFORE answering anything they could answer; answer from returned passages only, cite Source: <filename> (page N); else say the docs lack it.
```
Nothing else in `SYSTEM_PROMPT` or `agent.py` changes.

`tools.py`, the `search_documents` tool spec (`"description":` around
line 1372-1375):
```python
"description": (
    "Search the users uploaded documents; returns the best passages with "
    "filename and page. Use it before answering about their files."
),
```
Replace the string with this **exact** text:
```
Search the user's uploaded documents; returns the best passages with filename and page. Call it before answering any question their files could answer — never answer such questions from memory.
```
The `parameters` object right below it is unchanged.

## Tests to amend (exhaustive)

- `tests/test_prefix.py:29` — `PROMPT_LIMIT = 700` → `PROMPT_LIMIT = 800`;
  append to the trailing comment: `, raised by REQ-V1101-PRM-01 (EC-02)`.
- `tests/test_v190_tool.py:372-390`:
  - `PROMPT_LINE` (the module-level constant) becomes PRM-01's new docs
    line (the exact 203-char text above).
  - `test_t_v190_tool_05_the_line_is_140_chars_or_fewer_and_ascii`: rename
    to `test_t_v190_tool_05_the_line_is_210_chars_or_fewer_and_ascii`;
    `assert len(PROMPT_LINE) <= 140` → `<= 210`.
  - `test_t_v190_tool_05_the_whole_prompt_stays_at_or_under_700_chars`:
    rename to `test_t_v190_tool_05_the_whole_prompt_stays_at_or_under_800_chars`;
    `measured <= 700` → `<= 800`.
  - `test_t_v190_tool_05_the_prompt_gains_exactly_this_line` is unchanged
    in body (still `assert PROMPT_LINE in agent.SYSTEM_PROMPT`) — it just
    now checks against the new `PROMPT_LINE` value.

`tests/test_v1_guardrails.py:829-864` is **not edited** — its substrings
must survive unchanged; confirm it stays green, do not touch it (a red
there is a defect in your change, never a licence to edit it).

Add a small test (in whichever file you judge fits best — `tests/
test_prefix.py` or `tests/test_v190_tool.py`) proving the rendered prompt
is **736** characters after your edit (`len(agent.SYSTEM_PROMPT.replace("{skill_lines}", ""))
== 736`) — if your exact-text edit above doesn't land at exactly 736, the
literal text has drifted from what's quoted here; recount and match the
literal above precisely rather than adjusting the expected number.

## PRM-02 — gate 7 twice, in sequence, nothing else concurrent

This is a **measurement**, not a repair target. Steps, in this exact
order:

1. **Before**: on the tree as it stands at the start of this task (before
   your edit), run `uv run --locked python devtools/rag_eval.py` live.
   Record: exit code, wall time, `hybrid` `recall@5`, and the two
   advisory smoke lines verbatim — `conversation-aware smoke (TOOL-06
   pin): pass|fail -- <detail>` and `conversation-aware smoke
   (context-proof): pass|fail -- <detail>`. **This is not the same as
   T1's gate-7 run** — T1's tree lacked this task's literals, but more
   importantly PRM-02 requires this run happen at T4, immediately before
   your edit, not reused from an earlier task.
2. Make the two literal changes and the test amendments above.
3. **After**: run `uv run --locked python devtools/rag_eval.py` live
   again, same way, same machine state, nothing else running
   concurrently. Record the same four values.
4. Build a four-cell table (before/after × TOOL-06/context-proof) plus
   each run's wall time and `recall@5`, for your final report. `recall@5`
   **must** stay at or above its floor in **both** runs — if it drops
   below the floor in either run, stop and report back (this would be a
   real regression, not the advisory smoke). The TOOL-06/context-proof
   verdicts themselves are advisory (NG-07) — pass, fail, or a change
   either direction is all fine to simply record.

Exactly two gate-7 executions this task, in sequence — never run it a
third time, never run gate 5, 6, or 8.

## Constraints

- Touch only: `agent.py` (the one comment + the one docs-line string),
  `tools.py` (the one description string), `tests/test_prefix.py:29`
  (+ your new 736-char test if you add it there), `tests/
  test_v190_tool.py:372-390`. Do not touch any other file.
- Run gates **1–4** (`uv sync --locked`, `ruff check .`, `pytest`,
  `bot.py --selftest`) offline first, both green, before either gate-7
  run. Gate 7 runs live exactly twice as PRM-02 describes above. Never
  gate 5, 6, or 8.
- `.env` is never read directly. `--no-verify` is never used.
- One commit at the end, conventional-commit message, referencing
  `docs/prompts/207-v1101-t4-prompt-tool-literals.md` (create this prompt
  file yourself, in the shape of `docs/prompts/TEMPLATE.md`, Stage T4, if
  the orchestrator has not already created it). Include
  `docs/spec/task-briefs/v1101-T4.md` in the same commit.

## Report back

A short summary: what changed (file:line), the four-cell before/after
table with wall times and `recall@5` figures, gates 1–4 exit codes, and
the final commit sha. Do not paste full file contents back — name
file:line ranges only.
