# Task brief v1101-T2

Spec: `docs/spec/spec-v1.10.1.md`, §6 `REQ-V1101-GC-01`, §13 `REQ-V1101-GATE-03`,
§14 `REQ-V1101-RPT-01` (first sentence only — the rest of RPT-01 is T7/T8's
job, not yours). Read those sections in full before writing anything — this
brief carries only the load-bearing bits already resolved, not a substitute
for the spec text.

## Goal

An optional, validated `env:` map on **command** gates, passed through to
the subprocess; pin it on `skylos` (the gate that needs
`SKYLOS_GREP_BUDGET=180` to avoid `SKY-ANALYSIS-INCOMPLETE`, the
v1.10.0-authoring push failure of 2026-09-13). Repoint the gate-matrix test
and `lint-docs` at this release. Write tests first.

## What to add, verbatim

`devtools/checks.py`:

- `_COMMAND_BASE_KEYS` (`:344-354`) gains `"env"` in the set.
- In `_validate_command_gate` (`:470-524`), after the existing
  `argv`/`placeholders`/`success_exit_codes` checks (right after
  `_validate_placeholders_used(name, argv)` at `:490`, before the
  `result_mode == "findings"` branch at `:492` — `env:` applies to every
  command gate regardless of `result_mode`), add:

  ```python
  if "env" in gate:
      env = gate["env"]
      if not isinstance(env, dict) or not all(
          isinstance(k, str) and k and isinstance(v, str) for k, v in env.items()
      ):
          raise GateConfigError(f"gates.{name}.env must map non-empty string keys to string values")
      for key, value in env.items():
          if not re.fullmatch(r"[A-Za-z_][A-Za-z0-9_]*", key):
              raise GateConfigError(f"gates.{name}.env key is not an identifier: {key}")
          if "\0" in value:
              raise GateConfigError(f"gates.{name}.env value contains NUL: {key}")
          if key.casefold() in _SECRET_ENV_KEYS_CASEFOLDED:
              raise GateConfigError(f"gates.{name}.env must not name a secret: {key}")
  ```

  (check `re` is already imported in `devtools/checks.py`; if not, add the
  import). Define `_SECRET_ENV_KEYS_CASEFOLDED = {"openrouter_api_key",
  "telegram_bot_token"}` near the top of the file — the two registered
  secrets are `OPENROUTER_API_KEY` and `TELEGRAM_BOT_TOKEN`
  each passed to `register_secret(...)` in `config.py` (line numbers shifted
  by T1's edit -- `grep -n register_secret config.py` to confirm current
  ones); compare `key.casefold()` against this set so
  `openrouter_api_key`, `OpenRouter_API_Key`, etc. are all refused too.
- `_validate_builtin_gate` (`:527-539`, `_BUILTIN_BASE_KEYS` at `:356`)
  does **not** gain `"env"` — a builtin gate carrying it must still hit the
  existing unknown-key error at `:536-538`. Do not touch this function
  beyond confirming that.
- `run_argv` (`:1125-1146`) gains a keyword-only `env: dict[str, str] |
  None = None` parameter; the `subprocess.Popen(...)` call
  (`:1129-1136`) gains `env=None if env is None else os.environ | env`. No
  other change to this function's body.
- `execute_command_gate` (`:1254-1300`): the call at `:1294` (`cmd =
  run_argv(argv, repo_root, timeout)`) becomes `cmd = run_argv(argv,
  repo_root, timeout, env=gate.get("env"))`. The other `run_argv(...)`
  call sites in this file (`:908`, `:919`, `:943`, `:1239`, `:1380`,
  `:1411`) are **untouched** — they keep calling it without `env=`, which
  defaults to `None` (unchanged `Popen(env=None)` behaviour, i.e. inherits
  the current environment).

`config/quality_gates.yaml`, the `skylos` entry (`:311-325`): add a
comment right above the entry naming the push failure, then add `env: {
SKYLOS_GREP_BUDGET: "180" }` immediately after `timeout_seconds: 120`:

```yaml
  # REQ-V1101-GC-01: SKYLOS_GREP_BUDGET pinned after the v1.10.0-authoring
  # push failed SKY-ANALYSIS-INCOMPLETE (2026-09-13) without it.
  skylos:
    ...
    timeout_seconds: 120
    env: { SKYLOS_GREP_BUDGET: "180" }
```

No other gate in this file gains `env:`; no `profiles:`/membership change.

`config/quality_gates.yaml:684` (the `lint-docs` gate's `report_path`):
`docs/reports/report-v1.10.0.md` → `docs/reports/report-v1.10.1.md`. Do
not touch `ledger_header` or anything else in that block.

`tests/test_v15_standards.py`:

- `:1821` — the spec file the matrix test parses:
  `docs/spec/spec-v1.10.0.md` → `docs/spec/spec-v1.10.1.md`. **Do not edit
  `docs/spec/spec-v1.10.0.md` itself** — it already carries its own frozen
  26-row matrix; `spec-v1.10.1.md` already carries its own copy (27 rows,
  §13 `REQ-V1101-GATE-03`, load-bearing markup — do not touch that table
  either, it is already correct).
- `_GATE_MATRIX_LABEL_TO_NAME` (`:1772-1800`): add one entry immediately
  after the `"`mutation_check.py --select v1100-`": "mutation-v1100",`
  line: `` "`mutation_check.py --select v1101-`": "mutation-v1101", ``.

`tests/test_v170_bench.py:316-331` — rename
`test_t_v1100_rpt_01_lint_docs_repointed_to_this_release` to
`test_t_v1101_rpt_01_lint_docs_repointed_to_this_release`; change the
assertion `lint_docs["report_path"] == "docs/reports/report-v1.10.0.md"`
to `== "docs/reports/report-v1.10.1.md"`; the docstring may be updated to
name this release, `ledger_header`'s assertion is unchanged.

## Tests to write

New file `tests/test_v1101_gates.py` covering `T-V1101-GC-01`…`GC-06`:

- valid `env:` on a command gate is accepted and reaches `Popen` (mock or
  capture `subprocess.Popen`'s `env=` kwarg through `run_argv` directly,
  or via `execute_command_gate` on a synthetic gate dict).
- `env:` absent → `Popen(env=None)` (today's behaviour, unchanged).
- rejected shapes, each its own test with the exact `GateConfigError`
  message asserted: not a mapping; a non-string value; an empty-string
  key; a key failing `^[A-Za-z_][A-Za-z0-9_]*$` (e.g. `"A=B"`, a key
  starting with a digit); a value containing `"\0"`; a key naming a secret
  in **any** case (`OPENROUTER_API_KEY`, `openrouter_api_key`,
  `Telegram_Bot_Token`).
- a builtin gate carrying `env:` still raises the existing unknown-key
  `GateConfigError` (not a new message — `env` is simply not in
  `_BUILTIN_BASE_KEYS`).
- the `skylos` entry in the real `config/quality_gates.yaml`, loaded via
  `load_gate_config`, has `env == {"SKYLOS_GREP_BUDGET": "180"}`; no other
  gate in the loaded config carries `env:`.
- `T-V1101-GATE-03`: `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table`
  (already amended above) passes against `spec-v1.10.1.md`'s own matrix.
- `T-V1101-RPT-01` (first sentence only): `load_gate_config()["gates"]
  ["lint-docs"]["report_path"] == "docs/reports/report-v1.10.1.md"` (the
  renamed `tests/test_v170_bench.py` test already covers this — a
  duplicate assertion here is fine but not required).
- `T-V1101-ERR-01` row 6: each of the five `GateConfigError` shapes above
  (not-a-mapping, non-identifier key, NUL value, secret-naming key in any
  case, builtin-gate-carrying-env) is covered by a dedicated test whose
  message is asserted, not just the exception type.

Run `uv run --locked python devtools/checks.py doctor` and confirm it
still passes after your yaml edit (a malformed `env:` shape would make
`doctor` red at load time).

## Constraints

- Touch only: `devtools/checks.py`, `config/quality_gates.yaml`,
  `tests/test_v15_standards.py:1772-1834` (only the two named edits —
  nothing else in that file), `tests/test_v170_bench.py:316-331` (only
  the rename + assertion — nothing else in that file), and the new
  `tests/test_v1101_gates.py`. Do not touch `config.py`,
  `llm/embeddings.py`, `bot.py`, `devtools/agent_eval.py`,
  `devtools/mutation_check.py`, or any `v1101-*` mutation entry (that's
  T6's job — do not add `v1101-gate-env-passthrough-dropped` yourself,
  it lands at T6 against your already-shipped code).
- Run gates **1–4 only** (`uv sync --locked`, `ruff check .`, `pytest`,
  `bot.py --selftest`), plus `uv run --locked python devtools/checks.py
  doctor` and `uv run --locked python devtools/checks.py lint-docs`. Do
  **not** run gate 5, 6, 7, or 8 — not your job this task.
- `.env` is never read directly. `--no-verify` is never used.
- One commit at the end, conventional-commit message, referencing
  `docs/prompts/205-v1101-t2-gate-env.md` (create this prompt file
  yourself, in the shape of `docs/prompts/TEMPLATE.md`, Stage T2, if the
  orchestrator has not already created it). Include
  `docs/spec/task-briefs/v1101-T2.md` in the same commit.

## Report back

A short summary: what changed (file:line), the new test count and names,
the exact `GateConfigError` message text your implementation produces for
each rejected shape, gates 1–4 exit codes, `doctor` and `lint-docs` exit
codes, and confirmation that `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table`
passes against `spec-v1.10.1.md`. Do not paste full file contents back —
name file:line ranges only.
