# Task brief — v1.9.4 T4: the "not now" ruff rows, now

One commit, prompt 184, after `v194-T3.md` lands. Closes the three "not
now" rows of v1.9.3's decision table (`pyproject.toml` `[tool.ruff.lint]`
comment block): `PTH*` (18 hits), `RUF043` (1). `PLW0603` (3 module-level
`global` statements) stays "never" — it is the module-state pattern
`_shutdown` / `_started_at` / `_dropped_spans` rely on; move its row from
"not now" to "never" with that reason.

Inventory on `673f32f`:

```
uv run --locked ruff check . --select PTH,RUF043 --statistics
 5 PTH101 os-chmod        5 PTH116 os-stat      4 PTH123 builtin-open
 1 PTH110 os-path-exists  1 PTH114 os-path-islink  1 PTH118 os-path-join
 1 PTH108 os-unlink       1 RUF043 pytest-raises-ambiguous-pattern
```

Rules that bite:

- **`os.chmod` / `os.stat` in `storage.py`** are deliberate mode-bit code
  (`storage.py:430-440`, the `-wal`/`-shm` and parent-directory
  permissions, REQ-V16x security items; v1.9.2 dismissed
  `storage.py:487 insecure-file-permissions` on exactly that basis).
  `Path(p).chmod(mode)` and `Path(p).stat()` are the same syscalls — the
  rewrite is fine **only if** the surrounding `FileNotFoundError` handling
  and the `str(db_path) + suffix` string arithmetic keep identical
  semantics (`Path(str(db_path) + suffix)`, not `db_path.with_suffix` —
  the latter replaces `.db`). Read each of the 10 sites; keep the tests
  that pin the resulting modes green (grep `0o600`, `0o700` in `tests/`).
- **`PTH123` `open()`** (4): `Path.open()` — same args, same encoding;
  where the target is a string path from config, wrap it once.
- **`PTH110/114/118/108`** (4): mechanical.
- **`RUF043`** (1): the `pytest.raises(match=...)` pattern has regex
  metacharacters — escape with `re.escape` or anchor it; the test must
  still fail on a different message (prove by mutating the message under
  test once by hand, then restoring).
- Mutation `find` strings: any of these lines may be a `find` target
  (`storage.py` has many). Drift script after the change; re-derive with
  identical semantics; `--only` each re-derived id. `devtools/
  bench_scenarios.py` is byte-frozen (REQ-V13-BEN-12) — if PTH hits it,
  add the rule to its `per-file-ignores` with the same reason and say so.
- Then `PTH` and `RUF043` join `select` (family-level for `PTH` is fine —
  no never-listed member); the comment block's rows move to "adopted".

Paperwork: `docs/prompts/184-v194-pth-tail.md`; report section "T4 — the
not-now rows"; `docs/llm-usage.md` row 94; delegation line; README/AGENTS
sentences describing the rule set if any name a count. All `v194-T1.md`
constraints (amend → re-read counts; self-excluding wait patterns; no
concurrency with mutation runs; drift 0 at the current entry count).

Acceptance (sequential): `ruff check .` 0; `ruff format --check .` 0;
`pytest` 0 (collected count); `bot.py --selftest` 0; `checks.py lint-docs`
0; drift script all entries; `--only` each re-derived entry killed.

Stop and report when: a `storage.py` rewrite changes a mode bit or an
exception path (then keep `os.*` at that site with `# noqa: PTHxxx --
<why>` and count it); a mutation cannot be re-derived with identical
semantics.

Return a summary only: hits fixed per rule, noqa count with reasons,
drift/re-derive record, commit hash, acceptance exit codes.
