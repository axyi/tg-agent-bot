# Task brief — v1.9.4 T5: version bump, paperwork, authoritative gates, tag

Same shape as `docs/spec/task-briefs/v193-T4.md` (read it; only the
differences are restated). Runs after T1–T4 and the clean-context review
(and its fix commit, if any) have landed. Prompt number: the next free one
after the review-fix prompt (check `ls docs/prompts | sort -n | tail -1`).

## What v1.9.4 is

The operator's "по всем трём предложенным пунктам и прочим хвостам":

- T1 — secrets are masked at the logging layer (`RedactingFormatter` at
  every entry point; tracebacks and chained exceptions included); every
  hand-written `redact()` call kept.
- T2 — gate 7's conversation smoke has a context-proof third turn whose
  `fail` means something; the smoke's chat route is measurable on
  OpenRouter via an opt-in env; the wall is explained by measurement.
- T3 — a hung mutation is reported as `errored` with its id after 180 s
  instead of tripping the gate's own timeout nameless.
- T4 — the `PTH`/`RUF043` "not now" rows adopted; `PLW0603` moved to
  "never" with its reason.

## What to do

1. Version 1.9.3 → **1.9.4**; repoint `tests/test_v193_version.py` to the
   frozen `v1.9.3` tag blob; new `tests/test_v194_version.py` red-before /
   green-after.
2. Count-bearing lines (`AGENTS.md` test count and mutation-entry count,
   README release table, `tests/test_v190_agents.py` pins) set to what the
   final tree collects, measured after every other edit.
3. `docs/reports/report-v1.9.4.md` finished: consolidated gates table;
   the review summarised with its fix commit; the per-task delegation
   record complete (T1–T5 by brief file, the review as a clean-context
   review, its fixes by brief file, executor models named); disclosures
   stated as such (trailer string; anything T1–T4 flagged; the
   `LLM_EVAL_CHAT_MODEL` decision left to the operator with both numbers);
   open tails — none unless one is genuinely open; the Ledger row
   (v1.9.3's shape; prompts of this release from 181 to the last; review
   findings tallied from the review brief file, not from memory).
4. `docs/reports/tg-post-v1.9.4.md` — Russian, < 1500 chars, executor
   models, repo link; lead with: secrets masked at the logging layer,
   gate-7 smoke that can fail for a reason, hung mutations named, the
   last ruff rows.
5. `docs/llm-usage.md` row for this prompt.
6. `config/quality_gates.yaml` `lint-docs.report_path` →
   `report-v1.9.4.md`; every timeout touched this release re-checked
   against this run's walls under the file's rules.
7. Gates — all seven verbatim from `AGENTS.md`, in order, final tree,
   nothing else on the box during gate 6, gate 6 and 7 never concurrent;
   exit code and wall each. Gate 7 on the **production** route (env unset).
   Preconditions as `v193-T4.md` (LM Studio at `192.168.0.145:1234`,
   Docker up, `.env` untouched) — blocked, not skipped, if absent.
8. Tag `v1.9.4` annotated, after all seven are green, message naming the
   four tasks. Do not push.
9. After any amend or added commit, re-read every place a count or hash
   is named — the ledger row is pasted byte-identically into the lab
   ledger. Any wait loop uses a self-excluding `pgrep` pattern.

Constraints and return format: as `v193-T4.md`. One prompt → one commit;
no logic changes in this commit — a red gate that needs code stops the
task.
