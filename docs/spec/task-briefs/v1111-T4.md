# Brief v1111-T4 — DOC-01/DOC-02, delegated (second commit)

Spec: `docs/spec/spec-v1.11.1.md:479-532` (REQ-V1111-DOC-01, -02), test
table row `:697-698` (`T-V1111-DOC-01`, `T-V1111-DOC-02`; `T-V1111-DOC-03`
is also asserted here per EC-02's carve-out timing, but its five edits
already landed in T4's first commit, `9d8dc0e` — you only need to
confirm it's green now, not re-implement it). Base tree: `2431034` (tag
`v1.11.0`), current `HEAD` has T0-T3 plus T4's first commit landed.

**This is T4's SECOND commit.** T4's first commit (`9d8dc0e`, prompt
`docs/prompts/261-v1111-t4-doc03.md`, already on disk **untracked** —
do not modify it, just stage and commit it as-is) touched only
`docs/reports/report-v1.11.0.md` and `docs/reports/tg-post-v1.11.0.md`
(DOC-03, artefacts only). **Your commit's allowed path set is exactly
nine paths, nothing else**: `README.md`, `docs/plan.md`, `AGENTS.md`,
`tests/test_v1111_doc.py`, `docs/prompts/261-v1111-t4-doc03.md`
(already on disk, bundle it in), `docs/prompts/262-v1111-t4-doc.md`
(write this — your own prompt file), `docs/llm-usage.md`,
`docs/spec/task-briefs/v1111-T4.md` (this file), and
`docs/reports/report-v1.11.1.md`. Two `docs/llm-usage.md` rows land
here: one for prompt 261 (T4's first commit — write a short row
describing DOC-03's five edits) and one for prompt 262 (this commit).
Disclose the bundling in both the commit body and the report bullet
(EC-03's own requirement — "a commit that bundles an orchestrator edit
into a delegated commit says so in its body and in the bullet").

**Test-first (EC-02)**: write `tests/test_v1111_doc.py` first (three
tests — DOC-01 and DOC-02 red before your README/plan/AGENTS edits,
DOC-03 already green from the first commit, per spec `:71-74`'s
carve-out timing), watch DOC-01/-02 fail for the right reason, then
implement.

**DOC-01**: three README edits. (a) `## Limits` (`README.md:918-952`
±5) gains the `/model` catalogue-cap row after the `rerank candidates`
row, plus one sentence after the table about decimal vs. binary sizes
— exact text at spec `:479-484`. (b) `## Error behaviour` (`:953-1002`
±5) gains three rows — exact text at spec `:485-493` (the `/documents`
empty row, the `/sessions` empty row already partly covered by T2's own
TAB-03 work — check it isn't duplicated —, and the Telegram-400 row).
(c) `## Versioning` (`:1008-1011` ±5): one sentence rewritten, exact
text at spec `:494-497`.

**DOC-02**: (a) `docs/plan.md` gains exactly the seven quoted banner
lines (spec `:508-516`) inserted before its current first line, then
one blank line, nothing else changed — `T-V1111-DOC-02` compares the
remainder byte-for-byte to `git show v1.11.0:docs/plan.md`. (b)
`AGENTS.md`'s `## Secrets` (`:325-328` ±5) gains one paragraph after
the existing one — exact text at spec `:525-529`.

**Commit + gates**: before your own gates, record two pending
`gitleaks-tree` results the orchestrator already ran and is handing you
verbatim: `5587fd1` (T3's commit) exit 0, no leaks; `9d8dc0e` (T4's
first commit) exit 0, no leaks. Add both as rows to the appropriate
`gitleaks-tree` tables in `docs/reports/report-v1.11.1.md` (T3's own
table, and start T4's own table with the `9d8dc0e` row — follow T0's
table shape). Fill in T4's own report section (replace `## T4 — not
reached: ...`), a **single-physical-line** delegation-record bullet for
T4 (note it's `delegated: yes` even though it bundles the first
commit's artefacts-only work — disclose that bundling in the `map vs
actual` cell, the same pattern T4's v1.11.0 precedent used, see
`report-v1.11.0.md`'s own T7 entries for the shape). Run gates 1-4
(`uv sync --locked`, `ruff check .`, `pytest`, `bot.py --selftest`) and
`devtools/checks.py lint-docs`, all green, before committing. Do not
run the standalone `gitleaks-tree` block yourself — the orchestrator
does that after your commit lands.

**Before committing, self-check with `advisor`** — verify every line
range, pass/fail count, and ordering claim against the actual tree
before writing it into the report/usage rows. T2 and T3 both did this
cleanly (no follow-up commit needed); T1 didn't and needed one. Repeat
the clean pattern.

**Constraints**: never open/print/grep `.env`, `data/`, `bot.db`,
`sandbox/`, `exec_audit.jsonl`; no LAN address or secret literal
anywhere; zero new dependencies. Commit message: conventional type,
≤72-char header, body names prompt 262 and discloses the first-commit
bundling, ends with the same Co-Authored-By/Claude-Session trailer as
`git log -3`. `--no-verify` never.

Return a summary: files touched, the three tests' pass/fail, gate
outputs, commit sha, and anything your self-check caught. Never paste
full file contents.
