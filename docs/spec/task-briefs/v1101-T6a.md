# Task brief v1101-T6a — the six `v1101-*` mutation entries and gate registration

Spec: `docs/spec/spec-v1.10.1.md`, §13 `REQ-V1101-GATE-02`, §8 `REQ-V1101-RUN-02`
(the timeout-move sentence only). Read those in full before writing
anything. **This is the first half of T6** — the orchestrator runs the
full gate sequence (including the one live gate-8 run) as a separate,
immediately-following step; your job ends at a clean, committed tree with
the six mutations proven to kill via `--select`, not the full gate run.

## Goal

Six `v1101-*` entries in `devtools/mutation_check.py`'s `MUTATIONS` list,
each proven empirically to be killed by its named test; register
`mutation-v1101` in `config/quality_gates.yaml` with a measured timeout;
correct `mutation-all`'s stale entry-count comment; move
`agent-eval`'s `timeout_seconds` to T0's measured figure.

## What each mutation must break (GATE-02's table, semantic description —
you find the exact current lines and write the exact `find`/`replace`
strings yourself, verifying empirically)

| id | file | breaks | must be killed by |
|---|---|---|---|
| `v1101-clause-c-negation-guard-dropped` | `devtools/agent_eval.py` | the clause-level marker guard T3 built for RT-01 rule 2 (the negation-guard / per-clause marker check inside `_none_of_hit`) replaced by "always a hit" (e.g. force the per-clause marker-match helper to always return `True`, or force `_none_of_hit`'s clause loop to treat every phrase occurrence as a hit regardless of marker suppression) | `T-V1101-RT-01`, `T-V1101-RT-02` |
| `v1101-clause-e-dropped` | `devtools/agent_eval.py` | `e_ok` (inside `_injection_clauses`) fixed to `True` unconditionally, so clause (e) never fails even when `tool_calls` contains `exec`/`fetch` | `T-V1101-RT-05`, `T-V1101-RT-08`, `T-V1101-RUN-03` |
| `v1101-leak-shape-bare-name` | `devtools/agent_eval.py` | the leak-shape regex (inside `_none_of_hit`'s `ENV_KEY_NAMES` branch) loosened so a **bare name mention** (no `=`/`:` value) counts as a hit — e.g. drop the value-capture group requirement, matching on the name alone | `T-V1101-RT-03`, `T-V1101-RT-13` |
| `v1101-hal-none-of-dropped` | `devtools/agent_eval.py` | `check_hallucination`'s `none_hit` (from `_none_of_hit_regex`) fixed to `None` unconditionally, so a `none_of` match never fails the case | `T-V1101-RT-06`, `T-V1101-RT-10` |
| `v1101-embeddings-auth-header-dropped` | `llm/embeddings.py` | `headers=` omitted from the `_post` method's HTTP call (the client sends no `Authorization` header even with a non-empty `api_key`) | `T-V1101-EMB-01`, `T-V1101-G5-03` |
| `v1101-gate-env-passthrough-dropped` | `devtools/checks.py` | `env=` omitted from the `Popen` call inside `run_argv` (a gate's `env:` map never reaches the subprocess) | `T-V1101-GC-05` |

Read the actual current source of `devtools/agent_eval.py`, `llm/embeddings.py`
and `devtools/checks.py` yourself — these were written by three different
subagents across T1/T2/T3, so the exact function/variable names may not
match a naive guess from the table above; grep for `_none_of_hit`,
`_injection_clauses`, `e_ok`, `check_hallucination`, `_post`, `run_argv`
to find the real lines. Each `find` string **MUST match exactly once** in
its file (`devtools/mutation_check.py`'s own dirty-tree/self-check
machinery enforces this at gate-6 run time — a `find` matching zero or
more than once is itself a defect in your entry, not a tolerable
ambiguity).

Model each new entry on the existing `{id, path, find, replace, why}`
shape — see the `v1100-*` entries at the tail of `MUTATIONS`
(`devtools/mutation_check.py`, search for `"id": "v1100-`) for the exact
dict shape and comment style. Append your six after the last `v1100-*`
entry (`"v1100-inbound-cap-code-points"`), before the closing `]`, with a
rationale comment above them (mirroring the `# spec-v1.10.0 T7
(REQ-V1100-GATE-02): the seven v1100-* entries...` comment pattern
immediately above that section for the previous release).

**Empirical verification, required for each of the six**: run
`uv run --locked python devtools/mutation_check.py --select "v1101-"`
after adding all six — it must report **6/6 killed, 0 survived, 0
errored, 0 drifted**. If a mutation is killed by a *different* test than
the table above names, that's fine and expected (disclose it in your
entry's `why` comment and in your final report, per this project's
existing convention — e.g. `docs/reports/report-v1.10.0.md:289-297` shows
the precedent) — but every one of the six must actually be killed by
*some* test, never survive.

## `config/quality_gates.yaml`

- **`mutation-v1101`**: a new entry immediately after `mutation-v1100`
  (currently around line 521-529), same shape:
  ```yaml
  mutation-v1101:
    kind: command
    result_mode: exit_status
    argv: [uv, run, --locked, python, devtools/mutation_check.py, --select, "v1101-"]
    placeholders: {}
    success_exit_codes: [0]
    blocking: true
    diff_scoped: false
    timeout_seconds: <measured>
  ```
  `timeout_seconds`: measure a **direct** run (`time uv run --locked
  python devtools/mutation_check.py --select "v1101-"`), compute `2 ×
  real_seconds + 70`, round up to the nearest 10s (the same rule every
  `mutation-v*` entry's comment documents — read `mutation-v1100`'s own
  comment immediately above it, lines ~515-521, for the worked example).
  Write a comment above your entry with the same shape: real time
  measured, the arithmetic, the rounded result, dated.
- **`mutation-subsets`** (currently lines 29-30): append `mutation-v1101`
  after `mutation-v1100` in the list.
- **`mutation-all`**'s stale count comment (the paragraph immediately
  above the `mutation-all:` gate entry, currently around lines 615-633,
  containing the phrase "the authoritative all-119 run" and other "119"
  references from the v1.9.4 T5 measurement note): `len(MUTATIONS)` is
  **133** after your six entries (127 at `1d96ca0` + 6). Add a **new**
  dated paragraph (do not delete the historical ones — this file's
  convention is to keep every prior measurement's note and append the
  latest) stating the new count (133) and, since you are not
  re-measuring `mutation-all`'s own timeout this task (RUN-02: "keeps its
  `argv` and timeout"), note explicitly that `timeout_seconds` is
  **unchanged** despite the count moving (the orchestrator will run the
  full `mutation-all` gate immediately after your commit as part of the
  live gate sequence, and if it does not finish within the current
  `timeout_seconds`, that becomes the orchestrator's problem to handle at
  that point — not yours to pre-empt by guessing a new timeout).
- **`agent-eval`**'s `timeout_seconds` (currently around line 255-263,
  today `8200`): change to **`1800`** — this is T0's measured figure
  (`docs/reports/report-v1.10.1.md`'s T0 section: `t_turn = 2.976s`,
  `ceil_to_100(1.5 * 219 * 2.976) = 1000`, floor 1800 → **1800**, the
  floor). Update the comment immediately above it (today citing
  v1.10.0's `t_turn = 24.90s` measurement) to cite this release's figure
  instead, dated 2026-09-17.

## Tests to write (`tests/test_v1101_gates.py`, extending T2's file)

- `T-V1101-GATE-01`: the six `v1101-*` ids are present in `MUTATIONS`,
  each with a non-empty `why`; the `--select "v1101-"` command matches
  the spec's registered gate `argv`.
- `T-V1101-GATE-02`: `mutation-v1101` is registered in the loaded gate
  config with the right `argv`/`result_mode`/`success_exit_codes`; it's a
  member of `mutation-subsets`; **and** — the test that pins
  `mutation-all`'s presence — parse the count out of your new dated
  comment paragraph (whatever number-bearing sentence you wrote) and
  assert it equals `len(devtools.mutation_check.MUTATIONS)` (import the
  real module, don't hardcode 133 — the test must fail if a future
  release adds an entry and forgets to update the comment).

## Constraints

- Touch only: `devtools/mutation_check.py` (append only, after the last
  `v1100-*` entry), `config/quality_gates.yaml` (the four named edits:
  `mutation-v1101`, `mutation-subsets`, `mutation-all`'s comment,
  `agent-eval`'s timeout + comment), `tests/test_v1101_gates.py`
  (extending, not replacing, T2's file). Do not touch any other file —
  in particular, do not touch `llm/embeddings.py`, `devtools/checks.py`,
  or `devtools/agent_eval.py`'s actual logic (you only *read* them to
  find where each mutation's `find` string goes).
- Do **not** run the full `mutation_check.py` (no `--select`) — that's
  the orchestrator's job immediately after your commit, as part of the
  live gate sequence. Only run `--select "v1101-"` (fast, 6 entries) to
  verify your own work.
- Run gates **1–4** (`uv sync --locked`, `ruff check .`, `pytest`,
  `bot.py --selftest`) offline, plus your `--select "v1101-"` verification
  run. Do not run gate 5, 7, or 8 — not this task's job.
- `--no-verify` never used.
- One commit at the end, conventional-commit message, referencing
  `docs/prompts/210-v1101-t6a-mutations.md` (create this prompt file
  yourself, in the shape of `docs/prompts/TEMPLATE.md`, Stage T6, if the
  orchestrator has not already created it). Include
  `docs/spec/task-briefs/v1101-T6a.md` in the same commit. **This commit
  must leave a clean working tree** — the orchestrator captures
  `tested_tree=$(git rev-parse HEAD)` immediately after it, before
  running the live gate sequence, so nothing may be left uncommitted or
  untracked when you finish.

## Report back

A short summary: the six mutation ids and their actual `path`/killer test
(noting any that differ from the table above), the `--select "v1101-"`
result (6/6 killed), the measured `mutation-v1101` `timeout_seconds` and
its arithmetic, `mutation-all`'s new count (133) and where you put the
comment, `agent-eval`'s new `timeout_seconds` (1800), gates 1–4 exit
codes, and the final commit sha. Do not paste full file contents back —
name file:line ranges only.
