# Task brief — v1.9.2 T3: version bump, paperwork, authoritative gates, tag

Patch release, no spec file. Same shape as `docs/spec/task-briefs/v191-T2.md`
(read it first — the conventions below are the same, restated here only
where v1.9.2 differs). Tree at handoff: `main` = `b9e5174`, seven commits
ahead of `origin/main`, clean, unpushed.

## What this release is

- **T1** (`6c9a904`, `d25d664`, `c21ffb3`; prompts 164–166): whole-tree
  static analysis — skylos 27 → 0, semgrep no longer scans its own rule
  pack, `B` + `RUF100` in ruff `select`, whole-tree `ruff format`
  (REQ-V15-NG-04 closed with one named exception,
  `devtools/bench_scenarios.py`, REQ-V13-BEN-12), `ruff-format-all` gate in
  pre-commit / pre-push / full.
- **T2** (`92b667c`, `c3a38ea`, `260c7e1`, `b9e5174`; prompts 167–170):
  mutation runner orders test files by relevance (no test excluded; shrink
  guard fails closed; two `v192-` entries), `pytest-xdist` 3.8.0 with
  `-n auto` in `addopts` (runner opts out with `-n 0`), fixture and grace
  costs. Measured: five pre-push mutation subsets 2227 s → 281 s;
  `--select v13-` 34/34 in 210 s; pytest 70 s → ~24 s; duplicates 0 removed
  (time is in ~10 real-tool tests, documented).
- Both phases reviewed in a clean context; findings closed in `c21ffb3` and
  `b9e5174`. Mutation entries 108 → 110, drift 0 throughout.

## What to do

1. **Version.** `pyproject.toml` 1.9.1 → **1.9.2**, `uv.lock` accordingly.
   Repoint `tests/test_v191_version.py` to the frozen `v1.9.1` tag blob
   exactly the way `tests/test_v190_version.py` was repointed for v1.9.0;
   new `tests/test_v192_version.py` red-before / green-after, both recorded.
   REQ-V190-EC-03: repoint, never delete.
2. **Version- and count-bearing lines.** `AGENTS.md:160-161` (test count),
   `:169` (mutation entry count), the README release table (`README.md:838`
   region) and whatever `tests/test_v190_agents.py:139-140` pins. Set them
   to what the final tree collects — measure after every other edit in this
   task, not before (the v1.8.0 `/verify-run` finding). Current tree:
   pytest **1600 collected** (+ the new version test), mutation entries
   **110**.
3. **`docs/reports/report-v1.9.2.md`** — T1/T2 wrote their sections; finish
   it with:
   - a consolidated gates table: the seven gate results with exit codes and
     wall clocks from step 6, **plus** the gate-6 before/after line
     (61m39s at 108 entries on v1.9.1 → this run's wall at 110);
   - the two clean-context reviews summarised (T1: 🔴 stale ledger row /
     commit body, 🟠 NG-14 citation, delta-1 line refs, pre-commit format
     gap; T2: 🟠 shrink guard fail-open, 🟠 sub-suite timeouts; all closed,
     commits named) — the T1/T2 sections may already carry this; then only
     cross-reference, do not duplicate;
   - the **per-task delegation record**, complete: T1 delegated by
     `docs/spec/task-briefs/v192-T1.md`; T1 review = clean-context review;
     T1 review fixes delegated by `v192-T1-review.md`; T2 delegated by
     `v192-T2.md`; T2 review = clean-context review; T2 review fixes by
     `v192-T2-review.md`; T3 by this file. Every executor model named
     (T1/T2 implementer `claude-sonnet-5`, both reviewers `claude-opus-5`);
   - **disclosures, stated as such**: (a) `Co-Authored-By` trailers on the
     sonnet commits read `Claude Sonnet 5` — accurate to the executor,
     non-conforming to the session's named string; (b) gate 6's SIGKILL
     hazard on a gate timeout (mutated file left on disk) is pre-existing
     and listed as a v1.10.0 item; (c) `mutation-v170`/`-v190` timeouts are
     the tightest under the amended rule — confirm against this run's
     per-subset walls if the runner prints them, else against the total;
     (d) the pre-push profile still runs five subsets: the operator decides
     after seeing this run's `mutation-all` wall whether pre-push switches
     to `mutation-all` — write the number and both options, decide nothing;
   - the **open tail carried from v1.9.1**: one rerank call succeeding only
     on attempt 3 — say whether gate 7's log in this run shows it again;
   - the **Ledger row** section, real and complete, v1.9.1's shape
     (`docs/reports/report-v1.9.1.md:328-332`) and `ledger_header` in
     `config/quality_gates.yaml`. Prompts for this release: 164–171. Review
     findings: T1 1 🔴 / 4 🟠 / 5 🟡, T2 0 🔴 / 3 🟠 / 6 🟡 — all fixed.
4. **`docs/reports/tg-post-v1.9.2.md`** — Russian, under 1500 characters,
   executor models named, repo link; `tg-post-v1.9.1.md`'s shape. Lead with
   the numbers people care about: gate 6 wall before/after, pytest wall
   before/after, skylos 27 → 0, 75 files formatted, 0 tests deleted and why.
5. **`docs/llm-usage.md`** — row 81 for prompt 171 (this task).
6. **Gates.** All seven verbatim from `AGENTS.md`, in order, on the final
   tree, nothing else running on the box while gate 6 runs, and **gate 6
   and gate 7 never concurrent**. Record exit code and wall for each. Gate 6
   is the authoritative all-110 run under the new runner — its wall is the
   number the operator's pre-push decision needs. Preconditions verified
   at handoff: LM Studio answers at `192.168.0.145:1234` and `.env` already
   points there (do not read `.env`; `grep -c '^LMSTUDIO_BASE_URL=http://
   192.168.0.145' .env` → 1); Docker daemon up; OpenRouter key present in
   `.env` (gate 7's rerank route). If gate 5 or 7 fails for an environment
   reason, that is **blocked**, not skipped — stop and report. If gate 6
   times out or reports a survivor, stop and report with the id.
7. **Tag.** Only after all seven are green on the final tree: annotated tag
   `v1.9.2` on the release commit, message naming the two phases and the
   gate-6 before/after. Do not push — the operator pushes.
8. **`config/quality_gates.yaml`** `lint-docs.report_path` →
   `docs/reports/report-v1.9.2.md`, and `mutation-all.timeout_seconds` by
   the amended rule from this run's measured wall (2× + 70 s, rounded up to
   10 s), comment updated the way the file does it.

## Constraints

- One prompt → one commit: prompt 171
  (`docs/prompts/171-v192-t3-version-bump.md`), bullet-header frontmatter
  with `Model reason:`; `checks.py lint-docs` is the authority.
- No logic changes. A gate failing for a reason that needs a code change →
  stop and report; a version-bump commit does not carry fixes.
- `.env` never read, printed or committed; `data/`, `evals/rag/corpus`
  never opened. Never `--no-verify`. Do not push.
- Absolute paths in every shell command; scratch under
  `/home/akh/.claude/jobs/45feeee3/tmp/t3/`.

Return a summary only: the seven gates with exit codes and walls, the
gate-6 wall at 110 entries, the tag name and commit, the ledger row verbatim
(it is pasted byte-identically into the lab ledger), and any stop item.
