# Task brief — v1.11.0 T0 (pin-inventory construction)

Spec: `docs/spec/spec-v1.11.0.md`. This brief covers only the delegated part
of T0: constructing the frozen-pin inventory (REQ-V1110-PIN-01,
`spec-v1.11.0.md:1290-1336`). The orchestrator has already run T0's
precondition checks, gates 1–5, and the measurements directly (commands-only
exemption) — you do not need to repeat them.

## Your job

Write `docs/spec/task-briefs/v1110-T0-pin-inventory.md`: a table
`site (file:line) | literal | task that rewrites it | rewrite form`, plus a
**rename mapping** section `old node id → new node id` for every test a later
task renames. Build the table by **grepping the live tree** — do not copy
line numbers from the spec verbatim without confirming them against the
current file content, since the spec cites `295b01f` and a ≤5-line drift is
expected and must be recorded as `cited → actual` where you find it.

Cover **at minimum** these literals/sites (the spec's own starting list,
`spec-v1.11.0.md:1305-1325`) — extend it with anything else you find that a
later task's change would break:

- `NEW_CONVERSATION_REPLY`'s literal (rewritten by T3, SES-03)
- `MODEL_USAGE_REPLY` (rewritten by T4, MOD-03)
- `DELETE_USAGE_REPLY` (rewritten by T2, DOC-02)
- `File too large (over 10 MiB).`, `Document too large (over 500,000
  characters).`, `Indexing timed out (over 300 s). Nothing was saved.`
  (rewritten by T5, ING-01/DOC-03)
- README limits literals `10,485,760` / `500,000` / `300 s` / `500 pages`
  and the error-table strings (`tests/test_v190_agents.py:238-280`,
  `README.md:828-833`, `:863-864`, `:511-517`) — T5 also **adds** new row 10
  and row 17 strings `❌ Interrupted by restart.` and `Indexing is already
  finishing.` to README's error table (T5, VER-02 will pin these)
- every `/stats` line-shape assertion (`tests/test_observability.py`,
  `tests/test_v160_observability.py`, `tests/test_pricing.py`) — rewritten
  by T2 (STA-01); note the spec says 43 pins total rewritten by T2
- `Your documents (2):` in `tests/test_v190_commands.py:592` (rewritten by
  T2, DOC-01)
- the `allowed_updates` body in `tests/test_telegram.py:272` (rewritten by
  T4, CBQ-01)
- the typing-ceiling test in `tests/test_v190_commands.py:503` (rewritten by
  T5, ING-05)
- `/model` reply strings in `tests/test_v1_guardrails.py` and
  `tests/test_failover.py` — **unchanged**, list them to prove they were
  checked, not rewritten
- the README commands-order pin (`tests/test_v190_agents.py:226-235`,
  rewritten by T6, EXT-01)
- `AGENTS.md`'s `2311` and `144 entries` lines (`tests/test_v190_agents.py:134-152`,
  rewritten in place under its existing name by T8, VER-01 — **not** part of
  the rename mapping)
- `config/quality_gates.yaml:790`'s `report_path` and its two pins
  (`tests/test_v170_bench.py:327`, `tests/test_v1104_gates.py:209`) —
  rewritten by T6 (VER-03) **and these two test node ids are renamed** to
  `…_v1110_rpt_01…` forms — put the exact `old → new` names you find in the
  rename mapping
- the gate-matrix reader (`tests/test_v15_standards.py:1824`, rewritten by
  T6, REV-02)
- `spec-v1.md:135`'s "exactly five commands" — docs only, note it, do not
  edit it

Also record, by command (do not re-run gates — just cite the orchestrator's
already-measured values so the inventory is self-contained):
`len(MUTATIONS) == 144`, the v190 `find` string
`    if isinstance(file_size, int) and file_size > DOCUMENT_MAX_BYTES:` in
`bot.py` occurs exactly once, the baseline node-id list is
`docs/spec/task-briefs/v1110-T0-nodeids.txt` (2311 lines), the `bot_state`
override count is `0`.

## Rules

- **Read-only.** Do not edit any file except writing the new
  `v1110-T0-pin-inventory.md` artefact. Do not run `ruff format`, do not run
  `--selftest-live`, `mutation_check.py`, `rag_eval.py`, `agent_eval.py`, or
  `bench.py`. Gates 1–4 already ran; you do not need to re-run them.
- **Rewrite form is presence/contiguity/order** (`README.md:918`'s v1.10.4
  rule) — never a frozen-equality/tail-equality assertion; note this in the
  table's "rewrite form" column per row where relevant.
- A pin you discover that isn't in the starting list above is fine — add it,
  it's a disclosed amendment per PIN-01, not an error.
- Return a summary: how many pin rows, how many rename-mapping entries, any
  drift >5 lines from a cited spec location (flag those explicitly, do not
  silently correct them), and confirmation the artefact file was written.
  Do not paste the full table back in your summary — the orchestrator will
  read the file directly.
