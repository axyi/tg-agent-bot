# Task brief — v1.8.0 T7 (docs + config, no version bump)

Spec: `docs/spec/spec-v1.8.0.md` REQ-V180-AGT-03 (T7's half only), REQ-V180-RPT-01.
No version bump here — that's T9, strictly after this.

## Facts, measured on the current tree (commit `a786dfc`, T6 landed)

- `pytest --collect-only -q` total: **1217** (was 1133 at T0).
- `devtools/mutation_check.py` `MUTATIONS` entries: **98** (92 + this
  release's 6 `v180-*`).
- `--select` example should read `v180-` (the newest prefix), matching the
  style of the existing `v170-` example it replaces/joins.

## What to write

1. **`AGENTS.md:146-159`** (current line numbers — re-check with `grep -n
   "1133 tests\|92 entries" AGENTS.md` first in case they've shifted): the
   two count-bearing sentences in the `## Gates` section —
   - `"...is 1133 tests as of spec-v1.7.0 T8)"` → update to the new count
     (1217) and cite spec-v1.8.0 T8 instead of v1.7.0 T8 (T8 is where the
     full-suite gate run against the final tree happens — that's what
     "as of spec-vX.Y T8" has meant in every prior release's version of
     this sentence; if you judge a different task id fits better per this
     release's own §12 table, use that instead, briefly note why).
   - `"...92 entries as of spec-v1.7.0 T9 (up from 83 at spec-v1.6.0's
     close; T9 added nine v170-* entries..."` → update to 98 entries,
     "up from 92 at spec-v1.7.0's close", name this release's own task
     that added them (T6) and what it added (six `v180-*` entries covering
     the chat-status signal, the typing-indicator ceiling, and transcript
     redaction/truncation/budget), pointing at `docs/reports/report-v1.8.0.md`
     instead of `report-v1.7.0.md`.
   - `"--select v170-"` example → `"--select v180-"`.
   - **Touch nothing else in this file** — no reflow, no style edit, no
     other paragraph (AGT-03: count-bearing lines only, written once, here).

2. **`README.md`'s `## Dashboard` section** (currently `:204-228`, re-check
   line numbers first): it currently says (`:216-223`) that the dashboard
   serves `/`, `/traces`, `/tools` and that "no route ever serves message
   or tool content, regardless of `OBS_CAPTURE_CONTENT` ... only counters,
   timings and truncated non-content attributes." **This sentence is now
   false** — `/conversations/<id>` (and its `/api/` mirror) serves
   redacted, truncated message content by design (REQ-V180-CONV-01…-05,
   SEC-01/-02). Fix it: add `/conversations` (the conversation list) and
   `/conversations/<id>` (the transcript) to the route list, and correct
   the "no route ever serves content" claim to instead describe the real
   invariant — trace/span content stays off by default
   (`OBS_CAPTURE_CONTENT`, unchanged, NG-10) while the conversation
   transcript is a **different, always-available source**
   (`messages.content`, redacted and length-capped) — this is exactly the
   distinction REQ-V180-SEC-03 draws in the spec; word it accurately, do
   not just delete the claim. Keep the section's existing tone/structure;
   this is a correction, not a rewrite.

3. **`docs/plan.md`**: read it. It is a benchmark/performance-candidates
   document (NG-03: this release does no benchmark work). Judge whether
   anything in it is now wrong because of v1.8.0 — most likely **nothing
   is**, since this release doesn't touch tokens, prompts, routing or any
   candidate it lists. If so, leave it unchanged and say why in your
   report (mirroring REQ-V180-AGT-04's "unchanged is a legitimate outcome"
   treatment, even though this file isn't literally in that requirement's
   list). Only edit it if you find a concrete claim this release falsifies.

4. **`config/quality_gates.yaml:330-340`-ish** (re-check line numbers,
   T6 may have shifted them slightly): `report_path:
   docs/reports/report-v1.7.0.md` under the `lint-docs` gate → repoint to
   `docs/reports/report-v1.8.0.md`. This is the **only** change in this
   file for this task — `lint-docs` must go green against the T0 report
   skeleton immediately after.

## Constraints

- No version bump anywhere (`pyproject.toml` stays `1.7.0` — that's T9's).
- No source code changes — this task touches only `AGENTS.md`, `README.md`,
  possibly `docs/plan.md`, and `config/quality_gates.yaml`'s one line.
- No file outside this repository is read or written (EC-01).
- Conventional commit `docs:`. Header ≤ 72 Unicode characters, no trailing
  period. Body references `(prompt: docs/prompts/135-v180-t7-docs-config.md)`.
  End the commit message with:
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01FnGubtn2ec8m9fan73tGzZ
  ```
- Write your own prompt file first at
  `docs/prompts/135-v180-t7-docs-config.md` using
  `docs/prompts/TEMPLATE.md`'s exact shape, committed together with your
  changes.
- Run `uv run --locked python devtools/checks.py lint-docs` and confirm it
  exits 0 against the repointed `report_path` before returning. Run `uv
  run --locked ruff check .` too (should be a no-op given the files
  touched, but confirm). Do not run the full six-gate sequence or pytest —
  this task changes no code or test file, so pytest's count is unaffected;
  a quick `uv run --locked pytest --collect-only -q` to confirm the count
  still reads 1217 is a reasonable sanity check if you want one, but is
  not required.

## Acceptance

`checks.py lint-docs` exits 0. The three count-bearing facts (1217, 98,
`v180-`) appear correctly in `AGENTS.md`. `README.md`'s Dashboard section
accurately describes all five live pages/routes and no longer claims no
route ever serves content. `docs/plan.md` either correctly unchanged
(with your stated reason) or correctly corrected.

## Report back

A summary only: exact diffs by `file:line`, the commit sha, `lint-docs`
exit code, and your one-line reason for `docs/plan.md`'s disposition. Do
not paste full file contents back.
