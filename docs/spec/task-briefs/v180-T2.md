# Task brief — v1.8.0 T2 (§4 part 1: delete-on-success, structured outcome)

Full spec: `docs/spec/spec-v1.8.0.md` §4 (REQ-V180-CHAT-01…-04, -06, -07, -08
items 1/3/4/5 only — **not** item 2, see below), §1, §2, §11.

Verified against the current tree (unchanged since T1, which touched only
`AGENTS.md`/`skills/*`/`CLAUDE.md`/`tests/test_v180_agents.py`) — these line
numbers are accurate right now:

- `bot.py:62-63` — `STATUS_WORKING`/`STATUS_DONE` constants.
- `bot.py:93-176` — `TelegramClient`: `call` (`:105`), `_call_with_retry`
  (`:142-157`, `-> dict`), `edit_message_text` (`:171-175`).
- `bot.py:241-294` — `_StatusMessage`: `finish()` (`:264-266`, no args
  today), `_edit()` (`:280-284`), `_fail()` (`:286-288`, sets `_disabled`
  and logs once redacted — **reuse this discipline**, do not reinvent it).
- `bot.py:689-708` — the production call site inside `process_update`
  (function starts `bot.py:593`): today `reply = agent.run_agent(...)` then
  `status.finish()` then `_send(tg, chat_id, split_message(reply))`.
- `bot.py:964-971` — `_send(tg, chat_id, parts) -> None`, catches
  `TelegramError`, logs redacted, returns (does not raise). It has ~15 call
  sites; only the production one at `:708` needs its return value read.
- `bot.py:1085-1103` — `_SelftestTelegram`: `sent`/`status`/`edits` lists,
  `send_message`/`edit_message_text`.
- `bot.py:1211` — the assertion in `_selftest_failure`
  (`bot.py:1167-1219`): today `len(edits) != 2 or not edits[0].startswith("⚙️ exec: ") or edits[1] != STATUS_DONE`.
- `tests/test_v12_patch.py:141-145` — `_FakeSelftestTg`, fabricates
  `self.edits = [(424242, 1, "⚙️ exec: uname…"), (424242, 1, bot.STATUS_DONE)]`.
- `agent.py:226-258` — `run_agent(...)  -> str`, thin wrapper opening the
  root span and delegating to `_run_agent_turn`.
- `agent.py:261-297` — `_run_agent_turn(...) -> str`; its inner `finish(text)
  -> str` closure at `:280-297` does the redact + one-transaction
  root-span-finish + `add_assistant_message` dance — **byte-unchanged
  except gaining the two new keyword args and building an `AgentOutcome`
  where it returns `text` today.**
- Fallback return sites, verified exact: `:328` `finish(FALLBACK_INTERRUPTED)`
  → kind `"interrupted"`; `:398` `finish(FALLBACK_LLM_ERROR.format(...))` →
  `"llm_error"`; `:403` `finish(_with_truncation_notice(response))` → answer
  path, `failed=False`, `kind=None`; `:405` `finish(FALLBACK_NO_ANSWER)` →
  `"no_answer"`; `:412` `finish(FALLBACK_EMPTY)` → `"empty"`; `:416-418`
  `finish(_with_truncation_notice(response) if has_content else
  FALLBACK_NO_ANSWER)` → same `has_content` condition decides `None` vs.
  `"no_answer"`; `:445` `finish(FALLBACK_NO_ANSWER)` (the defensive
  round-limit fallback) → `"no_answer"` too.

## What to write (test-first: watch each test fail for the right reason before implementing)

1. **`TelegramClient.delete_message`** (REQ-V180-CHAT-01): add next to
   `edit_message_text`, exactly as the spec's code block gives it —
   `-> bool`, via `self._call_with_retry("deleteMessage", ...)`. Widen
   `_call_with_retry`'s return annotation to `-> dict | bool` in the same
   commit; no other annotation moves, no other method changes.

2. **`_StatusMessage.finish(self, *, ok: bool) -> None`** (REQ-V180-CHAT-02,
   -03) replacing today's no-arg `finish()`:
   - `ok=True`: when a message exists and status work is not disabled, call
     `delete_message` inside the same try/except discipline `_edit` uses
     (reuse `_fail` on any exception). Set `self._message_id = None`
     **only** when the unwrapped result is exactly `True` (`is True`, not
     truthy) — `False`, a `dict`, or `None` is a **failed delete**: message
     stays, `_message_id` stays, one redacted `log.warning`, nothing
     retried. Do not disable the whole `_StatusMessage` on a failed
     delete unless you decide `_fail`'s existing semantics already cover
     it correctly — check `T-V180-CHAT-02`'s exact expectations once you've
     written it (red-before).
   - `ok=False`: **attempt** `self._edit(STATUS_FAILED)` (new constant,
     `bot.py` beside `STATUS_WORKING`, under `STATUS_MAX_CHARS = 64`,
     value `"⚠️ failed"`). The message is never deleted on this path,
     even if the edit itself fails — the stale previous text is an
     accepted outcome, not a retry loop.
   - **Delete `STATUS_DONE` from `bot.py` entirely** (its three other
     occurrences are the two amendment sites below and its own
     definition — after this task none remain).

3. **`agent.py`: `AgentOutcome` + `run_agent_outcome`** (REQ-V180-CHAT-04):
   - `from dataclasses import dataclass`; a frozen `AgentOutcome(reply: str,
     failed: bool, kind: str | None)` — the four `kind` values are the
     closed vocabulary `"empty"` / `"no_answer"` / `"llm_error"` /
     `"interrupted"` / `None`.
   - `_run_agent_turn`'s inner `finish` gains keyword-only `failed: bool =
     False, kind: str | None = None` and returns an `AgentOutcome` instead
     of `str` — same redaction, same one-transaction discipline, byte
     identical otherwise.
   - Every fallback call site above passes its `failed=True, kind="..."`
     per the mapping verified above; the answer sites (`:403`, and the
     `has_content` branch of `:416-418`) pass nothing extra (`failed=False`
     is the default).
   - New public function `run_agent_outcome(...) -> AgentOutcome` takes
     over today's `run_agent` body (the root span + delegation to
     `_run_agent_turn`) verbatim, same signature.
   - `run_agent` becomes a **one-line wrapper**: `return
     run_agent_outcome(...).reply`. Its signature, name and `-> str` are
     **byte-unchanged** — do not touch any of its ~25 existing test call
     sites across eleven test files. `no branch, no round cap, no
     fallback text, no control flow moves` (NG-06) — you are relocating
     the return type at existing return statements, nothing else.

4. **`bot.py`'s call site** (`:689-708`), REQ-V180-CHAT-08 — **but only
   steps 1, 3, 4, 5.** Step 2 ("stop and join the typing indicator") does
   **not** exist yet — `_TypingIndicator` is T3's. Do not write a typing
   indicator, do not add a no-op placeholder for it; T3 inserts step 2 by
   editing this same call site again. Write:
   1. obtain the `AgentOutcome` from `agent.run_agent_outcome(...)` (same
      arguments `run_agent` took);
   2. *(T3's step — skip)*;
   3. send the reply: `_send(tg, chat_id, split_message(outcome.reply))`,
      which gains an additive `-> bool` (`True` when every part went out,
      `False` when it stopped at a failed part — `_send`'s existing
      `try/except TelegramError: log; return` becomes `try/except
      TelegramError: log; return False`, falling through to `return True`
      after the loop; its other ~14 call sites are untouched since Python
      allows discarding a return value);
   4. only when the send returned `True` **and** `outcome.failed` is
      false: `status.finish(ok=True)`;
   5. otherwise: `status.finish(ok=False)`.

   **The exception arm**: wrap the `run_agent_outcome` call so that an
   exception escaping it (e.g. `_run_agent_turn`'s inner `finish`'s
   transactional write can raise) calls `status.finish(ok=False)` and
   **re-raises** — this is what `T-V180-CHAT-09` drives. Structure this
   however reads cleanest (a `try/except Exception: status.finish(ok=False);
   raise` around just the `run_agent_outcome` call is one reasonable shape;
   you decide the exact code shape, the tests decide correctness).

5. **Amend, exactly as scoped — no other test may be touched**:
   - `bot.py:1085-1103` (`_SelftestTelegram`): add `deleted: list[tuple[int,
     int]] = []` and a `delete_message(self, chat_id, message_id) -> bool`
     that appends `(chat_id, message_id)` and returns `True`.
   - `bot.py:1211`: the assertion becomes — exactly one edit starting
     `"⚙️ exec: "` (no `STATUS_DONE` edit, since the constant no longer
     exists), plus exactly one recorded deletion of that message id
     (message id `1`, per `_SelftestTelegram.send_message`'s
     `{"message_id": 1}` for `STATUS_WORKING`).
   - `tests/test_v12_patch.py:145` (`_FakeSelftestTg`): `self.edits` drops
     its `bot.STATUS_DONE` entry, leaving exactly
     `[(424242, 1, "⚙️ exec: uname…")]`; the fake gains
     `self.deleted: list[tuple[int, int]] = [(424242, 1)]` — same
     attribute name, type and shape as `_SelftestTelegram`'s (it feeds
     `bot._selftest_failure` and must satisfy the amended assertion
     byte-for-byte).
   - **No other test file may change.** If any other test goes red, that is
     a defect in your change — stop and reconsider, never edit that test.
     (Two doubles already define an unused `delete_message` stub —
     `tests/test_pricing.py:128`, `tests/test_observability.py:205` — they
     need no amendment.)

6. **New tests** in `tests/test_v180_chat.py` (create it): `T-V180-CHAT-01`
   through `-04`, `-08`, `-09`, `-10` (per the spec's §8 table — read it for
   the exact assertions each id names). `T-V180-CHAT-10` drives the
   **production path** — `process_update` with fakes, twice (a successful
   outcome and a structurally-failed one) — and is what actually catches an
   inverted `ok` at the call site; `-02`/`-03` drive `_StatusMessage.finish`
   directly and cannot see a call-site inversion.

## Constraints

- `run_agent`'s signature, name, and `-> str` return are untouched; its
  existing test call sites (eleven files) are untouched.
- No branch, round cap, fallback text or control-flow change in `agent.py`
  beyond relocating the return values into `AgentOutcome` (NG-06).
- No typing indicator, no `_TypingIndicator`, no `TYPING_*` constant — all
  T3.
- No file outside this repository is read or written (EC-01).
- Conventional commit, most likely `feat:` (new client method, new outcome
  type, behaviour change to the status message and call site) — your
  judgement, staying inside `AGENTS.md`'s list. Header ≤ 72 Unicode
  characters, no trailing period. Body references
  `(prompt: docs/prompts/129-v180-t2-chat-outcome.md)`. End the commit
  message with:
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01FnGubtn2ec8m9fan73tGzZ
  ```
- Write your own prompt file first at
  `docs/prompts/129-v180-t2-chat-outcome.md` using `docs/prompts/TEMPLATE.md`'s
  exact shape, committed together with your changes (one prompt → one
  commit).
- Do not run the full six-gate sequence (gate 5 is live, gate 6 is slow —
  orchestrator's job at T8/T10). Do run `uv run --locked ruff check .` and
  `uv run --locked pytest` (the **whole** suite — this task touches a
  widely-imported module, `agent.py`, so a targeted run is not enough) as
  your own smoke check before returning. All ~1133+ tests must stay green
  except the two amended assertions, which must be green under their new
  shape.

## Acceptance

Every new `T-V180-CHAT-*` id in this task's list is red before its
corresponding code lands and green after. `--selftest`-equivalent logic
(the amended `_selftest_failure` assertion) is exercised by the amended
test, not by actually running `bot.py --selftest` yourself (that gate is
T8/T10's). Full `uv run --locked pytest` green, `ruff check .` clean.

## Report back

A summary only: what changed (`file:line` ranges), the commit sha, full
`pytest` result (pass count), and confirmation that only the two scoped
sites in `tests/test_v12_patch.py`/`bot.py` were amended and no other test
file changed. Do not paste full file contents back.
