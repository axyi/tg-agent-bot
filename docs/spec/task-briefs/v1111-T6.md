# Brief v1111-T6 — VER-01…04, delegated (bump commit only)

Spec: `docs/spec/spec-v1.11.1.md:811-853` (REQ-V1111-VER-01), test table
rows `:702-705` (`T-V1111-VER-01…04`), §10 T6 row (`:1024`), §10.1 T6
row (`:1040`). Base tree: `2431034` (tag `v1.11.0`), current `HEAD` has
T0-T5 landed (`03d351c`). Read the cited spec ranges yourself — this
brief points, it does not copy.

**This is T6's bump commit only** — the first of T6's two commits. The
second (evidence-only, four fixed paths) is the orchestrator's own
work, landed after this commit's gates/checks all pass; do not write
`docs/reports/report-v1.11.1.md`'s T6 section, `tg-post-v1.11.1.md`, or
touch the evidence commit's path set. Your own prompt file
(`docs/prompts/267-v1111-t6-version-bump.md`) is already on disk,
written by the orchestrator — read it, commit it as-is alongside your
own changes, do not edit it.

**Test-first (EC-02)**: write `tests/test_v1111_ver.py` first — four
functions, exact names/assertions at spec `:702-705`
(`test_t_v1111_ver_01_live_version_is_1_11_1`,
`test_t_v1111_ver_02_dependency_diff_version_only`,
`test_t_v1111_ver_03_agents_md_and_release_row`,
`test_t_v1111_ver_04_report_path_and_own_report_lint`) — and confirm
VER-01/-03 fail for the right reason against the pre-bump tree (VER-02
and VER-04 are structural, EC-02's carve-out, may be green on first
execution). Then implement.

**The bump**: `pyproject.toml:3` `1.11.0` → `1.11.1`. `uv lock`
regenerates `uv.lock` — the diff must be confined to the project's own
`version = "..."` line, nothing else (verify with `git diff v1.11.0 --
pyproject.toml uv.lock` and eyeball every hunk before committing;
`devtools/agent_eval.py:1279`'s `dependency_diff_is_version_only`
function is what actually judges this later).

**The fourteen T6 pin sites**: `docs/spec/task-briefs/v1111-T0-pin-inventory.md`
already catalogues all fourteen (the eleven the spec names at
`:817-828` plus three found by extension:
`tests/test_v1101_gates.py:259`, `tests/test_v1103_gates.py:61`,
`tests/test_v1104_version.py:108`) — read that file, rewrite every one
in place under its existing name, none renamed or deleted. Two of the
eleven need special care per the spec's own text: `tests/test_v1110_ver.py:75-78`
repoints exactly as `REQ-V1110-VER-01` repointed `test_v1104_version.py`
(only the live read becomes `git show v1.11.0:pyproject.toml` ==
`"1.11.0"`, every other assertion stays); `tests/test_v1110_ver.py:57`,
`:106-112` get T6's measured count (as of spec-v1.11.1 T6).

**AGENTS.md**: `:161-162`, `:172` → this task's measured test count and
`152 entries as of spec-v1.11.1 T6`; `:95` → `v1111-T<N>.md`; the NG-11
sentence — `AGENTS.md:274-282`'s benchmark-waiver paragraph gains
exactly one sentence, verbatim: "v1.11.1 changes nothing token-bearing
either; the rule does not fire."

**`config/quality_gates.yaml:797`**: `report_path` →
`docs/reports/report-v1.11.1.md` (this repoints `lint-docs` to check
this release's own report from this commit on — expect `lint-docs` to
now actually validate `report-v1.11.1.md`'s delegation-record bullets
and ledger row, which have already been kept in that grammar since T0;
if it fails, that is a real gap to fix, not a false alarm).

**README's release table** (`:1044-1047` ±5): the `v1.11.0` row loses
its `; this release` clause; a new `v1.11.1` row is added after it with
exactly the text at spec `:837` (fill in `<effective judge>` per spec
`:839-840` — `anthropic/claude-sonnet-5 (another vendor)` on the
default route). Do **not** touch the `/sessions` error-behaviour row —
T5's review waived its wording gap, out of scope for this task.

**Constraints**: never open/print/grep `.env`, `data/`, `bot.db`,
`sandbox/`, `exec_audit.jsonl`; no LAN address or secret literal
anywhere; zero new dependencies beyond the version bump itself. Commit
message: conventional type (`chore:`), ≤72-char header, body names
`docs/prompts/267-v1111-t6-version-bump.md`, ends with the same
Co-Authored-By/Claude-Session trailer as `git log -3`. `--no-verify`
never.

**Gates before committing**: run gates 1-4 (`uv sync --locked`, `ruff
check .`, `pytest`, `bot.py --selftest`) and confirm the collection
count is exactly **2411** (2407 + your 4 new VER functions = floor 2384
+ 27). Do not run gate 5, 6, 7 or 8 — the orchestrator runs the full
fresh gate 1-6 sequence, `replay`, and the identity check after your
commit lands, separately. Do not run `devtools/checks.py lint-docs`
yourself if it fails due to the `report_path` repoint you just made —
report the failure output to the orchestrator instead of trying to fix
`report-v1.11.1.md` yourself (that file's ownership is the
orchestrator's for T6).

Return a summary: the exact `pyproject.toml`/`uv.lock` diff shape, all
fourteen pin sites confirmed rewritten (list them), the four new tests'
pass/fail with red-before/green-after evidence, the collection count,
`lint-docs`'s result (pass or the failure output, verbatim), and the
commit sha. Never paste full file contents.
