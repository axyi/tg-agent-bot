# Task brief v1103-T1 — the `exec` guard (REQ-V1103-EXEC-01, EXEC-02)

Repo: `/home/akh/aihome/coders-su/projects/tg-agent-bot`. Spec:
`docs/spec/spec-v1.10.3.md` §4 (lines 309-383), §7 rows 1-2 (lines
616-619), §8 SEC-01 (lines 637-670). Report:
`docs/reports/report-v1.10.3.md` (already has T0's section; append a
`## T1 — the exec guard` section in your commit, replacing the `## T1 —
not reached: T1` placeholder).

## Goal

Add three deny rules inside `_validate_exec_arguments` (`tools.py:1568-
1584`) that refuse the three named environment-inspection shapes before
any runner is called. Write `tests/test_v1103_exec.py` first, watch it
fail for the right reason, then implement.

## Exact change — `tools.py`

Two new module constants, placed exactly where named:

- Beside `MAX_ARGV_ELEMENTS` (currently `tools.py:47`):
  ```python
  EXEC_DENY_PROGRAMS = frozenset({"env", "printenv"})
  ```
- Beside `MAX_ARGV_ELEMENT_CHARS` (currently `tools.py:48`), the pinned
  refusal text as a module constant (name it something like
  `EXEC_ENV_REFUSAL_TEXT`, your choice, but the value MUST be exactly):
  ```python
  "exec refused: environment inspection is not available"
  ```

A compiled regex for procfs (module level, near the two constants above
or near the other imports — `os` and `re` are already imported at
`tools.py:16-17`, do not add new imports):
```python
_PROCFS_ENVIRON_RE = re.compile(r"^/proc/(?:self|\d+)/environ$")
```

Inside `_validate_exec_arguments` (`tools.py:1568-1584`), insert three
rules **after** the `argv[0].strip()` check (currently lines 1582-1583:
`if not argv[0].strip(): return {"error": "argv[0] must be a program
name"}`) and **before** `return None` (currently line 1584). The three
rules run over the already-validated `argv`, first hit wins, each
returning exactly:
```python
{"error": "exec refused: environment inspection is not available"}
```
(use your constant, not a re-typed literal, so there is exactly one
string source of truth). Order and predicates, exact:

1. **program name**: `os.path.basename(argv[0]) in EXEC_DENY_PROGRAMS`
   — basename equality, never a prefix or substring check.
2. **env file**: any element `e` in `argv` where
   `os.path.basename(e) == ".env"` or `os.path.basename(e).startswith(".env.")`.
3. **procfs**: any element `e` in `argv` where
   `_PROCFS_ENVIRON_RE.fullmatch(e)` (full match, not `search`).

Nothing else in `_validate_exec_arguments`, `_run_exec` (`:1486-1523`,
already routes any non-`None` return from `_validate_exec_arguments`
through the existing refused-record path — no change needed there) or
the `exec` tool description (`tools.py:1289-1291`) changes. **Do not
edit the tool description under any circumstance** — it is byte-equal
to `636a281` by requirement (NG-14): the serialized catalog
(`len(json.dumps(tools.tool_specs()))`) must stay **1798** chars, cap
1800.

## Rule semantics — must refuse

`["printenv"]`, `["env"]`, `["/usr/bin/env"]`, `["./printenv", "-0"]` →
rule 1. `["cat", ".env"]`, `["cat", "./.env"]`, `["ls", "-la",
"/app/.env"]`, `["head", ".env.local"]`, `["cat", "/app/.env.production"]`
→ rule 2. `["cat", "/proc/self/environ"]`, `["cat", "/proc/1/environ"]`,
`["xxd", "/proc/12345/environ"]` → rule 3.

## Must NOT refuse (near-misses — run normally, through the recording runner, argv unchanged)

`["ls", "-la", ".envs"]`, `["cat", "env.txt"]`, `["printenv2"]`,
`["envsubst"]`, `["cat", "/proc/cpuinfo"]`, `["cat", "/proc/self/status"]`,
`["cat", "dotenv"]`, `["cat", "environment.yml"]`, `["cat",
"/proc/self/environ/"]` (trailing slash breaks the full-match regex).

## Must NOT refuse — documented bypass shapes (scope boundary, NG-03/NG-04 — do NOT try to catch these, do NOT expand the deny rules to catch them)

`["busybox", "printenv"]`, `["sh", "-c", "printenv"]`, `["python3", "-c",
"import os;print(os.environ)"]`, `["cat", "/proc/self/../1/environ"]`,
`["cat", "/x/.env/y"]`, `["ls", "/app/.env/"]` (trailing-slash basename
is empty), a Cyrillic look-alike `["cat", ".еnv"]` (U+0435, not U+0065),
a symlink alias `["cat", "/workspace/env-link"]`. The validator performs
**no** filesystem access, no symlink resolution, no shell tokenising, no
Unicode folding, no path normalisation — these all pass through
unrefused, on purpose. Do not add any of that logic.

## Precedence

Shape checks (presence, list, length, `str`, NUL, per-element length,
non-blank `argv[0]`) still run **first** and **win** if they also match:
`["printenv", "\x00"]` → the NUL-byte shape message, never the guard's.
The guard rules only see `argv` that already passed every shape check.

## Tests — `tests/test_v1103_exec.py` (test-first)

Write these before touching `tools.py`, confirm they fail for the right
reason (guard not yet refusing / `EXEC_DENY_PROGRAMS` not yet defined),
then implement:

- `T-V1103-EXEC-01`: rule 1's four refuse-shapes above → exact refusal
  dict; the runner spy never called; `tools.EXEC_DENY_PROGRAMS ==
  frozenset({"env", "printenv"})`.
- `T-V1103-EXEC-02`: rule 2's five refuse-shapes → the refusal; runner
  never called.
- `T-V1103-EXEC-03`: rule 3's three refuse-shapes → the refusal.
- `T-V1103-EXEC-04`: the nine near-miss shapes run (recording runner
  called once each, argv unchanged) AND the eight documented bypass
  shapes run (recording runner called once each, argv unchanged) — see
  `tests/test_exec.py` for the existing recording-runner fixture
  pattern.
- `T-V1103-EXEC-05`: through `execute_tool` with an `audit` list — a
  refused `["printenv"]` records exactly `{"tool": "exec", "argv":
  ["printenv"], "outcome": "refused", "error": <text>}` (four keys, no
  `duration_ms`); `["cat", "/app/.env"]` preserves both argv elements in
  the record; the tool envelope parses to `{"error": <text>}`. Also
  prove SEC-01's redaction: after `config.register_secret
  ("VALUE-abcdefgh12")` (check `config.py` for the exact registration
  function name/signature — it's used elsewhere in the test suite, e.g.
  `tests/test_v1_guardrails.py` and `tests/test_v12_patch.py`), a
  refused `["printenv", "VALUE-abcdefgh12"]` call's audit record shows
  `argv == ["printenv", "***REDACTED***"]`
  (`config.REDACTION`, `config.py:23`) — the registered value string
  must not appear anywhere in the serialised record.
- `T-V1103-EXEC-06`: precedence — `["printenv", "\x00"]` → the NUL text;
  `["env"] + ["x"] * 32` → the element-count text; `["  "]` → `argv[0]
  must be a program name`; a joined-string `argv` (not a list) → the
  existing list-shape text (see `tests/test_exec.py:137`'s family) —
  refused before the guard ever runs.
- `T-V1103-EXEC-07`: `run_agent_outcome` with a scripted client calling
  `exec` with argv `["printenv"]` under INJ-04's turn (see
  `devtools/agent_eval.py:1113-1120` `_refusing_runner`, `:1322-1381`
  `_one_turn`), `runner=_refusing_runner`, `record_tool_calls=True` →
  `tool_calls == ["exec"]`, the log entry `'exec("printenv")'`,
  `check_injection` still fails clause (e) even on an otherwise-positive
  reply (the guard changes what the model receives, never what the gate
  scores — GATE-01 owns the bad-run check, not this guard); the runner
  spy is never entered; no `subprocess.Popen` spy called.
- `T-V1103-EXEC-08`: `tools.tool_specs()`'s `exec` entry description
  equals the `636a281` blob's description byte-for-byte (`git show
  636a281:tools.py` slice `:1289-1291`, or diff against the current
  working tree before your edit — you are not touching that
  description, so this should trivially hold; write the test to *prove*
  it, not merely assume it); `len(json.dumps(tools.tool_specs())) <=
  1800`.
- ERR-01 rows 1-2 (offline, same file, may reuse EXEC-01/06 cases):
  row 1 — a guard hit produces the pinned text, refused audit record,
  no runner call; row 2 — a shape defect + a guard-shaped argv in the
  same call ⇒ the shape text wins (already covered by EXEC-06's NUL
  case, cross-reference it).
- `T-V1103-SEC-01`: covered by EXEC-05 above (the redaction proof) — add
  it as a separate named test if that reads more clearly, your call.

## Non-goals — do not do any of this (NG-03, NG-04, NG-14)

No shell parser, no tokenising, no allow-list, no `sh -c` detection, no
multicall-binary detection, no path normalisation, no symlink
resolution, no Unicode folding/confusable detection. No change to
`_run_exec`, `execute_tool`, `build_docker_argv`, or the exec tool
description. Do not widen scope to "actually" block the documented
bypass shapes — they are explicitly out of scope, proven by the
EXEC-04 test that they still run.

## Acceptance

- `uv run --locked ruff check .` clean.
- `uv run --locked pytest tests/test_v1103_exec.py tests/test_exec.py
  tests/test_v1100_toolcall.py -q` — all green; `tests/test_exec.py` and
  `tests/test_v1100_toolcall.py` pass **unamended** (you must not need
  to touch either file for this task).
- Full `uv run --locked pytest -q` green, count increased by however
  many new tests you added (no pre-existing test deleted or renamed).
- Gates 1-4 (`uv sync --locked`, `ruff check .`, `pytest`, `bot.py
  --selftest`) green. Do **not** run gates 5, 6, 7 or 8 — those are
  scheduled at T0/T6/T7 only, never at T1 (network/live-inference
  budget discipline, REQ-V1103-EC-01).

## Delegation record

When you report back, give me the exact five-cell bullet for the
report's `## T1` section:
`- T1 | delegated: yes | to: <your agent identity> | brief:
docs/spec/task-briefs/v1103-T1.md | map vs actual: <matches the reading
map below, or names what you had to read beyond it>`

## Reading map (§13.1)

`tools.py:44-49`, `:1286-1293`, `:1453-1461`, `:1486-1523`, `:1568-1590`;
`tests/test_exec.py:1-40`, `:130-140`; `tests/test_v1100_toolcall.py:
140-165`; `devtools/agent_eval.py:1113-1120`, `:1322-1381`;
`tests/test_v1103_exec.py` (new).

## Commit

One commit, this task only. Conventional commit header, body references
`(prompt: docs/prompts/221-v1103-t1-exec-guard.md)` — the orchestrator
will have written that prompt file before dispatching you; if it isn't
there yet, use the placeholder text `(prompt: docs/prompts/221-v1103-t1-exec-guard.md)`
verbatim, the orchestrator reconciles it. Do not touch
`docs/reports/report-v1.10.3.md`'s other `## T<n>` sections or the
`## Operator inputs` / `## Gate-7 attempt log` sections — only append
your own `## T1` section content, replacing the placeholder line.
