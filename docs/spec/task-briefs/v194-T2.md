# Task brief — v1.9.4 T2: gate 7's conversation smoke means something, and its wall is explained

One commit, prompt 182, after `v194-T1.md` lands. Paid OpenRouter calls
authorised for measurement (≤ $0.30 for this task; report the cost).

## The two facts from v1.9.3

1. **The advisory smoke cannot fail for a real reason today.** `devtools/
   rag_eval.py:79-82` sets `_SMOKE_FOLLOWUP = "а в неделях?"`; the verdict
   (`:380-390`) is `pass` iff turn 2 recorded a `search_documents` query
   sharing a token with turn 1's question. In every run since v1.9.3 T2's
   fix the model answers «28 дней — это 4 недели» from turn 1's own
   context without calling `search_documents` — a *correct* answer that
   the check reports as `fail`, and nobody can tell that apart from a real
   regression of REQ-V190-TOOL-06.
2. **Gate 7's wall is the smoke turn's chat, not retrieval.** Instrumented
   run (v1.9.3 T1+T2 review fixes): `index_corpus` 2.2 s, the 12 scored
   retrieval+rerank calls 12.2 s combined, the smoke turn **253 s of 268 s**
   — three chat completions on the production route (LM Studio primary,
   `qwen/qwen3.8-27b`: 112 / 97 / 42 s).

## What the spec pins — read before changing anything

`docs/spec/spec-v1.9.0.md:1158-1164` (REQ-V190-TOOL-06) names the pair
«Сколько дней отпуска?» → «а в неделях?» as the illustrative guarantee, and
`:1505-1512` describes the eval's smoke exactly as implemented (two turns,
that follow-up, the token-overlap verdict, advisory). Offline it is pinned
by T-V190-TOOL-07 (`tests/test_v190_tool.py:399-460`, `FakeLLM` scripting
the query) and the eval's own smoke tests (`tests/test_v190_eval.py:183`,
`:297-345`). **Route (a):** keep the pinned pair as turn 1 → turn 2 exactly
as today (it still exercises the history-carrying mechanism and stays
advisory), and **add a third turn** whose failure is meaningful. Do not
take route (b) (changing the pair and amending the shipped spec).

## Turn 3 — context-proof, gold-checked

Turn 3 text: a paraphrase of `evals/rag/questions.json`'s **second**
answerable item («Сколько дней отпуска можно перенести на следующий год?»,
gold `vacation_policy.md`, evidence «не более 10 дней») phrased as a
follow-up that cannot be answered from turns 1–2: `"а сколько из них можно
перенести на следующий год?"` — a module constant `_SMOKE_FOLLOWUP_2`,
commented with why it exists (this brief's fact 1). Verdict for turn 3,
computed from what `_RecordingSearcher` already records plus the results
it returns (extend it to keep the `SearchResult` per query if it does not):

- `pass` iff turn 3 made at least one `search_documents` call **and** at
  least one of its calls returned the gold source (`expected_source` of
  that item, prefix-matched the way the scored modes match) among its
  passages;
- `fail` with the recorded queries and the top sources otherwise.

Turn 2's verdict stays as it is (advisory, token overlap). The printed line
becomes two lines, e.g. `conversation-aware smoke (TOOL-06 pin): pass|fail —
…` and `conversation-aware smoke (context-proof): pass|fail — …`. Both
advisory, neither in the exit code — the spec says so and this patch does
not change gate semantics. Offline tests: extend `tests/test_v190_eval.py`'s
smoke script to three turns; add the cases "turn 3 calls search and hits
gold → pass", "turn 3 calls search, misses gold → fail with sources", "turn
3 makes no call → fail". Mutation entry `v194-smoke-turn3-gold-unchecked`
(the gold condition dropped so any search call passes) killed by the
miss-gold test; `--only` it.

Count-bearing lines: README "Tests" paragraph that says the eval "spends
inference tokens" on a two-turn exchange → three turns; the `spec-v1.9.0.md`
text is **not** edited (shipped spec); one dated note in
`docs/spec/spec-v1.9.0-delta-1.md`'s style if that file carries eval
addenda (read it; if it does not, the report's T2 section is the record).

## The wall — measure the alternative, decide nothing

`llm/openrouter.py:75-100` passes `tools` through the shared
`build_payload` (`llm/base.py:233-256`) and tool calls are parsed in the
shared `llm/base.py:358` path, so the OpenRouter client already carries the
agent's tool-calling route (production failover exercises it). Add an
**opt-in** route for the smoke turn's chat only: env `LLM_EVAL_CHAT_MODEL`
(`<provider>:<model>`, parsed with `config.parse_routed_model` like
`LLM_RERANK_MODEL`; read by `config.load_config` into
`cfg.llm_eval_chat_model`, default `None`), and in `rag_eval.run()` /
`conversation_smoke()` build the smoke's chat client as
`build_llm_client(cfg, client=client, purpose="eval-chat")` when set, else
the production client as today. `.env.example` documents it **commented
out** (the default stays the production route so the gate keeps exercising
what a deployment runs). Unit tests: routing chosen when set / not set
(mirror `tests/test_routing.py`'s rerank cases).

Then measure, alone on the box, sequentially, nothing else running:

| run | env | wall | smoke verdicts | retry lines |
|---|---|---|---|---|
| A1, A2 | production route (unset) | | | |
| B1, B2 | `LLM_EVAL_CHAT_MODEL=openrouter:mistralai/mistral-small-24b-instruct-2501` **in the process environment only** (`env VAR=… uv run …`), never written to `.env` | | | |

If `mistral-small-24b` fails to make tool calls through the agent loop on
run B1, try `openrouter:mistralai/mistral-nemo` once and say so. Report the
table and the per-turn completion times (reuse the instrumented-wrapper
technique from `/home/akh/.claude/jobs/45feeee3/tmp/v193/gate7_instrumented.py`
— wrap `agent.run_agent_outcome` and `rag.Searcher.search` with
timestamps). The operator decides whether `.env` gets the line; the report
ends the T2 section with the two numbers and the recommendation. The
`rag-eval` gate timeout (`config/quality_gates.yaml`) is re-derived from
the production-route walls (A1/A2, the default) under the file's rule.

## Paperwork and constraints

`docs/prompts/182-v194-smoke-turn3.md`; report section "T2 — gate-7
smoke" (facts 1–2, route (a) and why, the turn-3 criterion, the offline
tests, the measurement table, the recommendation, the cost);
`docs/llm-usage.md` row 92; delegation line. All constraints of
`v194-T1.md` apply (amend → re-read counts; self-excluding wait patterns;
no concurrency with mutation runs or other gate-7 runs; drift 0, 116 → 117;
`.env` never read — the probe reads it through `config` like the bot;
question text may be printed, passage text never). Never run gate 6 here.

Acceptance (sequential): `ruff check .` 0; `ruff format --check .` 0;
`pytest` 0 (collected count); `bot.py --selftest` 0; `checks.py lint-docs`
0; drift 117/117; `--only v194-smoke-turn3-gold-unchecked` killed; gate 7
on the production route (A1) exit 0 with both smoke lines printed.

Stop and report when: turn 3 on the production route makes no search call
in both A1 and A2 (then the context-proof design is wrong for this model —
report the captured outcomes, keep the code, mark the verdict `fail` as
real); the OpenRouter route cannot complete a tool call with either model;
cost exceeds $0.30.

Return a summary only: commit hash, acceptance exit codes, the measurement
table, per-turn times, the recommendation, cost, any stop item.
