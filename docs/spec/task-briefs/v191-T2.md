# Task brief — v1.9.1 T2: version bump, paperwork, gates, tag

T1 (`b959a63`) and its review fix (`9299da5`) are committed. This task closes
the patch. No source logic changes: only the version, the documentation that
tracks it, and the release paperwork.

## The measured evidence this release rests on

Put this table in `docs/reports/report-v1.9.1.md` — it is the whole
justification for the model choice and for the new constants. Measured with a
verbatim port of `_RERANK_SYSTEM` / `_rerank_messages` / `_parse_rerank_reply`
against the real `evals/rag/corpus`, gold passage planted outside the top-5 so
reranking has to do real work. 10 answerable questions per run (5 for the
first row — an earlier probe build matched corpus filenames strictly and
skipped the `.docx`/`.pdf` converted names; the per-call numbers are
unaffected).

| Backend | Model | max_tokens | schema | parsed | recall@5 | median s/call | completion tokens |
|---|---|---|---|---|---|---|---|
| LM Studio (box) | `qwen/qwen3.8-27b` — **v1.9.0 production** | 2048 | no | 5/5 | 5/5 | **196.9** | 695–1152 |
| LM Studio | `qwen/qwen3.8-27b` | 64 | yes | 0/10 | — | 16.4 | 35, content empty |
| LM Studio | `google/gemma-4-e4b` | 2048 | no | 10/10 | 9/10 | 35.9 | 132–688 |
| LM Studio | `google/gemma-4-e4b` | 512 | yes | 4/10 | — | 34.9 | 6 of 10 hit the cap |
| LM Studio | `google/gemma-4-12b-qat` | 64 | yes | 0/10 | — | 8.5 | 64, capped |
| LM Studio | `google/gemma-4-12b-qat` | 512 | yes | 3/10 | — | 47.5 | 7 of 10 hit the cap |
| OpenRouter | **`google/gemma-3-12b-it`** — chosen | 64 | yes | **10/10** | **10/10** | **0.83** | 10–53 |
| OpenRouter | `mistralai/mistral-nemo` | 64 | yes | 10/10 | 9/10 | 1.61 | 10–35 |
| OpenRouter | `mistralai/mistral-nemo` | 64 | no | 9/10 | 6/10 | 0.96 | 4–7 |
| OpenRouter | `ibm-granite/granite-4.0-h-micro` | 64 | yes | 10/10 | 8/10 | 1.25 | 10–39 |

Three conclusions to state plainly in the report:

1. `_RERANK_TIMEOUT_S` was 120.0 against a production median of 196.9 s.
   Gate 7 was never flaky — it was arithmetically impossible: 10 items ×
   196.9 s ≈ 1970 s against a 600 s gate timeout.
2. **Every chat model on the box is a thinking model**, `gemma-4` included —
   both local gemmas return *empty content* when the budget is small and hit
   the cap mid-reasoning at 512. The box holds no non-thinking chat model, so
   "use a smaller local model" is not available; that is why rerank routes out.
3. A JSON schema carries half the quality, not just the parse: without it
   `mistral-nemo` replies with two-element arrays that parse cleanly and leave
   eight of ten candidates in RRF order, recall@5 9/10 → 6/10.

Cost of the chosen route, at the prices read live from `openrouter.ai/api/v1/models`
on 2026-09-12 (`google/gemma-3-12b-it`: $0.050/Mtok in, $0.150/Mtok out):
≈ **$0.0035 per gate-7 run** (≈60k in / 3k out including the smoke). Gate 7's
own measured wall clock after the fix: 287.388 s.

## What to do

1. **Version.** `pyproject.toml` 1.9.0 → **1.9.1**, `uv.lock` accordingly.
   Follow this repo's established version-pin test convention exactly — find
   how v1.9.0's own version test is written and pinned, and do the same for
   1.9.1, including whatever repointing the previous version's test needs
   (`REQ-V190-EC-03` forbids deleting such a test; v1.9.0's T12 repointed
   `tests/test_v180_version.py` to the frozen `v1.8.0` git-tag blob and
   declared that the standing rule). Prove the new version test red-before /
   green-after and record both.
2. **Version- and count-bearing lines.** README and `AGENTS.md` carry the
   version and the gate-3 test count; `tests/test_v190_agents.py` pins some of
   those lines. Update them to what the release actually collects, not to a
   mid-task snapshot — that exact drift was a `/verify-run` finding on v1.8.0.
3. **`docs/reports/report-v1.9.1.md`.** T1 started it; finish it. It MUST
   carry, beyond the table above:
   - the seven gate results with exit codes and wall clocks;
   - the clean-context review: one 🔴 (a test assertion comparing
     `rag._rerank_response_format(3)` to itself), how it was fixed in
     `9299da5`, and the mutation proof that the rewrite actually bites
     (`"maximum": n` → `n - 1` makes the test fail `assert 2 == 3`; reverted,
     it passes — before the rewrite the same mutation left it green);
   - **a per-task delegation record**, required by `standards/reporting.md`
     § Run report whether or not a spec asks for it. Every task that wrote
     source files was either delegated, or names one of the four closed-list
     exemptions from `standards/workflow.md` §5.1 **verbatim**. T1 was
     delegated by task-brief file; the review ran in a clean context; the
     review-finding fix (`9299da5`) was *a single edit* under every threshold;
     this task is delegated by this file. An incomplete record is what
     `/verify-run` failed v1.9.0 on — do not repeat it;
   - **two disclosures, stated as such, not buried.** (a) Gate 6 ran to
     107/107 before T1's last documentation-only tweaks, one of which touched
     `tests/test_v190_agents.py`; a mutation gate is only as valid as the
     suite it ran against, so the full suite is re-run in step 5 below and
     *that* run is the authoritative one. (b) Gate 7's advisory
     `conversation_smoke` printed "fail" on a follow-up turn — the main chat
     model's non-deterministic tool-call choice, advisory only, not part of
     the exit code, unrelated to rerank. Say so explicitly; an unremarked
     "doesn't affect the gate" is how v1.9.0's red became a shipped red.
   - the **Ledger row** section (`paste into economics.md`), real and complete,
     following v1.9.0's row shape and `ledger_header` in
     `config/quality_gates.yaml`. Prompts for this release are 160–162.
4. **`docs/reports/tg-post-v1.9.1.md`** — Russian, under 1500 characters,
   names the executor model, carries the repo link. Follow
   `docs/reports/tg-post-v1.9.0.md`'s shape.
5. **`docs/llm-usage.md`** — a row per prompt of this release: 160 (T1), 161
   (the review fix), 162 (this task). Bullet-header frontmatter convention,
   not YAML — `checks.py lint-docs` is the authority.
6. **Gates.** Run all seven verbatim from `AGENTS.md`, in order, on the final
   tree — after the version bump and every documentation edit, so the run
   covers exactly what gets tagged. All seven MUST exit 0. Gate 6 is slow
   (~60 min); do not skip it, this is the authoritative run.
7. **Tag.** Only after all seven are green on the final tree: an annotated tag
   `v1.9.1` on the release commit. Do not push — the operator pushes.

## Constraints

- One prompt → one commit: this task is prompt 162
  (`docs/prompts/162-v191-t2-version-bump.md`), written with this repo's
  bullet-header frontmatter including `Model reason:`.
- No logic changes. If a gate fails for a reason that needs a code change,
  stop and report rather than fixing it inside a version-bump commit.
- `docs/reports/report-v1.9.0.md` and the v1.9.0 record stay untouched:
  v1.9.0 shipped with gate 7 red, and this patch fixes it forward rather than
  rewriting the history.
- `.env` is never read, printed or committed.
- Never `--no-verify`.
