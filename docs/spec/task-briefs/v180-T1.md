# Task brief — v1.8.0 T1 (§3: AGENTS.md context discipline)

Full spec: `docs/spec/spec-v1.8.0.md` §3 (REQ-V180-AGT-01…-04), §1, §2, §11.
Delta file (not needed for T1): `docs/spec/spec-v1.8.0-delta-1.md`.

## What to write

1. **`AGENTS.md` gains a `## Context discipline` section**, placed
   immediately after `## Project layout` (before `## Commit format`, which
   currently starts at line 64). It MUST carry all four of:
   - The delegation triggers, **including the write trigger**: a subagent
     when **any one** of: more than one file or folder to explore; a single
     read over 100 lines or 8 KB; **a task that writes source files — code a
     gate compiles, imports or runs**; more than ~10 edits within one task
     across every file it touches; applying a review or critique to a spec.
     State this holds **inside a `go` run, per task**, not only in
     interactive work.
   - Brief by file, never by retyping: load-bearing content the orchestrator
     already resolved goes into a task-brief file at
     `docs/spec/task-briefs/v180-T<N>.md`, and the subagent gets its path;
     the brief is ~5 lines, carries no history, names files and line ranges;
     the subagent returns a summary — findings, counts, `file:line` — never
     raw content.
   - **The closed list of four exemptions, verbatim, in these exact words**:
     *commands only* (no file writes); *artefacts only* (docs, config,
     fixtures with no code-shape dependency — anything no gate compiles,
     imports or runs); *a single edit under every threshold*; or *the task
     is itself the clean-context review*. State that the report names the
     exemption in those words, and that "the main context already holds
     what this needs" is NOT on the list.
   - In the main context: `Read` with offset/limit, `grep`/`find` with line
     context — never a whole-file read, never a directory walk. Absolute
     paths in shell.
   - A `<!-- SYNC: ... -->` comment naming `standards/workflow.md` §5.1 as
     canonical, matching the convention already at `AGENTS.md:76` and
     `:165` (grep for existing `SYNC:` comments to match the exact style).
   - **Important repository-boundary note**: the spec says this section is
     "adapted from `templates/project/AGENTS.md` § Context discipline", but
     that path does not exist inside this repository and is above the
     repository root — EC-01 forbids reading or writing anything there. Do
     **not** search for it. Write the section directly from the bullets
     above and this repo's existing `AGENTS.md` prose style/tone (see the
     existing `## Branch strategy` / `## Commit format` sections for voice).

2. **`## Branch strategy` gains the ownership-zone rule**, appended to the
   existing bullets at `AGENTS.md:78-89` (no existing bullet deleted or
   reworded): *parallelism is decided by **edit scope, not agent count** —
   split into ownership zones (a zone = files exactly one agent may write);
   disjoint zones run in parallel, one worktree each; **overlapping scopes
   run sequentially**, because separate worktrees only defer the conflict to
   merge time.*

3. **AGT-03, T1's half only**: correct the `## Project layout` line for
   `dashboard_server.py` to name the conversations routes it will gain
   (T5/T6 land the actual routes later — this is a documentation line, not a
   functional dependency). **Touch no count-bearing line** — not the pytest
   count, not the mutation-entry count, not the `--select` example. Those
   are T7's, after T6 lands the real numbers. Style edits, reorderings or
   rewrites of untouched paragraphs are a defect — change only what this
   release makes wrong.

4. **Review, don't rewrite, the three project prompt files**:
   `skills/host-info.md`, `skills/weather.md`, `CLAUDE.md` (which imports
   `AGENTS.md` via `@AGENTS.md` and documents the rtk hook). Read all three,
   judge each against this release, and change only what this release makes
   wrong — an unchanged file is a legitimate outcome. Return, for each: did
   it change, and if not, one sentence saying why it is still correct.

5. **Test**: `tests/test_v180_agents.py`, asserting `T-V180-AGT-01`:
   `AGENTS.md` carries `## Context discipline` with the write trigger, the
   four verbatim exemptions, and the ownership-zone rule. Write the test
   first, watch it fail for the right reason, then write the section.

## Constraints

- No count-bearing `AGENTS.md` line (test counts, mutation counts, gate
  numbers) — that is T7's, strictly after T6.
- No file outside this repository is read or written (EC-01).
- Conventional commit, type `docs:` (this task adds prose/docs and one new
  test file — no runtime behaviour changes; if you judge `test:` fits
  better because of the new test file, that is fine, use your judgement but
  stay inside the conventional-commit list in `AGENTS.md`). Header ≤ 72
  Unicode characters, no trailing period. Body references
  `(prompt: docs/prompts/128-v180-t1-context-discipline.md)`. End the commit
  message with:
  ```
  Co-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>
  Claude-Session: https://claude.ai/code/session_01FnGubtn2ec8m9fan73tGzZ
  ```
- Write your own prompt file first at
  `docs/prompts/128-v180-t1-context-discipline.md` using
  `docs/prompts/TEMPLATE.md`'s exact shape (seven-bullet header, then
  `## Goal`, `## Constraints`, `## Acceptance`, `## Stop`, in that order).
  Commit it together with your code/doc changes in the same commit (one
  prompt → one commit).
- Do not run the full six-gate sequence (gate 5 is live and gate 6 is slow —
  those are the orchestrator's job at T8/T10). Do run `uv run --locked ruff
  check .` and `uv run --locked pytest tests/test_v180_agents.py` (and the
  full `uv run --locked pytest` if you have time budget for it) as your own
  smoke check before returning.

## Acceptance

`T-V180-AGT-01` red before the `AGENTS.md` edit, green after. The four
exemptions appear verbatim. The project-prompt review record is returned
(3 files, changed/unchanged + reason). No count-bearing line touched.

## Report back

A summary only: what changed (`file:line` ranges), the commit sha, the test
result, and the project-prompt review record for the three files. Do not
paste full file contents back.
