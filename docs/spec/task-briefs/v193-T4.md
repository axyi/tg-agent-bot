# Task brief — v1.9.3 T4: version bump, paperwork, authoritative gates, tag

Same shape as `docs/spec/task-briefs/v192-T3.md` (read it; only the
differences are restated). Runs after T1–T3 and their clean-context
reviews have landed. Prompt number: the next free one after the review-fix
prompts (check `ls docs/prompts | sort -n | tail -1`).

## What v1.9.3 is

The three tails v1.9.2 disclosed, closed on the operator's order, plus the
operator's pre-push decision:

- T1: pre-push runs `mutation-all`; a gate timeout can no longer leave a
  mutated tree (restore-on-start-up refusal + SIGTERM-before-SIGKILL in
  the gate runner); `mutation-all` timeout re-measured.
- T2: the rerank third-attempt tail diagnosed (H1 cold start / H2 item)
  and fixed; three clean gate-7 runs with zero retry lines.
- T3: the ruff family proposal decided — adopted rules in `select` with the
  tree clean, never-list written into `pyproject.toml`.

## What to do

1. **Version** 1.9.2 → **1.9.3**; repoint `tests/test_v192_version.py` to
   the frozen `v1.9.2` tag blob; new `tests/test_v193_version.py`
   red-before / green-after.
2. **Count-bearing lines** (`AGENTS.md` test count and mutation-entry
   count, README release table, `tests/test_v190_agents.py` pins) set to
   what the final tree collects, measured after every other edit.
3. **`docs/reports/report-v1.9.3.md`** finished: consolidated gates table
   (seven gates, exit codes, walls); the gate-6 wall vs v1.9.2's 680 s;
   the two-or-three clean-context reviews summarised with their fix
   commits; the **per-task delegation record** complete (T1–T4 by brief
   file, reviews as clean-context reviews, review fixes by their brief
   files, executor models named); disclosures stated as such (the trailer
   string; anything T1–T3 flagged); **no open tail** unless one is
   genuinely open — then named; the **Ledger row** (v1.9.2's shape,
   `report-v1.9.2.md`'s row; prompts of this release from 173 to the last
   one, review findings tallied from the review briefs — count them from
   the files, not from memory).
4. **`docs/reports/tg-post-v1.9.3.md`** — Russian, < 1500 chars, executor
   models, repo link; lead with: pre-push on all 110+ mutations in ~11 min,
   the SIGKILL hazard closed, the rerank tail's one-sentence diagnosis,
   N ruff rules adopted / M never with reasons.
5. **`docs/llm-usage.md`** row for this prompt.
6. **`config/quality_gates.yaml`** `lint-docs.report_path` →
   `report-v1.9.3.md`; every timeout touched this release re-checked
   against this run's walls under the file's rules.
7. **Gates** — all seven verbatim from `AGENTS.md`, in order, final tree,
   nothing else on the box during gate 6, gate 6 and 7 never concurrent;
   exit code and wall each. Gate 6 = `mutation-all` with every entry this
   release added. Preconditions: LM Studio at `192.168.0.145:1234`
   (`curl -sS -m 3 http://192.168.0.145:1234/v1/models`; if silent try
   `172.16.50.233`, `192.168.178.170`, then stop and report — blocked, not
   skipped), Docker up, `.env` untouched.
8. **Tag** `v1.9.3` annotated, only after all seven are green, message
   naming the three tails. Do not push.
9. **After any amend or added commit, re-read every place a count or hash
   is named** — the v1.9.2 T3 executor's erratum commit itself left the
   prompt count stale and the coordinator had to amend it. The ledger row
   is pasted byte-identically into the lab ledger: it must be right on the
   tagged tree.

Constraints and return format: as `v192-T3.md`. One prompt → one commit;
no logic changes in this commit — a red gate that needs code stops the
task.
