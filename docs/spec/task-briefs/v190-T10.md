# Task brief — v1.9.0 T10 (docs and config catch-up, no version bump)

Read `docs/spec/spec-v1.9.0.md` §13 RPT-01/RPT-05 (lines 1751–1756,
1812–1841), §1 EC-07 item 6 (lines 190–195) and EC-13 (lines 221–230), and
§8 ERR-03 (lines 1364–1372) directly. This brief restates the decided doc
structure. T10 follows T1–T9 — everything it documents already exists;
**read the actual code** (`storage.py`, `rag.py`, `documents.py`,
`llm/embeddings.py`, `config.py`, `tools.py`) for the real numbers/names
rather than trusting this brief's paraphrase where the two might drift
(chunking numbers, config defaults, the six env var names).

## Resolved facts (not in the spec text)

- Work from whatever `HEAD` is when you start.
- Operator's embedding pair for the README's Embeddings section (a
  worked example, not a hardcoded requirement): `EMBEDDING_MODEL=
  text-embedding-nomic-embed-text-v1.5`, `EMBEDDING_DIM=768`, served
  locally by LM Studio (no new external dependency, no API cost).
- T8's eval numbers table (recall@5, MRR, page hit-rate per mode) — pull
  the actual measured numbers from `docs/reports/report-v1.9.0.md`'s T8
  section (already recorded there) rather than inventing placeholder
  numbers.
- The test count and mutation count are **explicitly not this task's
  job** — REQ-V190-RPT-05 says `AGENTS.md:146`'s count and `:155`'s
  mutation count are written **once, in T12**, not here. Leave those two
  specific numbers alone even though you're editing nearby lines; if your
  edit would touch them, stop and re-scope your edit to avoid it.

## `README.md` — `## Documents (RAG)` section

New section between `## The fetch tool` (`README.md:287`) and `## Add a
skill` (`:370`), in this exact subsection order — **Architecture** (the
two pipelines — ingest and query — one fenced diagram each, ASCII is
fine), **Chunking** (the four numbers: target 1000, hard_max 1200,
overlap 200, minimum 50 — and the two risks named in DOC-03: too-small
loses context/adds chunks/weakens BM25 stats, too-large dilutes the
embedding/reduces distinct hits in K=5/lengthens tool output), **Embeddings**
(the model name and dimension the operator supplied — 768,
`text-embedding-nomic-embed-text-v1.5` — why: runs locally in LM Studio,
no new dependency; batch 32; the `rag.embedding` `bot_state` pair),
**Retrieval** (cosine metric, 20/20/10/5 funnel, RRF k=60, the LLM
reranker and its fallback, K=5 and why — read RET-07's rationale),
**Storage** (STO-02's DDL summarised in prose — not the full SQL, a
paragraph — and the `vec_chunks.chunk_id → chunks.id → documents.id →
filename` chain), **Security** (the user predicate everywhere, the
partition key, the schema's one parameter — `EMBEDDING_DIM` interpolated
at DDL time, everything else bound — memory-only parsing, redaction),
**Limitations** (no OCR, no sharing, BM25 rebuilt per query, the
model-change manual fix and the empty-index rebind — NG-04 — replace-
on-reupload — NG-11 — the `Sources:` fallback, the eval's advisory items,
and the 300s budget for exactly what it is: checked between stages and
between PDF pages, no cancellable worker, hard bounds from the size
guards).

Then, still within or right after that section: the eval command
(`uv run --locked python devtools/rag_eval.py`) and the **numbers table**
filled with T8's actual measured recall@5/MRR/page-hit-rate per mode
(pull from the report, don't re-derive); the two new commands under
`## Commands` (`/documents`, `/delete <filename>` — one line each, same
table style as the existing rows, after `/reload_skills`); the ERR-01 rows
under `## Error behaviour` (`:593`) — one row per class (1–7, 10a–c, 11,
13–15; rows 8 and 9 reference the existing LLM rows, don't duplicate).
**One addition beyond the spec's original table, operator-authorized
during T4** (see the report's T4 section): a document whose total
extracted text clears the 20-char floor but whose per-page chunking
(PDF) yields zero chunks overall is refused with the same row-4 message
("The document contains no readable text.") via
`documents.EmptyDocumentError` — fold this into row 4's description as a
parenthetical (no new user-facing string exists, so it doesn't need its
own table row).
`## Limits` (`:570`) gains rows for file size (10 MiB), text length
(500,000 chars), documents per user (20), indexing budget (300s, what it
checks between — stages and PDF pages, no cancellable worker), the DOCX
archive bounds (2000 members/50MiB/20MiB single member/ratio 100) and the
500-page PDF ceiling, chunk size/overlap (1000/1200/200/50), K (5), rerank
candidates (10), tool output cap (12000 chars, `RAG_PASSAGE_CHARS` 1000
per passage). The seven-gate `## Tests` block (mirrors `AGENTS.md`'s
updated gate list — T8 already added the `rag_eval.py` line there; if
this README block wasn't updated to match at T8, fix it here). One
sentence on the partial lift of `REQ-NG-05`/`REQ-V1-NG-05` (EC-13) —
user-uploaded documents/embeddings/a vector index are now in scope,
conversation-level semantic memory/routing/caching stay out (NG-08).

## `AGENTS.md` edits — narrow, line-scoped

- `:18-30` — the stack/allowed-dependency list: add the five distributions
  (`sqlite-vec` named as the vector store, alongside `httpx`/`python-dotenv`).
- `:32-65` — layout bullets: add `rag.py`, `documents.py`,
  `llm/embeddings.py`, `devtools/rag_eval.py`, `devtools/pdf_fixture.py`
  (with the clause "tests and the eval import it" for the latter, matching
  DOC-06's existing rule about `devtools/` never being imported by the
  bot), `evals/rag/`.
- `:82` — the task-brief path token: `docs/spec/task-briefs/v180-T<N>.md`
  → `docs/spec/task-briefs/v190-T<N>.md` on **this one line only**. Do
  **not** touch anything else on this line or nearby —
  `tests/test_v180_agents.py:84-89` asserts the literal substrings
  "task-brief file", "`docs/spec/task-briefs/`", "never" survive; run that
  test after your edit to confirm it's still green.
- `:135-144` — the gate block gains the seventh line (`uv run --locked
  python devtools/rag_eval.py`) — **check whether T8 already did this**;
  if so, don't duplicate, just confirm it's correct. `:135`'s "Six MUST
  exit 0" (or similar) becomes "All seven MUST exit 0".
- `:146` and `:155` — **leave alone**, T12's job (see above).
- `:162-166` — the env-var note paragraph gains one clause naming the six
  new variables (`EMBEDDING_BASE_URL`, `EMBEDDING_MODEL`, `EMBEDDING_DIM`,
  `EMBEDDING_TIMEOUT_S`, `RAG_TOP_K`, `RAG_RERANK`), matching the existing
  paragraph's style for `LLM_REASONING_POLICY`/`LLM_REASONING_ON_PURPOSES`.

## `config/quality_gates.yaml`

`report_path: docs/reports/report-v1.8.0.md` (`:362` region) → `docs/reports/report-v1.9.0.md`
(REQ-V190-RPT-01). This is the **only** edit to this file this task makes
— T9 already did the mutation-gate wiring. After this edit,
`checks.py lint-docs` must be green against the T0 skeleton (already
extended by T1–T9's report updates).

## Task

Implement RPT-01, RPT-05, EC-07 item 6, EC-13's report/README sentences,
ERR-03. Test: `T-V190-EC-01` (read §11 for its exact assertion — likely
checks the README section exists with the right subsections in order, or
similar structural check). Files: `README.md:105-118`, `:287-300`,
`:370-372`, `:570-613`, `:776-790`; `AGENTS.md:18-65`, `:66-101`,
`:162-166`; `config/quality_gates.yaml:338-366`; `docs/spec/spec-v1.md:131-135`
(read-only — the "partially lifted" precedent language EC-13 cites); new
`tests/test_v190_agents.py`.

## Acceptance

`checks.py lint-docs` green against the T0 skeleton. `tests/test_v180_agents.py`
green (unchanged — a **constraint**, not something you edit). Docs match
the tree (spot-check: does every number you wrote match what the code
actually does — chunking constants, config defaults, the six env var
names — by reading the source, not by copying this brief). Full `pytest`,
`ruff check .`, `bot.py --selftest` green. Do not run
`devtools/mutation_check.py` or any live gate.

## Report back

A summary: files touched, the README section's final subsection order (so
the orchestrator can confirm it matches the required order), test ids
added/passing, the new `pytest --collect-only -q` total, any deviation
from this brief or spec §13 with a one-line reason, the commit sha and its
one-line message. Do not paste raw file content — findings and `file:line`
only.
