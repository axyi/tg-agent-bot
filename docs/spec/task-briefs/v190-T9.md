# Task brief — v1.9.0 T9 (mutation entries and the gate matrix)

Read `docs/spec/spec-v1.9.0.md` §12 (lines 1643–1737) directly for full
context. This brief restates the seven mutation entries verbatim. T9
follows T1–T8 — every mechanism these mutations target already exists in
`storage.py`, `documents.py`/`bot.py`, `rag.py`. **You must author each
`find` string against the actual shipped source** (`git grep` for the
exact substring in the real files) — do not trust this brief's mechanism
description to be a verbatim quote of the code; it names the mechanism,
you find the exact string.

## Resolved facts (not in the spec text)

- Work from whatever `HEAD` is when you start.
- `devtools/mutation_check.py:23-60`'s `MUTATIONS` list shape:
  `{"id", "path", "find", "replace", "why"}` — read the tail of the
  existing list (the last `v180-*` entries) for the exact dict shape and
  style before appending.
- `mutation-all` was last measured at 98 entries (see
  `config/quality_gates.yaml:278-308`'s ritual comment) — it becomes 105
  after this task's seven additions.
- `docs/reports/report-v1.9.0.md` already records T0's baseline
  `mutation_check.py` run: 98 mutations, 98 killed, 0 survived — that's
  the pre-v190 baseline your seven new entries add to.

## The seven mutation entries, exact table

| id | mechanism it breaks | must be killed by |
|---|---|---|
| `v190-knn-user-predicate-dropped` | `knn_chunk_ids`: remove `AND user_id = ?` (and its bound value) from the KNN | `T-V190-SEC-01` |
| `v190-bm25-user-predicate-dropped` | `user_chunks`: remove `WHERE d.user_id = ?` so BM25 indexes every user's chunks | `T-V190-SEC-01` |
| `v190-documents-user-predicate-dropped` | `list_documents`: remove the user predicate | `T-V190-SEC-02` |
| `v190-delete-user-predicate-dropped` | `document_id_for`: remove the user predicate so `/delete` resolves another user's file | `T-V190-SEC-03` |
| `v190-delete-vec-skipped` | `delete_document`: skip the `vec_chunks` deletes | `T-V190-STO-03` |
| `v190-size-precheck-disabled` | the handler's `file_size > 10_485_760` check: replace the comparison with `False` | `T-V190-CMD-02` |
| `v190-sources-fallback-dropped` | `attach_sources`: return the reply unchanged instead of appending the canonical block when no valid source line remains | `T-V190-TOOL-06` |

Each `find` string **must match exactly once** in its file — verify this
yourself (`grep -c` the exact string) before adding the entry; if it
matches zero or more than once, adjust the string (widen context) until it
is unique, matching the existing entries' own style/precision. Each is
proved at T9 inside the gate's own mutate → expect red (the named test
goes red) → revert cycle — this is what `mutation_check.py` itself
automates; your job is authoring correct entries, not hand-running the
cycle (the script does that).

## `config/quality_gates.yaml` wiring (REQ-V190-EC-10)

`mutation-v190` gate added to the **`pre-push`** profile (`:9-11`), same
key set and order as `mutation-v180` (`:268-276`) — only the `--select`
prefix (`"v190-"`) and `timeout_seconds` differ. **Measure the timeout
yourself, never guess**: run `uv run --locked python
devtools/mutation_check.py --select v190-` once, time it, then
`timeout_seconds = 2 × that measured time, rounded up to the next 10s`,
per the existing rule at `:204-208` — comment the entry with today's date
and the measured figure, matching the style of the existing `mutation-*`
entries exactly. Also re-measure `mutation-all`'s timeout the same way
(a full `uv run --locked python devtools/mutation_check.py` run, now 105
entries) and update `:309`'s `timeout_seconds` if the measured value
requires it. **No existing gate's `argv`, `result_mode`, `blocking`,
`severity` or profile membership changes** — only `mutation-v190` (new)
and `mutation-all`'s timeout (re-measured) move.

## The gate matrix (REQ-V190-EC-12)

`tests/test_v15_standards.py:1728-1745`'s
`test_v15_gate_04_profile_matrix_agrees_with_the_spec_table` parses the
gate matrix from the file it names at `:1729` (today
`spec-v1.8.0-delta-1.md`) by the literal header `| gate | pre-commit` and
rows until the first non-`|`-starting line, mapping labels through
`_GATE_MATRIX_LABEL_TO_NAME` (`:1685-1707`). Your task:

1. Repoint `:1729`'s string literal to `docs/spec/spec-v1.9.0-delta-1.md`
   — this one line only, nothing else in that test changes.
2. `docs/spec/spec-v1.9.0-delta-1.md` **already contains** § The gate
   matrix (this is an existing spec artefact, not something you write from
   scratch) — read it and confirm it already carries the 21 rows of
   `spec-v1.8.0-delta-1.md:22-45` verbatim plus `rag_eval.py` and
   `mutation_check.py --select v190-`. If it's already correct, no edit
   needed there; if something is missing or wrong, fix only what's wrong.
3. `_GATE_MATRIX_LABEL_TO_NAME` (`tests/test_v15_standards.py:1685-1707`)
   gains the two new labels: `` "`mutation_check.py --select v190-`":
   "mutation-v190" `` and `` "`rag_eval.py`": "rag-eval" `` (per EC-03's
   amendment table, already authorized — confirm this hasn't already been
   done by an earlier task; if it has, skip).
4. `spec-v1.8.0-delta-1.md` itself must **not** carry the header literal
   `| gate | pre-commit` anymore if it still does (so the parser finds the
   matrix only in the v1.9.0 delta file) — check this; it may already be
   fine since round-1/2/3 of this spec's own authoring already moved
   content there per the spec's own §1 budget notes.

## Task

Implement EC-10, EC-12 end to end. Files: `devtools/mutation_check.py:23-60`
and **tail only** (append after the last `v180-*` entry; touch `main()`
only if the `--select` dispatch needs it, which it likely already
supports from earlier `--select v170-`/`v180-` usage); `config/quality_gates.yaml:204-208`,
`:262-320`; `tests/test_v15_standards.py:1685-1745`;
`docs/spec/spec-v1.9.0-delta-1.md` § The gate matrix (read first, edit
only if wrong); `storage.py`, `rag.py`, `bot.py` — **only the seven exact
lines the seven `find` strings target**, and only if you need to adjust
whitespace/context to make a `find` string unique (do not otherwise touch
these files' logic — T1/T4/T5/T6/T7 already implemented the mechanisms).

## Acceptance

`uv run --locked python devtools/mutation_check.py --select v190-` → 7/7
killed, each `find` matching exactly once (the script itself will error
loudly if a `find` matches zero or more than once — resolve before
reporting done). `uv run --locked python devtools/mutation_check.py`
(full, unfiltered) → 105/105 killed, 0 survived/errored/drifted.
`test_v15_gate_04_profile_matrix_agrees_with_the_spec_table` green. Full
`pytest`, `ruff check .`, `bot.py --selftest` green. Do not run any live
gate (`--selftest-live`, `rag_eval.py`) — T8 already ran gate 7 once;
T11/T13 re-run everything.

## Report back

A summary: the seven `find` strings you authored (exact text, so the
orchestrator can spot-check uniqueness), the measured `mutation-v190` and
`mutation-all` timeouts (with today's date), test/gate results, any
deviation from this brief or spec §12 with a one-line reason, the commit
sha and its one-line message. Do not paste raw file content beyond the
seven `find`/`replace` pairs — findings and `file:line` only otherwise.
