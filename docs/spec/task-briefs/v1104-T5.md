# Task brief v1104-T5 — the first commit: version bump, `uv lock`, version tests, the paperwork

Repo: `/home/akh/aihome/coders-su/projects/tg-agent-bot`. Spec:
`docs/spec/spec-v1.10.4.md` §8 (`REQ-V1104-VER-01`, lines 661-687;
`REQ-V1104-RPT-01`/`-02`/`-03`, lines 689-754), §1 EC-02 rows 15-16
(lines 107-108), §6.1 (lines 493-522, the `T-V1104-VER-01`/`-02`,
`T-V1104-DOC-02`, `T-V1104-DOC-04` rows). Report:
`docs/reports/report-v1.10.4.md` — append a `## T5` section (the
orchestrator will extend it further after your commit, with the gates
1-7/identity-check/replay/E1-E7 results — you write the version-bump
part of it, don't worry about those later additions).

Base commit: `244b9a4` (T4, on `main`). This is **the first of T5's two
commits, one prompt (233), REV-02**. Do **not** create the second
(evidence) commit — the orchestrator does that itself, in the main
context, after running gates 1-7 against your commit and reusing T4's
gate-8 result via the identity check. Your job ends after this one
commit.

## What this commit lands (all in one commit)

1. **`pyproject.toml`**: `project.version` `"1.9.5"` → `"1.10.4"`. Only
   this key changes in this file.
2. **`uv lock`** (online — this is the run's only other network-touching
   `uv lock`, besides T0's offline check): regenerate `uv.lock` for the
   version literal only; confirm no other dependency line moves (`git
   diff 244b9a4 -- uv.lock` should show only the project-version-related
   hash/version fields, never an added/removed/upgraded package).
3. **`tests/test_v195_version.py`** (EC-02 row 15): repoint from a live
   `pyproject.toml` read to a `git show v1.9.5:pyproject.toml` blob read,
   in the exact shape of `tests/test_v194_version.py:25-34` (read that
   file first, copy its pattern, just changing the tag from `v1.9.4` to
   `v1.9.5`).
4. **New tests** `T-V1104-VER-01` and `T-V1104-VER-02` (module: put them
   in a new `tests/test_v1104_version.py`, or extend
   `tests/test_v1104_gates.py` — your call, but `T-V1104-VER-01`/`-02`
   are new ids from spec §6.1 lines 519-520, not something T1/T2 wrote):
   - `T-V1104-VER-01`: `project.version == "1.10.4"` on the live tree;
     `git show v1.9.5:pyproject.toml` still reads `1.9.5`.
   - `T-V1104-VER-02`: for `pyproject.toml` and `uv.lock` **only**, the
     diff from the `f3ce1a5` blobs is project-version-only under
     `devtools/agent_eval.py`'s `dependency_diff_is_version_only` helper
     (no whole-repository claim), **and** the project-version delta from
     `f3ce1a5` exists and resolves to `1.10.4`. Write this test-first:
     confirm it's **red before the bump** (run it against `244b9a4`
     before you touch `pyproject.toml`) and **green after**. Record both
     runs (command + result) in your report section.
5. **`README.md`'s release table** (`REQ-V1104-VER-01`, after the
   `v1.10.3` row T1 already added): append, verbatim from
   `docs/spec/spec-v1.10.4.md:679` (read it, don't retype from this
   brief — the spec is the source of truth), the `v1.10.4` row with
   `<effective judge>` resolved to `anthropic/claude-sonnet-5 (another
   vendor)` (T4's gate 8 passed on the primary judge, no fallback used —
   confirm this against `docs/reports/report-v1.10.4.md`'s `## T0`
   section, Stage 0 check 7, before writing it). In the **same edit**,
   the `v1.9.5` row loses its trailing `; this release` clause (it reads
   `...startup guards` and stops there — the "this release" claim moves
   to the new `v1.10.4` row).
6. **README's gate-8 results table** (`README.md:594-600`, the five
   `| metric | pending (T9) |` rows) — this is the run's own T4 result,
   "on this run", filled with these exact values (read
   `docs/reports/report-v1.10.4.md`'s `## T4` section for the source of
   truth, don't take these numbers on faith from this brief):
   ```
   | injection | 5/5 (floor 5) |
   | hallucination | 4/4 (floor 3) |
   | memory | 3/3 (floor 3) |
   | judge mean | 0.907 (floor 0.8) |
   | latency (advisory) | max 3.58s vs 4.0s (advisory, non-blocking) |
   ```
   `T-V1104-DOC-02` (§6.1, spec line 515) is the test proving this: red
   before your edit (the `pending (T9)` rows and no-"this release"
   `v1.9.5` row), green after (the `v1.10.4` row ending `this release`,
   the `v1.9.5` row without it, no `pending` string left in `:594-600`).
7. **`AGENTS.md`'s two stale count-line citations** (EC-02 row 16,
   `:161-162` and `:172` on the pre-edit tree — re-verify the exact
   current line numbers before editing, they may have drifted slightly):
   `"1638 tests as of spec-v1.9.5 T2"` → this run's own **measured**
   pytest collection count (re-run `uv run --locked pytest --collect-only
   -q -o addopts="" | grep -c '::'` yourself on your own tree after all
   your edits — do not guess the number), dated `"as of spec-v1.10.4
   T5"`; `"120 entries as of v1.9.5 T2"` → `"144 entries as of
   spec-v1.10.4 T5"`. `T-V1104-DOC-04` (§6.1, spec line 517): rename the
   existing test (was
   `test_t_v1103_rpt_03_agents_md_count_lines_landed_at_t4`-shaped or
   similar — find its actual current name) to
   `test_t_v1104_rpt_03_agents_md_count_lines_landed_at_t5`, red before
   your edit (asserting the old numbers/date are gone), green after.
8. **Provisional `docs/reports/tg-post-v1.10.4.md`**: Russian, under
   1500 characters by `wc -m` (quote the count in your report section),
   naming `claude-sonnet-5`, linking
   `https://github.com/axyi/tg-agent-bot`, naming the subject (the five
   `v1103-*` mutations landed, every frozen-list pin rewritten, gate 8
   green on this run), the judge as VER-01's row words it, the gate-8
   numbers "on this run". This is **provisional** — the orchestrator may
   touch it again before the evidence commit if anything changes, but
   write it as if final; don't leave placeholder text in it.
9. **Provisional `## Ledger row`** section update in
   `docs/reports/report-v1.10.4.md`: replace the "Not reached" placeholder
   with a fenced row shaped like `tests/test_v170_bench.py:328-331`'s
   header expects, `Ver` = `1.10.4`. You do not have every final number
   yet (gates 1-7 haven't re-run against your own commit, the identity
   check hasn't run) — mark cells you cannot fill yet as `TBD`, matching
   this repo's own precedent for a provisional ledger row (see
   `docs/reports/report-v1.10.1.md` or similar for the `TBD`-cell
   convention if one exists; otherwise use your judgment, the
   orchestrator finalizes it before the evidence commit).
10. Append your own **version-bump part** of `## T5` to
    `docs/reports/report-v1.10.4.md` (the amendment sites you touched,
    the test-first record for `T-V1104-VER-02`, `T-V1104-DOC-02`,
    `T-V1104-DOC-04`, the measured pytest count and its date-stamp).
    Leave the rest of T5's section (gates 1-7, identity check, replay,
    E1-E7, the evidence commit) for the orchestrator to append after
    you.
11. Add a `docs/llm-usage.md` row for prompt 233 (check the file's last
    row number first — should be 143 after T4, so yours is 144).

## Non-goals this commit (NG-01, NG-08)

No NG-01-listed instrument file touched. No earlier report, handoff or
`docs/llm-usage.md` row edited (only appended). The evidence commit's
four paths (final `report-v1.10.4.md`, final `tg-post-v1.10.4.md`, the
`docs/prompts/233-*.md` file, the final `docs/llm-usage.md`) are **not**
yours to finalize — you write provisional versions, the orchestrator
does the rest after running the live gates.

## Acceptance

- `T-V1104-VER-02` proven red-before/green-after (both runs recorded).
- `T-V1104-DOC-02`, `T-V1104-DOC-04` proven red-before/green-after.
- `uv run --locked ruff check .` clean.
- `git diff 244b9a4 -- uv.lock` shows only the version-related change,
  no dependency added/removed/upgraded.
- Full `uv run --locked pytest -q` green; collection count ≥ 2285 + 22
  (already comfortably true before this commit — 2308 collected as of
  `244b9a4` — your own new tests push it further; record the exact
  final count).
- Gates 1-4 only this task (`uv sync --locked`, `ruff check .`,
  `pytest`, `bot.py --selftest`). Do **NOT** run `bot.py
  --selftest-live`, `devtools/rag_eval.py`, `devtools/agent_eval.py`, or
  `devtools/mutation_check.py` — the orchestrator runs gates 1-7 (plus
  the gate-8 reuse) against your finished commit itself, right after you
  report back.

## Delegation record

Report back the exact five-cell bullet:
`- T5 | delegated: yes | to: <your agent identity> | brief:
docs/spec/task-briefs/v1104-T5.md | map vs actual: <...>`

## Commit

**One commit only** (this is T5's first of two — REV-02's rule; the
second, evidence-only commit is the orchestrator's, not yours).
Conventional commit header, body referencing `(prompt:
docs/prompts/233-v1104-t5-version-bump.md)`, same
`Co-Authored-By`/`Claude-Session` trailer as recent commits (check `git
log -1 --format=%B` on `244b9a4` for the exact text).
