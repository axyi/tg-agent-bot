# Brief v1111-T0 — PIN-01/-02, delegated

Spec: `docs/spec/spec-v1.11.1.md:567-598` (REQ-V1111-PIN-01), `:704-706`
region for `T-V1111-PIN-02` if present in §6.1 — read the spec yourself for
the exact test names/assertions. Base tree: `2431034` (tag `v1.11.0`).

Task: write `docs/spec/task-briefs/v1111-T0-pin-inventory.md` — a table
`site (file:line) | literal | task that rewrites it | rewrite form` — by
grepping the **live tree** yourself (never copy the spec's list verbatim;
extend/verify it against what is actually in the tree today). Start from
and extend the sites PIN-01 names: `tests/test_v1110_doc.py:200-202`,
`:378`, `tests/test_v1110_ing.py:548`, `tests/test_v190_agents.py:300`
(`DOC_LIMIT_REPLY`), `tests/test_v1110_ver.py:135`; check
`tests/test_v1110_mod.py`/`test_v1110_out.py` for width pins (expected
none); T6's eleven VER-01 lists; the "checked, unaffected" sites
`tests/test_v1110_pin.py:100-120`, `tests/test_v1110_err.py:119-199`
(rows 10, 17), `tests/test_v1110_ver.py:116-124`,
`tests/test_v190_agents.py:285-306`; confirm `tests/test_v1110_inventory.py`
(`_SPEC_TEST_FUNCTIONS`) unchanged.

Then write `tests/test_v1111_pin.py` (module is T0's): `T-V1111-PIN-01`
asserts the floor (`docs/spec/task-briefs/v1111-T0-nodeids.txt` already
holds the frozen sorted node-id list, 2384 lines — assert your test
collection is `>= 2384` reading that file, never re-measuring/committing a
new floor) and asserts the inventory md exists/well-formed;
`T-V1111-PIN-02` asserts `tests/test_v1110_inventory.py`'s
`_SPEC_TEST_FUNCTIONS` (61 pairs) is unchanged — no `v1110` test renamed,
no `v1111` test added to it. Both tests must pass green. Rewrite form is
presence/contiguity/order only — never a "nothing follows" assertion.

Constraints: never open/print/grep `.env`, `data/`, `bot.db`, `sandbox/`,
`exec_audit.jsonl`; no LAN address or secret value in anything you write;
zero new dependencies. Return a summary (counts, file:line, pass/fail) —
never file content.
