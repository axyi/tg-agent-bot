# v1102-T3 — repoints and paperwork (minus the numbers, which land at T6)

Spec: `docs/spec/spec-v1.10.2.md` §9 (`REQ-V1102-GATE-03`), §10
(`REQ-V1102-RPT-01`'s first sentence, `REQ-V1102-RPT-03`'s T3 clauses).
Base: `HEAD` after T2 lands (check `git log -1` names a v1.10.2 T2 commit
before starting). Offline throughout — no live call this task.

**Important framing:** v1.10.1's own T7/T8 (which would have written most
of this paperwork) never ran — that spec stopped at T6 (gate 8 red,
Stage B'). So `AGENTS.md`/`README.md` still read the **v1.10.0-era** text
in most places below; you are not just tweaking a v1.10.1-era sentence,
you are landing v1.10.1's paperwork **and** v1.10.2's on top of it,
together, in this one task. Don't assume a "v1.10.1 already did most of
this" starting point — verify each site's *current* text with `grep`
before editing it.

## 1. `config/quality_gates.yaml` — `lint-docs.report_path` repoint (RPT-01)

Line ~727: `report_path: docs/reports/report-v1.10.1.md` →
`docs/reports/report-v1.10.2.md`. Nothing else in that gate's block
changes (the `ledger_header` string stays exactly as it is).

## 2. Gate matrix — GATE-03

`tests/test_v15_standards.py`:
- `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table` (around
  line 1822) currently reads `spec-v1.10.1.md` as the file it parses the
  gate matrix table out of — repoint to `docs/spec/spec-v1.10.2.md`.
  **`spec-v1.10.1.md` is never edited** (it is frozen history) — this
  test's own `docs/spec/spec-v1.10.2.md` §9 table (`REQ-V1102-GATE-03`)
  already carries the `mutation_check.py --select v1102-` row verbatim,
  so once you repoint the parse target this test reads the new table
  automatically.
- `_GATE_MATRIX_LABEL_TO_NAME` (around line 1791) gains one entry, placed
  immediately after the `` "`mutation_check.py --select v1101-`":
  "mutation-v1101", `` line:
  ```python
  "`mutation_check.py --select v1102-`": "mutation-v1102",
  ```
  (this label/name pair won't resolve to a real yaml gate until T5 adds
  `mutation-v1102` — that's fine, the test only requires every label in
  this dict to appear as a row in the spec's own table, which it already
  does; `config["profiles"]` membership is checked separately and
  `mutation-v1102` isn't expected to exist in the yaml until T5).

New test file `tests/test_v1102_gates.py` (per §8.1, ids
`T-V1102-GATE-03` and `T-V1102-RPT-01` — write both here, test-first
against the unedited tree, then make the edits above):
- `T-V1102-GATE-03`: `lint-docs.report_path ==
  "docs/reports/report-v1.10.2.md"`; the matrix-parsing test in
  `test_v15_standards.py` is green against **this** file (you can assert
  this indirectly by re-running that specific test, or by asserting
  `_GATE_MATRIX_LABEL_TO_NAME` contains the new key and every key is a
  row of `docs/spec/spec-v1.10.2.md`'s §9 table).
- `T-V1102-RPT-01`: `checks.load_gate_config()["gates"]["lint-docs"]
  ["report_path"] == "docs/reports/report-v1.10.2.md"`.

## 3. The EC-02 gap found at T0: `tests/test_v1101_gates.py:254-256`

`test_lint_docs_report_path_repointed_to_v1101` currently asserts
`config["gates"]["lint-docs"]["report_path"] ==
"docs/reports/report-v1.10.1.md"` — this will break the moment step 1
above lands. Rename the function (matching the pattern of
`test_t_v1102_rpt_01_lint_docs_repointed_to_this_release` in
`tests/test_v170_bench.py`, see step 5) and update the literal to
`"docs/reports/report-v1.10.2.md"`. This was recorded in the T0 report
section as an EC-02 amendment — you are closing it now.

## 4. `.env.example` (CFG-01 routing defaults) — key **names** only, never values

Read the current file first (`.env.example`). Per `REQ-V1101-CFG-01`
(`docs/spec/spec-v1.10.1.md:222-292`) the intended routing defaults are:
`LLM_PROVIDER` defaults to `openrouter` (not `lmstudio`); `LLM_FAILOVER`
stays as its current default; `LMSTUDIO_BASE_URL`/`LMSTUDIO_MODEL` become
**empty** with a comment "set both to use LM Studio, the alternative
provider" (LM Studio is no longer the assumed default provider);
`OPENROUTER_MODEL` gets a sensible non-empty example default;
`EMBEDDING_BASE_URL`/`EMBEDDING_MODEL`/`EMBEDDING_DIM` point at the
OpenRouter embeddings route by default; `LLM_JUDGE_MODEL`,
`EMBEDDING_TIMEOUT_S` and `DB_PATH` stay unchanged. **No new key** — only
existing key defaults/comments move. Never write a real credential value
— `.env.example` ships placeholder/empty values only, as it already does.

New test `T-V1102-CFG-01` (in `tests/test_v1102_docs.py`, see step 7):
load `.env.example` through `load_config()` with the token/id/key fields
stubbed with placeholder strings (see `tests/test_v1101_config.py`'s
`T-V1101-CFG-03`, never landed, for the pattern — you're writing what
that test would have been) and assert `cfg.llm_provider == "openrouter"`,
`cfg.lmstudio_model == ""`, no routed model carries the `lmstudio:`
prefix — do **not** assert `cfg.lmstudio_base_url` is empty (a built-in
default may substitute one, per `config.py:341`'s existing behaviour —
check this before asserting).

## 5. `README.md` paperwork

- **`## Configure`** (~line 47-72): add one sentence naming
  `OPENROUTER_API_KEY` as a secret to fill in alongside
  `TELEGRAM_BOT_TOKEN`/`ALLOWED_TG_IDS` (it currently isn't mentioned at
  all in this section) — since the production route now defaults to
  OpenRouter.
- **`## Switch provider`** (~line 236-257): currently presents
  `LLM_PROVIDER=lmstudio` as "the default" and OpenRouter second. Reorder
  so `LLM_PROVIDER=openrouter` (needing `OPENROUTER_API_KEY` and
  `OPENROUTER_MODEL`) is presented as the default, and `lmstudio` as the
  alternative (needing `LMSTUDIO_BASE_URL`/`LMSTUDIO_MODEL`, with its
  existing tool-calling-support caveat kept). Add a short mention of the
  embeddings route (`EMBEDDING_BASE_URL`/`EMBEDDING_MODEL`/
  `EMBEDDING_DIM`) alongside — one or two sentences, this section is not
  the RAG doc's home (that's `## Documents (RAG)`), just note where the
  embeddings default now points. Keep the Failover paragraph as is.
- **The `--selftest-live` paragraph** (~line 1061-1067): re-check its
  wording still matches `REQ-V1101-G5-02`: "unconfigured providers are
  skipped, not failed" should already be accurate (`lmstudio` SKIPs when
  no route uses it) — if the wording already holds, leave it; only edit
  what's actually stale.
- **Grep for any other "LM Studio is the default" framing** elsewhere in
  `README.md` (`grep -in "lmstudio" README.md` and read each hit in
  context) and reword anything that still assumes LM Studio is the
  primary/default provider rather than the alternative. **List every
  file:line you touched or considered-and-left in the report handback**
  (the orchestrator needs this list for RPT-03's "any other ... sentence
  reworded, listed in the report" requirement).
- **The release table** (`README.md`, the "Historic labels" table under
  `## Versioning`, whose `v1.9.5` row currently ends `; this release`):
  insert **two new rows**, after the `v1.9.5` row, **verbatim**:
  ```
  | v1.10.0 | — | run stopped at T9 by the stop route, gate 8 red on model behaviour (injection 2/5, hallucination 2/4 on lmstudio:qwen/qwen3.8-27b), not tagged; the implemented suite ships with v1.10.2 |
  | v1.10.1 | — | run stopped at T6 by the stop route, gate 8 red on model behaviour (injection 1/5 on openai/gpt-4.1-mini — three clause-(e) misses, the prompt gap v1.10.2 closes), not tagged; every live gate moved onto OpenRouter; ships with v1.10.2 |
  ```
  **Do not** touch the `v1.9.5` row's "; this release" clause (T6 removes
  it) and **do not** add a `v1.10.2` row yet (T6 adds it) — this table's
  `v1.10.2` row and the `v1.9.5` clause removal are explicitly T6's job,
  not this task's.
  **Also do not touch** the separate 5-row `| metric | result |` table
  under `## Agent evaluation (gate 8)` (~line 579-585, all "pending
  (T9)") — that's T6's job too, filled from T5's gate 8 run.

## 6. `AGENTS.md` paperwork

- Line ~148 "All seven MUST exit 0" → "All eight MUST exit 0" (this repo
  now has 8 gates, `agent_eval.py` already in the command list at
  ~line 159 — just the count sentence is stale).
- The gate-5 sentence (~line 162-168, "a provisioned `.env`, a reachable
  Docker daemon with the sandbox image pulled, LM Studio and an
  OpenRouter key" ... "**Gate 5 must be fully green at every commit,
  including its `lmstudio` check**"): drop "LM Studio and an OpenRouter
  key" for "every provider the configuration routes to", and drop "its
  `lmstudio` check" for "an OpenRouter key; LM Studio only when a route
  names it" (matching `REQ-V1101-G5-02`'s actual, already-implemented
  behaviour — LM Studio SKIPs cleanly when unconfigured, it is no longer
  a hard requirement).
- The benchmark paragraph (~line 254-271, "## Benchmark"): **add** two
  sentences (there is currently no waiver language here at all, since
  v1.10.1 T7 never ran to add it): "v1.10.1 waived this rule by operator
  decision — the provider, the model and `prompt_tools_sha256` all
  changed, so no run was comparable to the LM Studio baseline; **v1.10.2
  carries the waiver: its prompt change moves the hash again**; a fresh
  OpenRouter baseline is a candidate for a later release."
- The brief-path token (~line 95, `` `docs/spec/task-briefs/v190-T<N>.md` ``)
  → `` `docs/spec/task-briefs/v1102-T<N>.md` ``.
- **Do not** touch the two count lines (`~161-162` mutation-entry count,
  `~170` pytest count) — those move at **T6** because T5 changes both
  numbers (139 mutations, a higher pytest count). Leave them exactly as
  they are.

New test `tests/test_v1102_docs.py`'s `T-V1102-RPT-03`'s **T3 function**
(name it e.g. `test_t_v1102_rpt_03_agents_md_brief_path_token_is_v1102`,
written now — the T6 numbers function is written separately at T6, not
here): assert `AGENTS.md` contains `"All eight MUST exit 0"`, not `"All
seven"`; contains both waiver sentences (the v1.10.1-and-v1.10.2 one you
just added); contains the token `v1102-T<N>` (literally, as it appears in
the file) and not `v190-T<N>`.

## 7. `tests/test_v170_bench.py` and `tests/test_v190_agents.py` (RPT-01's already-listed sites, plus RPT-02's T3 function)

- `tests/test_v170_bench.py:316-331`: rename
  `test_t_v1101_rpt_01_lint_docs_repointed_to_this_release` (or whatever
  it's currently called — check) to name v1.10.2, and re-pin
  `report_path` to `docs/reports/report-v1.10.2.md`.
- `tests/test_v190_agents.py:279-291`: `"report_path:
  docs/reports/report-v1.10.1.md" in text` → `...v1.10.2.md`;
  `"report_path: docs/reports/report-v1.10.0.md" not in text` →
  `...v1.10.1.md` (the "not in text" assertion's target release moves
  forward by one each time, per the existing pattern at this site).
- `tests/test_v190_agents.py:82-100` (the brief-path-token test): rename
  to name v1102 and re-pin the `v1102-T<N>` token (mirrors what you wrote
  for `test_t_v1102_rpt_03_agents_md_brief_path_token_is_v1102` above —
  if that's the same site, don't duplicate; check which file currently
  owns this assertion and put it there once).
- **New** `tests/test_v1102_docs.py` function
  `test_t_v1102_rpt_02_stopped_release_rows_landed_at_t3` (`T-V1102-RPT-02`'s
  T3 half — the T6 half, `..._release_and_gate8_rows_landed_at_t6`, is
  written later at T6, not here): assert README's release table contains
  the exact `v1.10.0` and `v1.10.1` rows from step 5 above (both
  containing `"not tagged"`), and does **not** yet contain a `v1.10.2`
  row.

## 8. `docs/reports/report-v1.10.0.md` and `docs/llm-usage.md` row 108 (RPT-04)

- `docs/reports/report-v1.10.0.md:232-233`: append " — **not delegated: a
  deviation from `standards/workflow.md` §5.1, recorded on 2026-09-17 by
  v1.10.2 T3; `docs/llm-usage.md` row 108 corrected to match**" to
  whatever that T6 line currently reads (read it first — do not
  duplicate wording, just append this clause).
- `docs/llm-usage.md` row 108 (~line 258): replace "Delegated." with "Not
  delegated (a §5.1 deviation, recorded by v1.10.2 T3)." — read the exact
  current cell text first so you replace only that phrase, nothing else
  in the row.
- Nothing else in either file changes. New test `T-V1102-RPT-04` in
  `tests/test_v1102_docs.py`: both edits present, exact strings.

## Constraints

- Offline throughout.
- `docs/spec/spec-v1.10.1.md`, `docs/reports/report-v1.10.1.md`,
  `docs/handoff-v1.10.1.md` are **never edited** (frozen history, NG-06).
- Never write a real secret value into `.env.example`, a test fixture or
  this repo's docs — placeholders only.
- Do not touch the README "pending (T9)" table or the release table's
  `v1.9.5`/`v1.10.2` rows — those are T6's.
- Do not touch `AGENTS.md`'s two count lines (~161-162, ~170) — T6's.
- If you find another EC-02-shaped gap (a test hardcoding something this
  brief's edits will break, beyond the one named in §3), **stop and
  report it** rather than silently fixing it — name the `file:line` and
  why for the orchestrator to record as a mid-run amendment.

## Acceptance

- `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table` green
  against the repointed `docs/spec/spec-v1.10.2.md`.
- `uv run --locked python devtools/checks.py doctor` and `... lint-docs`
  both green.
- Every id in this brief green (`T-V1102-GATE-03`, `T-V1102-RPT-01`,
  `T-V1102-CFG-01`, `T-V1102-RPT-02`'s T3 function, `T-V1102-RPT-03`'s T3
  function, `T-V1102-RPT-04`); full offline suite green (floor + all
  prior tasks' new tests + this task's, no test deleted).
- No `v1.10.2` row anywhere in README yet; no pending-table numbers
  filled yet; `AGENTS.md`'s two count lines untouched.
- One commit (`docs: v1.10.2 T3 -- ...` — this is paperwork, `docs:` not
  `feat:`, unless the `.env.example`/config edits make you judge
  otherwise — use your judgement on the conventional-commit type, but
  keep it one commit), with prompt file
  `docs/prompts/215-v1102-t3-...md`. Do not write the report's T3 section
  or `docs/llm-usage.md` row.

## Report back to the orchestrator

- Every README `file:line` you reworded for the "LM Studio is the
  default" sweep, plus any you considered and left (with why).
- The commit sha.
- Full pytest count (passed/skipped) after your changes.
- Anything this brief didn't anticipate (report, don't act beyond scope).
