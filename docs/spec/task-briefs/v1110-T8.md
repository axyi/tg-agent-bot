# Task brief — v1.11.0 T8, bump commit (version, docs, identity tests)

Spec: `docs/spec/spec-v1.11.0.md`, §13 (`VER`, lines 1505-1601), §14 T8
row. Repo: `/home/akh/aihome/coders-su/projects/tg-agent-bot`, branch
`main`, committed at `e55ab9c` (T0-T7, all 8 gates green at T7). Gates
1-4 are green on this tree.

**This brief covers only the bump commit** (§14.1 T8's `delegate: yes`
half). The orchestrator handles, separately and after this commit lands:
fresh gates 1-6 + gate 5, the gate 7/8 identity-reuse check, the node-id
floor check, `checks.py replay`, the evidence-only second commit, and the
local tag — none of that is your job here.

## What to build

### REQ-V1110-VER-01 — the bump itself, and where it lives

`pyproject.toml:3`: `1.10.4` → `1.11.0` (grep for the exact current line
first — do not assume the line number).

`uv lock` (online, the one network call this task is permitted —
EC-01's exhaustive list explicitly includes it): regenerates `uv.lock`
for the version literal only. Confirm afterward that `git diff -- uv.lock`
touches only this project's own entry (no dependency drift) — if it
touches anything else, stop and report rather than force it through.

New file `tests/test_v1110_ver.py`, four tests (exact names —
`T-V1110-INV-01`, landed at the orchestrator's own next step, will assert
these exist verbatim):

- `test_t_v1110_ver_01_live_version_is_1_11_0`: the live tree's
  `pyproject.toml` `project.version == "1.11.0"` (this is the ONE test
  that checks the *live* tree's version — the repointed
  `tests/test_v1104_version.py` test below checks a frozen *tag* blob
  instead, never the live tree).
- `test_t_v1110_ver_02_dependency_diff_version_only`: `git diff v1.10.4 --
  pyproject.toml uv.lock` is version-only under
  `devtools.agent_eval.dependency_diff_is_version_only`; the dependency
  lists equal the `v1.10.4` tag blob's. **Structural regression check
  (EC-02 carve-out)** — it may be green on first execution; record the
  initial result honestly, don't fabricate a red-first run.
- `test_t_v1110_ver_03_agents_md_and_release_row`: `AGENTS.md` carries
  the T8-measured test count and `152 entries`, both dated `as of
  spec-v1.11.0 T8`, and no longer `2311`/`144 entries` (whichever count
  was current before this task's own edits — check what
  `tests/test_v190_agents.py`'s existing pin currently asserts, since
  that's the "old" figure this test's negative check should exclude);
  README's release table has a `v1.11.0 | 1.11.0` row and the `v1.10.4`
  row's `; this release` clause is dropped.
- `test_t_v1110_ver_04_readme_deliverables`: README carries the
  `/documents` sample block [T2's work, already landed], a `## Sessions`
  section [T3], the `## Switch provider` paragraph naming
  `LMSTUDIO_MODELS`/`OPENROUTER_MODELS` [T4], the ingest diagram line
  reading `embed (batched, OpenRouter by default)` [T5], the batch-size
  paragraph naming both `documents.EMBED_BATCH_SIZE` and
  `llm.embeddings.BATCH_SIZE` [T5], the Limits row mentioning indexing
  running in a worker thread [T6]; `AGENTS.md:274-279`'s waiver paragraph
  carries the exact literal substring `"v1.11.0 changes nothing
  token-bearing; the rule does not fire."` [T6]. **All of these are
  already landed by T1-T6** — this test is a regression check confirming
  they're still there, not new work; it may also be green on first
  execution (same EC-02 carve-out as `-02` above).

New file `tests/test_v1110_inventory.py`,
`test_every_spec_test_function_exists` (`T-V1110-INV-01`, structural,
EC-02 carve-out): a frozen list of **all 61 other** `module::function`
pairs from §11.3's test table (every `T-V1110-*` id this whole release
defines, from T1 through T7 — you'll need to enumerate them; grep each
`tests/test_v1110_*.py` file for its `def test_t_v1110_*` functions and
cross-reference against the spec's own table if you want to double-check
completeness, but the authoritative source is "what actually landed",
not a re-transcription of the spec table) plus your own four VER tests
= 65 total function references the file's own docstring should note, but
the *list itself* the code checks holds the other 61 pairs (not counting
itself), length 61, no duplicate function name (per EC-03(a)'s exact
wording). For each pair: `importlib.import_module(module)` then
`hasattr(module, function)`.

**`tests/test_v1104_version.py` repointing** (PIN-01, not a new test) —
read this file yourself before touching it, it's short (79 lines):
- `test_t_v1104_ver_01_pyproject_version_is_1_10_4_v195_tag_frozen_at_1_9_5`
  (currently lines 40-53): its **live-tree read** (currently `with
  _PYPROJECT.open("rb") as handle: live_version = ...; assert
  live_version == "1.10.4"`, lines 41-43) becomes a **frozen tag-blob
  read** instead: `git show v1.10.4:pyproject.toml` parsed and asserted
  `== "1.10.4"` — the same pattern the function's own second half already
  uses for the `v1.9.5` tag. The `v1.9.5` assertion (lines 45-52) stays
  completely untouched. Function name stays exactly as-is (matches this
  file's own docstring convention: "the same permanent-historical-fact
  convention `tests/test_v194_version.py`/`tests/test_v195_version.py`
  already use for their own retired release stamps" — this function is
  now itself becoming one of those permanently-frozen historical markers).
- `test_t_v1104_ver_02_pyproject_and_uv_lock_diff_from_f3ce1a5_is_version_only`
  (currently lines 55-79): the spec text describes this as "kept as the
  historical fact it already is" and doesn't call out a change — **but
  its own trailing assertion, `assert live_version == "1.10.4"` (line
  78), reads the live tree**, and will fail once this task bumps the live
  version to `1.11.0`. Verify this yourself by reading the function
  before deciding, then treat it as a disclosed amendment (EC-02): update
  that one literal to `"1.11.0"` so the test keeps passing — the property
  it actually tests (the diff from `f3ce1a5` to the live tree is
  version-only, and a version delta exists) is completely unaffected by
  which specific version number is live; only the number needs to track
  releases. Record this in your commit body as a disclosed amendment,
  since the spec's own text didn't call it out explicitly.

**`tests/test_v190_agents.py:134-155`** (PIN-01, the existing
`test_t_v1104_rpt_03_agents_md_count_lines_landed_at_t5` — **rewritten in
place under its existing function name**, not renamed, not counted in
`T-V1110-INV-01`'s frozen list since `test_v1110_ver.py`'s own `-03` test
is the canonical one for this release): update its two literal
assertions and its negative-check line to the new T8 numbers (mirror
exactly how the docstring/assertions already show T5→T8 lineage across
releases — add one more history sentence following the existing pattern,
then update the assertions the same way line 155 already excludes the
*prior* release's stamp).

### REQ-V1110-VER-02/VER-04 — the remaining documentation

Nearly everything here is **already landed by T1-T6** (the brief's own
`test_t_v1110_ver_04...` above checks this). Your own new work in this
area is narrow: `README.md`'s `## Versioning` release table
(`README.md:981` onward, the row table starting around `:1000`) gains a
`v1.11.0` row (follow the exact style of the `v1.10.4` row — one sentence
summarizing the release, model under test, judge, gate 8 result, ending
`; this release`) and the `v1.10.4` row **drops** its own `; this
release` clause (it's no longer the current release). `AGENTS.md`: the
count lines (`:161-162`, `:172` — re-verify exact current line numbers)
get the new measured count and `152 entries`, both `as of spec-v1.11.0
T8`; `AGENTS.md:95`'s brief-path example `v1104-T<N>.md` → `v1110-T<N>.md`
(a minor illustrative-path update, not user-facing behavior).

## Measuring the final test count (do this last, after every other edit)

`uv run --locked pytest --collect-only -q -o addopts="" | grep -c '::'`
on the fully-edited tree (after your new test files, the repointed
files, everything) — this is the number that goes into `AGENTS.md`,
`test_v190_agents.py`'s repointed pin, and `test_t_v1110_ver_03`'s own
assertion. It must be **≥ floor + 62** (floor = 2311, so ≥ 2373 — the
tree was already at 2379 before this task's own new tests, comfortably
clear).

## Constraints

Test-first where genuinely possible (`-01` and `test_v1104_version.py`'s
repointing can run red-then-green against the unbumped tree; `-02`/`-03`/
`-04`/`INV-01` are structural regression checks per EC-02's carve-out —
report their initial result honestly). Gates 1-4 must stay green. Do
**not** run gate 5, `mutation_check.py`, `rag_eval.py`, or `agent_eval.py`
— those are the orchestrator's job next, on the committed bump. Do
**not** run `ruff format` (only `ruff check .`). Only two secret values
exist in this repo's config (`config.py:351`,`:379`) — never print/quote/
commit either.

## Acceptance

`T-V1110-VER-01` and `T-V1110-VER-03` (the live-version and the
AGENTS.md/README checks) red before your edits land, green after —
confirm this genuinely, don't fabricate it. `T-V1110-VER-02`,
`T-V1110-VER-04` and `T-V1110-INV-01` green (structural, initial result
recorded honestly). `tests/test_v1104_version.py`'s two tests green
(both the repointed one and the disclosed-amendment fix to the other).
`tests/test_v190_agents.py`'s repointed pin green. Gates 1-4 green.
Final collected count ≥ 2373, recorded.

## Delegation / secrets / commit

Delegated (EC-04, brief `v1110-T8.md`). This is T8's **first** commit
only (the bump) — the orchestrator makes the second, evidence-only
commit separately after running fresh gates. When gates 1-4 are green
and your test-first records are accurate, commit it yourself:
- Stage: `pyproject.toml`, `uv.lock`, `tests/test_v1110_ver.py` (new),
  `tests/test_v1110_inventory.py` (new), `tests/test_v1104_version.py`,
  `tests/test_v190_agents.py`, `README.md`, `AGENTS.md`, this brief file.
- Write `docs/prompts/251-v1110-t8-version-bump.md` (check the actual
  next free prompt number first) following `docs/prompts/TEMPLATE.md`'s
  shape. Stage `T8`, REQ ids `REQ-V1110-VER-01`, `-02`, `-04`,
  `REQ-V1110-INV-01`-equivalent (EC-03(a)).
- Append one row to `docs/llm-usage.md` (check the actual next free row
  number first).
- Append a `## T8 — version bump` section to
  `docs/reports/report-v1.11.0.md`, replacing the `## T8 — not reached`
  placeholder — describe what you built, the measured final count, any
  disclosed amendments (the `test_v1104_version.py` fix above, at
  minimum), and an accurate test-first account.
- Run `uv run --locked python devtools/checks.py lint-docs` before
  committing.
- Commit message: conventional type (`feat:` or `chore:`, your call —
  this is a version bump, `chore:` may fit better), header ≤72 chars,
  body references the prompt file and the disclosed amendment. Never
  `--no-verify`.

Report back: commit hash, gate results, the measured final test count,
confirmation of the `test_v1104_version.py` disclosed amendment and why
it was necessary, and an accurate test-first account. Do not paste full
file contents back.
