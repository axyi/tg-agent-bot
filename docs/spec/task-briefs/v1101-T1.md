# Task brief v1101-T1

Spec: `docs/spec/spec-v1.10.1.md`, §3 `REQ-V1101-CFG-02`, §4 `REQ-V1101-EMB-01`,
`REQ-V1101-EMB-02`, §5 `REQ-V1101-G5-01`, `REQ-V1101-G5-02`, §10 (error-matrix
rows 1–5, 13), §11 `REQ-V1101-SEC-01`. Read those sections in full before
writing anything — this brief carries only the load-bearing bits already
resolved so you don't have to re-derive them, not a substitute for the spec
text.

## Goal

Route embeddings through OpenRouter with an optional bearer key, make
`describe()` provider-aware, and rework gate 5's two live probes so they
only test the providers the run configuration actually routes to. Write
tests first (red for the right reason), then the code.

## What to add, verbatim

`config.py`, module level (near the other module-level helpers, imported
by `llm/embeddings.py` — the existing dependency direction is `llm →
config`, so this import adds no cycle):

```python
def is_openrouter_url(url: str) -> bool:
    parts = urlsplit(url)
    return parts.scheme == "https" and (parts.hostname or "").casefold() == "openrouter.ai"
```

`Config` (`config.py:188-191`), one new field after `embedding_timeout_s`:
`embedding_api_key: str = ""`. In `load_config`, after the
`embedding_base_url` block (`:485-491`), set it to
`openrouter_api_key if is_openrouter_url(embedding_base_url) else ""` — the
already-registered secret (`config.py:361`) under a second attribute, no
new environment variable.

`llm/embeddings.py`: `EmbeddingsClient.__init__` (`:30-43`) gains a
keyword-only `api_key: str = ""` after `client`, stored as `self.api_key`.
`_post` (`:95-100`) passes `headers={"Authorization": f"Bearer
{self.api_key}"}` when `self.api_key` is non-empty, `headers=None`
otherwise — body and `timeout=` byte-unchanged (the existing positional
constructor calls in `tests/test_v190_embeddings.py` must stay green
unamended). `describe()` (`:45-46`) becomes:

```python
def describe(self) -> tuple[str, str]:
    if is_openrouter_url(self.base_url):
        return ("openrouter", self.model)
    return ("lmstudio", self.model)
```

(imported from `config`, never a second host test) — and the span
attribute `gen_ai.provider.name` (`:71`, today the literal `"lmstudio"`)
becomes `self.describe()[0]`. The key must appear in no exception message
(`:102`, `:107-127` use status codes and shapes only), no span attribute,
and not in `describe()` (SEC-01).

**The four `EmbeddingsClient(...)` construction sites** all pass
`api_key=cfg.embedding_api_key` — **only the first three are this task's
job**: `bot.py:1911-1917` (`_live_embeddings`), `bot.py:2093-2099`
(`main()`), `devtools/rag_eval.py:750-756`. The fourth (the gate-8 runner)
lands at T3 — do not touch `devtools/agent_eval.py`.

`bot.py`, `_live_lmstudio` (`:1862-1877`), **before** its existing
"not configured" skip (`:1863-1865`): add the route rule — SKIP, printing
`live: SKIP lmstudio (no route uses it)`, when `cfg.llm_provider !=
"lmstudio"` **and** none of `cfg.llm_summary_model`, `cfg.llm_rerank_model`,
`cfg.llm_eval_chat_model`, `cfg.llm_judge_model` starts with `lmstudio:` —
otherwise the probe runs exactly as it does today and must still be green
when it does (a deployment routing anything to LM Studio still fails hard
on an unreachable box).

`bot.py`, `_live_embeddings` (`:1880-1925`): **drop** the `GET
{embedding_base_url}/models` step (`:1901-1910`) entirely — OpenRouter's
embedding catalogue lives at `/embeddings/models`, not `/models`, and gate
5 no longer lists it. Keep, in order: the `rag_enabled` guard with its
fixed message (`:1895-1900`), the client built with
`api_key=cfg.embedding_api_key`, `embedder.embed(["selftest"])`
(`:1918-1921`), the `len(vectors) == 1` check (`:1922-1923`) — the client
itself already enforces `len(vector) == embedding_dim`
(`llm/embeddings.py:121-122`).

## Tests to write

New files `tests/test_v1101_config.py`, `tests/test_v1101_embeddings.py`:

- `T-V1101-CFG-01`, `-02`, `-04`: `is_openrouter_url` — exact host
  `https://openrouter.ai` true (with/without trailing slash, upper-case
  host too); `http://openrouter.ai/…`, `https://openrouter.ai.evil/…`,
  `https://notopenrouter.ai/…` false. `embedding_api_key` resolves to the
  registered `openrouter_api_key` only when `embedding_base_url` passes
  the helper, else `""` (an LM Studio embeddings URL resolves to `""`, the
  client then sends no header).
- `T-V1101-CFG-05` — the fresh-interpreter import smoke: a `subprocess`
  run of `python -c "import config; import llm.embeddings"` exits 0 (no
  package-initialisation cycle reaches the live preflight).
- `T-V1101-EC-02` — the storage preflight's own expression (from
  `REQ-V1101-EC-04`/REV-04 Stage 0 check 2), proved **offline** on a
  `tmp_path` database: opens the db, `storage.init_schema(conn,
  embedding_dim=…, embedding_model=…)`, then
  `storage.document_count_all(conn)` and `SELECT COUNT(*) FROM chunks`
  both zero → `db_empty=True`; a database with one indexed document →
  `db_empty=False`; a database bound to a **different** embedding
  pair (so `init_schema` itself raises the indexed-pair mismatch
  `ConfigError`, `storage.py:600-604`) → the same expression still reports
  `db_empty=False` (never an unhandled exception).
- `T-V1101-EMB-01…04`: the header sent only when `api_key` non-empty
  (`headers=None` otherwise, positional-call compatibility preserved); the
  key never appears in an `EmbeddingError` message; `BATCH_SIZE`, the
  retry loop and the parsing are byte-unchanged (diff the surrounding
  lines).
- `T-V1101-EMB-05A`: the three T1 constructor sites' source literally
  passes `api_key=cfg.embedding_api_key` (AST or source-text match — the
  literal expression is what's pinned, never a key value).
- `T-V1101-G5-01`, `-02`: `_live_lmstudio` SKIPs with the exact message
  when nothing routes to `lmstudio:` and `llm_provider != "lmstudio"`;
  still runs and can still FAIL when a routed purpose does name
  `lmstudio:` or `llm_provider == "lmstudio"`.
- Rename in `tests/test_v190_embeddings.py`: `:381-389` →
  `test_t_v1101_g5_02_no_models_listing_is_issued` (a `/models` endpoint
  answering 500 no longer matters — the probe must still print `live: OK
  embeddings`); `:404-409` → `test_t_v1101_g5_02_embeddings_endpoint_http_error_fails`
  (`/embeddings` answering 500 → `live: FAIL embeddings`). These are the
  **only** edits EC-02 permits inside `tests/test_v190_embeddings.py` —
  touch nothing else in that file.
- `T-V1101-ERR-01` rows 1–2 (401/403 → `EmbeddingError("embeddings http
  401")`; vector-length mismatch → `EmbeddingError("embeddings dimension N
  != M")`) and row 13 (the `ConfigError` case above, `db_empty=False`, no
  exception text or path printed).
- `T-V1101-SEC-01`: the header built inside `_post` from `self.api_key`
  and never logged; `describe()` returns provider and model only.

`tests/test_v1_guardrails.py:1425-1445` is **re-checked, not edited** —
confirm it stays green unamended; a red there is a defect in your change,
never a licence to edit it.

## Constraints

- Touch only: `config.py` (imports, `Config`, `load_config`,
  `is_openrouter_url`), `llm/embeddings.py`, `bot.py:1862-1925`,
  `bot.py:2088-2100`, `devtools/rag_eval.py:750-756`, the two new test
  files, and the two renamed tests inside `tests/test_v190_embeddings.py`.
  Do not touch `devtools/agent_eval.py`, `devtools/checks.py`,
  `agent.py`, `tools.py`, `evals/`, `pyproject.toml`, or any `v1101-*`
  mutation entry (those belong to T2/T3/T4/T6/T7).
- Run gates **1–4 offline first** (`uv sync --locked`, `ruff check .`,
  `pytest`, `bot.py --selftest`) — all green before touching anything
  live. **Then, and only then**, run gate 5
  (`uv run --locked python bot.py --selftest-live`) and gate 7 (`uv run
  --locked python devtools/rag_eval.py`) **live, in sequence** (nothing
  else concurrent) — both must exit 0: gate 5 with `live: OK embeddings`
  and `live: SKIP lmstudio (no route uses it)`; gate 7's `hybrid` recall@5
  at or above its floor. **Do not run gate 6 or gate 8** — not your job
  this task. If gate 7's `recall@5` comes back red on the new embedder,
  **stop and report back** rather than attempting an embedder switch
  yourself — that decision (REV-04 Stage B″) belongs to the orchestrator.
- `.env` is never read directly — only through `load_config()`. Never
  print, log or commit a secret value; only `bool(...)` and `describe()`
  pairs are safe to print.
- One commit at the end, conventional-commit message, referencing
  `docs/prompts/204-v1101-t1-cfg-embeddings-gate5.md` (create this prompt
  file yourself, in the shape of `docs/prompts/TEMPLATE.md`, Stage T1, if
  the orchestrator has not already created it).
- `--no-verify` is never used.

## Report back

A short summary: what changed (file:line), the new/renamed test names and
counts, gates 1–4 exit codes, gate 5's full printed line-by-line output,
gate 7's exit code and `recall@5` figure with wall time. Do not paste full
file contents back — name file:line ranges only.
