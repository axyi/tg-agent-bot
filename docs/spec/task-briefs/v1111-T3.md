# Brief v1111-T3 — TST-01/-02, delegated

Spec: `docs/spec/spec-v1.11.1.md:404-475` (REQ-V1111-TST-01, -02), test
table rows `:689-696` (`T-V1111-TST-01…08`). Base tree: `2431034` (tag
`v1.11.0`), current `HEAD` has T0+T1+T2 landed. Read the cited spec
ranges yourself — this brief points, it does not copy.

**Test-first (EC-02)**: write `tests/test_v1111_tst.py` first (per spec
`:689-696`'s exact names/assertions), watch it fail for the right
reason against the pre-change tree, genuinely test-first if you can
manage the ordering (T2 managed it; T1 did not and needed a whole
follow-up commit — see `docs/prompts/258-v1111-t1-followup.md`).

**TST-01**: six new tests in `tests/test_v1111_tst.py`, one per
v1.11.0 ERR-01 row 11–16 (the table at spec `:417-424`), each driving
`bot.process_update` through `tests/test_v1110_err.py:81-109`'s
`process`/`text_update` helpers with a hand-written update dict.
Byte-equal reply assertion **and** the string present in README's
`## Error behaviour` section (`_error_section()`, `:112-116` region).
The existing `tests/test_v1110_err.py:119`'s frozen function stays
byte-unchanged — do not touch that file, add a new module instead.

**TST-02**: `tests/conftest.py` gains a context manager
`secrets_registry_snapshot()` (save `set(config._secrets)`, yield,
clear+update the *same* set object — never rebind it) and an
**autouse** fixture `restore_secrets_registry` that wraps every test in
it, ordered after the existing `no_real_bind` fixture
(`tests/conftest.py:37` ±5). Run the spec's own `[[VERIFY]]` grep first:
`grep -n "_secrets" config.py tests/conftest.py tests/test_v11_patch.py`
— if the registry is under a different name or is rebound rather than
mutated in place, follow the decision rule at spec `:444-448` (snapshot
the actual object, record `cited → actual` in this brief) rather than
guessing. **No test file other than `tests/conftest.py` changes for
this item** — do not hunt for "the leaking test", the fixture makes
ordering irrelevant.

**TST-07** (`tests/test_v1111_tst.py`): a nested-pytest-run test proving
the autouse fixture itself, not just the context manager — a two-test
module written to `tmp_path` (test A registers a sentinel and doesn't
clean up; test B asserts it's absent from `config._secrets` and the
registry `is` the same object) plus a `tmp_path/conftest.py` that
imports the project's fixture (`from tests.conftest import
restore_secrets_registry`) without ever calling
`secrets_registry_snapshot()` itself. Run the spec's second
`[[VERIFY]]` (`:456-464`) to confirm `tests` is importable as a package
this way; if neither the plain import nor the `pytest_plugins =
("tests.conftest",)` fallback works, fall back to the two permanent
ordered tests under `@pytest.mark.xdist_group("secrets_registry")` /
`--dist loadgroup`, disclosed as an amendment exactly as the spec's
fallback clause describes. Assert the nested `subprocess.run([...,
"-m", "pytest", "-p", "no:cacheprovider", "-p", "no:xdist", "-q",
str(tmp_path)])` exits 0.

**TST-08** (by command, not a function — record in this task's report
section): (1) `uv run --locked pytest
"tests/test_v1103_red_team.py::test_t_v1103_rt_06_leak_shape_fixture_still_fails_on_clause_c_only"
-o addopts=""` alone → must pass; (2) `uv run --locked pytest -p xdist
-n 4 -o addopts="" -q` run **twice** → both green — quote both summary
lines in the report.

**Commit + gates**: one commit, prompt 260. Before this commit's own
gates, record `ebc2822`'s own `gitleaks-tree` result (already scanned
by the orchestrator, exit 0, no leaks) as a new row in T2's
`gitleaks-tree` table in `docs/reports/report-v1.11.1.md` (follow T0's
table shape). Fill in T3's own report section (replace `## T3 — not
reached: ...`), a **single-physical-line** delegation-record bullet
(`- T3 | delegated: yes | to: ... | brief:
docs/spec/task-briefs/v1111-T3.md | map vs actual: ...`), and a
`docs/llm-usage.md` row (check the last row number first). Write
`docs/prompts/260-v1111-t3-tst.md` using `docs/prompts/TEMPLATE.md`'s
shape. **Prompt numbering is now fully sequential-by-next-free-integer,
not the static per-task map §10 originally laid out** (256=T0, 257=T1,
258=T2, 259=T3, 260+261=T4, 262=T5, 263=T6) — that map broke once T0's
resume/follow-up took 264/265 out of order and T1's follow-up took
258. Current state on disk: 256-260 used or about to be (256 T0a, 257
T1a, 258 T1-followup, 259 T2, **260 T3 — this task**), 261-263 free,
264-265 already used (T0-resume, T0-followup). **Disclose this
renumbering in the commit body**: T4 now takes 261+262 (its two
commits), T5 takes 263, and T6 must skip 264/265 (already used) and
take **266** — flag this forward in your report/usage-row text so
whoever writes `v1111-T4.md`'s brief doesn't collide. Run gates 1-4 and
`devtools/checks.py lint-docs`,
all green, before committing. Do not run the standalone `gitleaks-tree`
block yourself.

**Before committing, self-check with `advisor`** (or at minimum
re-verify every line-range citation, pass/fail count, and ordering
claim against the actual tree/actual command output) before writing it
into the report/usage row — T2's clean pass is the model to repeat,
T1's messy one is what to avoid.

**Constraints**: never open/print/grep `.env`, `data/`, `bot.db`,
`sandbox/`, `exec_audit.jsonl`; no LAN address or secret literal
anywhere (careful here — this task is literally about the secrets
registry; use only the fake `VALUE-abcdefgh12`-style sentinel already
established in this repo's tests, never a credential-shaped real
value); zero new dependencies. Commit message: conventional type,
≤72-char header, body names the prompt file, ends with the same
Co-Authored-By/Claude-Session trailer as `git log -3`. `--no-verify`
never.

Return a summary: files touched, all new tests' pass/fail, TST-08's two
command results, gate outputs, commit sha, and anything your self-check
caught. Never paste full file contents.
