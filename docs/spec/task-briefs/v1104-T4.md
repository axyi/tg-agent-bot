# Task brief v1104-T4 — the localized `mutation-v1103` calibration hunk (GATE-02 step (1))

Repo: `/home/akh/aihome/coders-su/projects/tg-agent-bot`. Spec:
`docs/spec/spec-v1.10.4.md` §7 (`REQ-V1104-GATE-02`, lines **558-609**).
Report: `docs/reports/report-v1.10.4.md` — do **not** touch it; the
orchestrator writes T4's report section itself after your commit (this
task's report bullet needs the calibration line plus the gate-6 tree
proof, which only the orchestrator has, since it runs gate 6 directly in
the main context after your commit).

Base commit: `2caa595` (T3, on `main`). This is the **only** thing you
change this task — one localized YAML hunk, nothing else.

## The measured fact (already resolved — do not re-run the calibration)

The orchestrator already ran `uv run --locked python
devtools/mutation_check.py --select v1103-` once, directly, on the
committed `2caa595` tree: **5/5 killed, 0 survived, 0 errored, 0
drifted, wall = 16s** (measured with `date +%s` bracketing, real
elapsed). Formula (`REQ-V1104-GATE-02`): `timeout_seconds` = wall × 2 +
70s, rounded up to the nearest 10s = `16 × 2 + 70 = 102` → **110**
(unchanged from the placeholder — that is expected and fine; the rule
requires the dated comment to land even when the computed value equals
110, per ERR-01 row 7).

## The exact edit

In `config/quality_gates.yaml`, the `mutation-v1103` gate block (find it
by searching for `mutation-v1103:` — it currently sits right after
`mutation-v1102`'s block and right before the `mutation-all` block).
Its **comment block immediately above** the `mutation-v1103:` key
currently reads (verify this is still the exact text before editing —
if it drifted, that's a disclosed EC-02 amendment, report it back, don't
guess):

```yaml
  # spec-v1.10.3 T6 (docs/spec/task-briefs/v1103-T6.md, REQ-V1103-GATE-02):
  # five v1103-* entries added (144 total in MUTATIONS -- see mutation-all's
  # own dated comment below). Same bootstrapping constraint as
  # mutation-v1100's/mutation-v1101's/mutation-v1102's comments above: this
  # task's own per-entry sanity checks (mutate -> run the one targeted test
  # -> revert, never the CLI's mutate -> run -> revert machinery over the
  # whole list or under --select "v1103-") confirmed each of the five
  # entries killed by its expected test, but did not time the five as a
  # group under the real runner.
  # calibration pending, orchestrator fills this: timeout_seconds below is
  # a placeholder matching v1102's value until the orchestrator runs the
  # one-time `--select v1103-` calibration (wall x 2 + 70s, rounded up to
  # 10s) and commits the real number separately.
```

Replace **only** the last paragraph (the two lines starting `# calibration
pending, orchestrator fills this:` through `... and commits the real
number separately.`) with, in the exact style of `mutation-v1102`'s own
dated calibration paragraph (`config/quality_gates.yaml`, the block right
above `mutation-v1102:` — read it for the precedent shape before you
write this):

```yaml
  # spec-v1.10.4 T4 (orchestrator, commands only, on the committed tree,
  # immediately after commit 2caa595): the `--select v1103-` calibration
  # run measured 5/5 killed, real=16s -> 2x 16s + 70s (survivor-safe
  # rule) = 102s, rounded up to 110s. This is the calibration run, not a
  # gate-6 acceptance run -- the five entries' proof is the full
  # `mutation-all` run that includes them.
```

Keep the first paragraph (`# spec-v1.10.3 T6 (docs/spec/task-briefs/v1103-T6.md, ...` through `... did not time the five as a group under the real runner.`) **unchanged** — that's history, not the placeholder.

`timeout_seconds: 110` under `mutation-v1103:` stays **unchanged** (the
computed value equals the existing value — do not delete or reformat
that line, just confirm it already reads `110`).

**Nothing else in the file changes.** Do not touch `mutation-all`'s
block, `mutation-subsets`, any other gate, or any other file. This is
"exactly one localized YAML hunk" (`REQ-V1104-GATE-02`'s own words) —
treat any temptation to also fix something else you notice as out of
scope; disclose it in your report instead.

## Acceptance

- `git diff 2caa595 -- config/quality_gates.yaml` shows exactly one hunk,
  touching only the comment paragraph above `mutation-v1103:` (the
  `timeout_seconds: 110` line unchanged, byte-identical before and
  after).
- `uv run --locked ruff check .` clean (this file isn't Python, but run
  it anyway per gate discipline — should be a no-op).
- Do **NOT** run gate 6 (`devtools/mutation_check.py`, full or any
  `--select`) — the orchestrator runs that directly in the main context
  immediately after your commit, on a tree it write-trees itself; your
  job is only the yaml edit.
- Do **NOT** run `bot.py --selftest-live`, `devtools/rag_eval.py`, or
  `devtools/agent_eval.py` this task.
- `uv run --locked pytest -q` full suite green (this edit touches no
  test, so this should be a no-op check).

## Delegation record

Report back the exact bullet for this task's report entry (the
orchestrator will insert it into the T4 section itself, alongside its
own commands-only bullet — per EC-03, T4's report carries **two**
bullets, one `yes` for your hunk, one `no` for the commands-only rest):
`- T4 | delegated: yes | to: <your agent identity> | brief:
docs/spec/task-briefs/v1104-T4.md | map vs actual: <matches, or names
what drifted>`

## Commit

One commit, this task's yaml hunk only. Conventional commit header (e.g.
"feat: v1.10.4 T4 -- mutation-v1103 calibration comment"), body
referencing `(prompt: docs/prompts/232-v1104-t4-calibration-hunk.md)`,
same `Co-Authored-By`/`Claude-Session` trailer as recent commits (check
`git log -1 --format=%B` on `2caa595` for the exact text). Do not add or
modify `docs/llm-usage.md` or `docs/reports/report-v1.10.4.md` — the
orchestrator does the bookkeeping for the whole of T4 in one pass after
your commit and after its own gate-6/live-gate work.
