# Task brief v1100-T6

Spec: `docs/spec/spec-v1.10.0.md`, §13 (`REQ-V1100-GATE-03`,
`REQ-V1100-EVAL-01`), §14 (`REQ-V1100-RPT-01`). Read both in full.

## Goal

Register gate 8 (`agent-eval`) in `config/quality_gates.yaml`, repoint the
gate-matrix test and `lint-docs`'s `report_path` at this release, and land
the eight-gate documentation blocks in `AGENTS.md` and `README.md` (numbers
table as placeholders — T10 fills the real numbers from T9's run).

## 1. `config/quality_gates.yaml`

**Add the `agent-eval` gate**, immediately after the `rag-eval` gate block
(`config/quality_gates.yaml:240-248`), same key set and shape:

```yaml
  agent-eval:
    kind: command
    result_mode: exit_status
    argv: [uv, run, --locked, python, devtools/agent_eval.py]
    placeholders: {}
    success_exit_codes: [0]
    blocking: true
    diff_scoped: false
    timeout_seconds: 8200
```

(`timeout_seconds: 8200` — T0 measured `t_turn = 24.90s` on the production
route and computed `ceil_to_100(1.5 * 219 * 24.90) = 8200` via
`gate8_timeout_seconds(219, 24.90)`; add a one-line dated comment above the
gate recording this, in the voice of the `rag-eval` gate's own preceding
comment block at `:236-239`.)

**Add `"agent-eval"` to the `full` profile** (`config/quality_gates.yaml:15-17`),
immediately after `rag-eval` in that list — and to **no other** profile.

**Add `mutation-v1100`** to the `mutation-subsets` list
(`config/quality_gates.yaml:28-29`), after `mutation-v190`. (The gate entry
itself for `mutation-v1100` is T7's job, not yours — T7 needs the
mutation-subsets membership to already exist so `_validate_profiles`
doesn't orphan it the moment T7 adds the gate, so add the membership now,
even though the gate definition lands later. If `devtools/checks.py`'s
`_validate_profiles` fails on this in the meantime because the gate
doesn't exist yet, that's fine — pytest doesn't call `_validate_profiles`
at import time on every test run; if it does turn out to break something,
report it rather than defining the gate yourself — that's out of scope for
this task.)

## 2. The gate-matrix test — `tests/test_v15_standards.py:1772-1834`

Repoint `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table`'s
source file: it currently reads `docs/spec/spec-v1.9.0-delta-1.md`
(`:1819`) — change that one line to read
`docs/spec/spec-v1.10.0.md` instead. **Do not edit
`spec-v1.9.0-delta-1.md`** — it stays as it is, historical.

Add two entries to `_GATE_MATRIX_LABEL_TO_NAME` (`:1772-1799`):
- `` "`agent_eval.py`": "agent-eval", `` — immediately after the
  `` "`rag_eval.py`": "rag-eval", `` entry.
- `` "`mutation_check.py --select v1100-`": "mutation-v1100", `` —
  immediately after the `` "`mutation_check.py --select v190-`":
  "mutation-v190", `` entry.

`docs/spec/spec-v1.10.0.md` already has (§13, `REQ-V1100-GATE-03`) the full
27-row gate matrix table with `agent_eval.py` and
`mutation_check.py --select v1100-` rows included — this test will parse
that table directly once repointed, so as long as the yaml's `full`/
`pre-push`/`pre-commit` membership matches what that table says, the test
passes. Read the table in the spec (§13) to confirm your yaml edits agree
with it exactly (in particular: `agent_eval.py` is `yes` only under
`full`, not `pre-commit`/`pre-push`).

## 3. `lint-docs`'s `report_path` — `config/quality_gates.yaml:~628`

Repoint `report_path: docs/reports/report-v1.9.5.md` →
`docs/reports/report-v1.10.0.md`. `ledger_header` stays byte-unchanged.

Amend `tests/test_v170_bench.py:316-331`
(`test_t_v195_rpt_01_lint_docs_repointed_to_this_release`): rename to
`test_t_v1100_rpt_01_lint_docs_repointed_to_this_release`, change the
asserted path to `docs/reports/report-v1.10.0.md`; the `ledger_header`
assertion literal is unchanged.

## 4. `AGENTS.md` — the eight-gate block

`AGENTS.md:150-158` (the fenced gate-commands block under "Gates — run
before reporting success") gains an eighth line:
```
uv run --locked python devtools/agent_eval.py
```
after the `rag_eval.py` line. **Do not touch the count-bearing lines**
(`:160`, `:169` — the "1638 tests"/mutation-entry-count prose) — those are
T10's job, measured after every other edit lands. Do not touch the gate
5/6/7 prose paragraphs describing what makes each gate red — that's this
release's §13 prose, already written into the spec; a short new sentence
about gate 8 in the same voice as the existing gate 5/6/7 sentences is fine
to add here (one sentence: "Gate 8 is the agent evaluation gate
(`devtools/agent_eval.py`) — red on a red-team category below its floor,
the judge mean below 0.8, or an environment failure.").

## 5. `README.md`

**`## Tests`** (`README.md:976-986`): the same eighth line added to its
fenced block, plus one sentence noting gate 8 needs the live environment
too (mirror the existing "gates 5 and 7 need the live environment" sentence
at `:987`, extending it to name gate 8).

**New `## Agent evaluation (gate 8)` section**, inserted between
`### Evaluation`'s end (`README.md:505-534`, the RAG evaluation section)
and `## Add a skill` (`:535`). Content (§14 `REQ-V1100-RPT-04` describes
what belongs here — read it):
- what the runner checks: three short paragraphs — level 1 (offline
  contract tests, no live LLM), level 2 (the twelve-case red-team dataset:
  5 injection / 4 hallucination / 3 memory, with their floors 5/5, ≥3/4,
  3/3), level 3 (the five-question LLM-judge with a 0.8 mean floor, plus
  the advisory latency SLA)
- the judge route: `LLM_JUDGE_MODEL`, default
  `openrouter:openai/gpt-4.1` — always a separate, stronger model, never
  the chat model under test; the runner verifies only that it is
  **different**, never that it is "stronger" (that's an operator
  responsibility); a `go`-request line overrides `.env`
- how to run: `` `uv run --locked python devtools/agent_eval.py` ``,
  `--select <category|id-prefix>` for development
- the exit codes: 0 = pass, 1 = a blocking metric failed, 2 =
  environment/infrastructure
- **a numbers table, as a placeholder** (T10 fills it from T9's live run):

```
| metric | result |
|---|---|
| injection | pending (T9) |
| hallucination | pending (T9) |
| memory | pending (T9) |
| judge mean | pending (T9) |
| latency (advisory) | pending (T9) |
```

## Constraints

- Touch: `config/quality_gates.yaml`, `tests/test_v15_standards.py`,
  `tests/test_v170_bench.py`, `AGENTS.md`, `README.md`, and a new
  `tests/test_v1100_gates.py` (for `T-V1100-EVAL-01`, `-02`, `-03` — see
  below). Do not touch `devtools/agent_eval.py`, `devtools/mutation_check.py`,
  or `devtools/checks.py`.
- New file `tests/test_v1100_gates.py`:
  - `T-V1100-EVAL-01`: `config/quality_gates.yaml`'s `agent-eval` entry has
    exactly the key set/values above (`argv` flag-free), is in `full` and
    no other profile; `mutation-v1100` is in `mutation-subsets` only;
    `agent-eval.timeout_seconds` is a multiple of 100 and ≥ 1800; assert
    `set(devtools.agent_eval.GATE8_DEPENDENCIES)` equals exactly the union
    of every root `*.py` (glob it) plus `{"llm/", "devtools/agent_eval.py",
    "evals/agent/", "config/quality_gates.yaml", "pyproject.toml",
    "uv.lock"}`, every entry exists in the repo; `main(["--print-dependencies"])`
    prints exactly those entries one per line, returns 0, calls neither
    `load_config` nor the injected `run_agent_outcome` (monkeypatch both to
    raise and confirm they're never called).
  - `T-V1100-EVAL-02`: `AGENTS.md` and `README.md` each carry the
    eight-gate fenced block verbatim (line-by-line equality), and README
    has the heading `## Agent evaluation (gate 8)`.
  - `T-V1100-EVAL-03`: `worst_case_calls`/`gate8_timeout_seconds` (T5's,
    already implemented) against the spec's worked examples:
    `worst_case_calls(r) == 23*r + 12` for `r in (1, 8, 9)`,
    `worst_case_calls(agent.HTTP_ATTEMPT_LIMIT) == 219`;
    `gate8_timeout_seconds(219, 1.0) == 1800`, `(35, 10.0) == 1800`,
    `(219, 10.0) == 3300`, `(219, 100.0) == 32900`, `(219, 1000.0) == 328500`
    — always a multiple of 100 and ≥ `ceil(1.5 * max_calls * t_turn)`.
- Run gates **1-4 only**, plus `uv run --locked python devtools/checks.py doctor`
  and `uv run --locked python devtools/checks.py lint-docs` (both must be
  green — `doctor` checks tool pins/hooks, `lint-docs` checks the T0
  skeleton's format against the newly repointed `report_path`). Never run
  gate 5, 6, 7, 8, and never `checks.py run --profile full` (that would
  execute gate 8, which doesn't belong to this task and isn't ready to run
  live yet).
- `--no-verify` never used.
- One commit referencing `docs/prompts/198-v1100-t6-gate-registration.md`.

## Report back

Confirm `_validate_profiles` doesn't choke (run
`uv run --locked python devtools/checks.py doctor` and paste its result),
confirm the gate-matrix test passes against the repointed spec file,
confirm `lint-docs` is green, test count added, gate 1-4 results, commit
hash.
