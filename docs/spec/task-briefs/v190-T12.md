# Task brief — v1.9.0 T12 (the version bump)

Read `docs/spec/spec-v1.9.0.md` §13 REQ-V190-VER-01 (lines 1741-1749) and
RPT-02 through RPT-04 (lines 1757-1811) directly for full context. This
brief carries the resolved facts you need; do not re-derive them.

## Resolved facts

- `<base>` = `d6c13124d8108d6ca23900b91ef30d27d95fc6fc`.
- Final test count, measured directly via `pytest --collect-only -q`:
  **1559**. This is the number for `AGENTS.md:146`.
- Final mutation count: **105** (98 pre-v1.9.0 + 7 new `v190-*` entries,
  confirmed 105/105 killed at T9 and reconfirmed at T11's full gate run).
  This is the number for `AGENTS.md:155`.
- `docs/llm-usage.md`'s last row is **67** — this run's usage rows append
  starting at **68** (per the report's own disclosed resolution of the
  spec's stale "from 67" citation — the file's actual state is
  authoritative, see `docs/reports/report-v1.9.0.md`'s dedicated section
  on this).
- Operator's embedding pair: `EMBEDDING_MODEL=text-embedding-nomic-embed-text-v1.5`,
  `EMBEDDING_DIM=768`.
- T8's eval numbers (recall@5=1.000/1.000/1.000, MRR 1.000/1.000/0.950,
  page hit-rate 1.000 on all three modes) and gate 7's known-red status
  (genuine model-latency stochasticity on this deployment, not a code
  defect) are already fully documented in `docs/reports/report-v1.9.0.md`
  §T8 and in `README.md`'s Evaluation subsection (T10's work) — just cite
  them, don't re-derive.
- T11's benchmark delta (`docs/reports/bench-v190.md`, hand-assembled
  since neither run completed cleanly) is already written.
- `<implementation-tip>` **is this task's own commit** — REQ-V190-REV-02:
  "T12 lands VER-01's bump and a provisional `report-v1.9.0.md` (RPT-02
  minus item 5's tip SHA); that commit's SHA **is**
  `<implementation-tip>`." Do not put this commit's own SHA inside the
  report — that's the "no self-referential SHA" acceptance criterion.

## The version bump (REQ-V190-VER-01)

`pyproject.toml`'s `project.version`: `"1.8.0"` → `"1.9.0"`. **Nowhere
else bumps it before this task** (T0–T11 never touched this line — verify
with `git log -p --all -- pyproject.toml | grep version` if you want to
confirm, but it should just be a clean one-line change here).

Write `T-V190-VER-01`: a test that is **red before your edit and green
after** — the standard pattern for this repo's version-pin tests (check
`tests/test_v180_version.py` for the exact style/pattern this repo already
uses for the equivalent v1.8.0 test, and follow it exactly — same
fixture/assertion shape, new version string).

`README.md`'s Versioning section (`README.md:614` region): add the 1.9.0
entry following the existing entries' exact format (check the existing
1.8.0/1.7.0 entries' one-line style and match it — a short phrase
summarizing the release, e.g. "RAG over user documents").

`AGENTS.md`'s version echo, plus its two count lines:
- Wherever `AGENTS.md` states the current version number in prose, update
  it to 1.9.0 (this is likely near the top, in a stack/overview
  paragraph — grep for "1.8.0" to find every occurrence and update each
  one that refers to the *current* release, not to a *historical*
  reference like "as of spec-v1.8.0 T10" which should stay as history).
- `:146`'s test count line → **1559** (find the exact current wording,
  e.g. "gate 3, pytest, is N tests as of spec-vX.Y.Z TN" and update both
  the number and the version/task tag to spec-v1.9.0's own).
- `:155`'s mutation count line → **105** entries (same treatment — find
  the current wording pattern and update the number, the "up from N"
  comparison if present, and the version/task tag).

Both of these two lines are explicitly **T12's job alone** — T9 and T10
each correctly left them untouched per their own briefs; this is the one
place they land.

## Reporting (REQ-V190-RPT-02, RPT-03, RPT-04)

**`docs/llm-usage.md`**: add rows for every prompt this run has produced
so far, starting at row **68**. List the prompt files in order (check
`docs/prompts/` for the exact filenames from 142 onward — 142 through
whatever the most recent one is, likely around 157-158 by now, plus this
task's own). For each row, follow this file's own established format
(one row per prompt, or bundle several into one row the way v1.8.0's
table did for its own `go`-run prompts — check the existing table's
style for rows 63-67, which bundle exactly this shape of information, and
match it). You do not have live token/cost data for most of these
prompts (this run's orchestrator is an interactive session, and per-task
subagents' own harness-reported aggregates would need to be pulled from
each task's own report section in `docs/reports/report-v1.9.0.md` if you
want that level of detail — reasonable to summarize as "subagents'
own harness-reported aggregates, see docs/reports/report-v1.9.0.md
for the per-task breakdown" rather than re-deriving exact numbers,
matching the precedent of row 64's own summary style for v1.8.0's go run).

**`docs/reports/report-v1.9.0.md`'s "Ledger row (paste into
`economics.md`)" section**: currently a provisional all-`TBD` fenced row
(landed at T10 as a `lint-docs` structural fix). Replace it with a
**structurally complete** row: `Ver` = `1.9.0`, and every other cell
filled with the real numbers now available (spec tokens — check the
handoff/report's own earlier "final: 70 MUST..." style counts if a token
figure isn't directly available, or state "not separately tracked" for
any cell this run's own harness genuinely cannot produce — do not invent
a number). Match the exact column order of `ledger_header` in
`config/quality_gates.yaml` (grep for `ledger_header` there to get the
literal header string). This is **provisional** in the sense that the
tip SHA cell (if the header has one — check) stays out per RPT-02 item
13/REV-02's "no self-referential SHA" rule; everything else should be
real, not `TBD`.

**`docs/reports/tg-post-v1.9.0.md`**: write it now, per RPT-03 — Russian,
under 1500 characters (quote the `wc -m` count), structure:
constraints → result → metrics (executor model named: `claude-sonnet-5`;
spec tokens, prompts, first-run, bugs, tokens in/out, cost — estimate at
public API prices where the harness doesn't expose exact tokens, marked
as an estimate) → link to `https://github.com/axyi/tg-agent-bot`. Mention
in one sentence that gate 7 (the live retrieval eval) is red on this
deployment due to a diagnosed model/environment characteristic (a
"thinking" chat model's unpredictable reasoning length), not a code
defect, and that retrieval itself measured recall@5=1.0 — this is honest,
not spin. Follow this repo's own `docs/reports/tg-post-v1.8.0.md` (or
whichever is the latest existing one) for the exact tone/structure
precedent.

## Task

Implement VER-01 end to end, test-first (`T-V190-VER-01`, confirmed
red-before via `git stash` or an equivalent before/after check — the
existing v1.8.0 test file shows the established pattern for proving this).
Files: `pyproject.toml` (`project.version` only), `README.md:614-647`,
`AGENTS.md:133-160` (plus wherever else the version number itself
appears — grep first), `docs/llm-usage.md`, `docs/reports/report-v1.9.0.md`
(Ledger row section only — do not touch any other section, those are the
orchestrator's own running record), new `docs/reports/tg-post-v1.9.0.md`,
new `tests/test_v190_version.py`.

## Acceptance

`T-V190-VER-01` proven red before your edit, green after (state exactly
how you proved "before" — `git stash`, a separate check, whatever is
cleanest). `checks.py lint-docs` green. Full `pytest`, `ruff check .`,
`bot.py --selftest` green. The report's Ledger row is a structurally
complete fenced row with no `TBD` cells (barring a cell this run's own
harness genuinely cannot produce, explicitly noted as such, never
invented) and **no self-referential SHA** — this commit's own sha does
not appear inside `report-v1.9.0.md`. Do not run `devtools/mutation_check.py`
or any live gate — this task is documentation plus one version-string
change, fully offline.

## Report back

A summary: files touched, the red-before/green-after proof for
`T-V190-VER-01`, the new `pytest --collect-only -q` total, gate results,
any deviation from this brief or spec §13 with a one-line reason, the
commit sha and its one-line message. Do not paste raw file content —
findings and `file:line` only.
