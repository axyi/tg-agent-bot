# Task brief v1103-T4 — paperwork: `.env.example`, README, AGENTS.md, the gate matrix (RPT-03's T4 part, GATE-03)

Repo: `/home/akh/aihome/coders-su/projects/tg-agent-bot`. Spec:
`docs/spec/spec-v1.10.3.md` §11 RPT-03 (lines 977-1015), §10 GATE-03
(lines 838-882), §3 INS-01 (lines 236-307, for exact wording). Report:
`docs/reports/report-v1.10.3.md` — append a `## T4 — paperwork` section,
replacing the `## T4 — not reached: T4` placeholder.

**Every value below has been pre-verified against the current file
content by the orchestrator** — the line numbers and existing text
quoted here are accurate as of this dispatch. If a file has drifted
since (should not happen — nothing else runs between T3 and T4), stop
and report rather than guessing.

## 1. `.env.example`

- Line 18: `OPENROUTER_MODEL=openai/gpt-4.1-mini` → `OPENROUTER_MODEL=openai/gpt-4.1`
- Line 101: `LLM_JUDGE_MODEL=openrouter:openai/gpt-4.1` → `LLM_JUDGE_MODEL=openrouter:anthropic/claude-sonnet-5`
- The judge comment at lines 98-100 stays unchanged in substance (still
  correct: "always a separate, stronger model, never the chat model
  under test").
- **Key names and structure only — no new line, no value ever printed
  elsewhere.** This pin holds even though INS-01 has a documented
  fallback (`openrouter:openai/gpt-5.6-sol`) — `.env.example` always
  shows the shipped Anthropic default, never the fallback, regardless
  of which one a given run actually used.

## 2. `README.md`

- **Judge paragraph** (currently lines 565-566): `The judge route is
  LLM_JUDGE_MODEL (default openrouter:openai/gpt-4.1, resolved the
  same way…` → change `openrouter:openai/gpt-4.1` to
  `openrouter:anthropic/claude-sonnet-5`. Keep the rest of the
  paragraph's substance (the "always a separate, stronger model…" and
  the `describe() != describe()` explanation) — this is a value swap,
  not a rewrite. You may add one clause noting the new default is from
  a different vendor than the shipped chat model, if it reads naturally
  — don't force it.
- **`## Switch provider`** (currently lines 237-260): this section
  currently does NOT name a specific `OPENROUTER_MODEL` value at all
  (it only says `LLM_PROVIDER=openrouter` needs `OPENROUTER_API_KEY`
  and `OPENROUTER_MODEL`, generically). Add a sentence naming
  `OPENROUTER_MODEL=openai/gpt-4.1` as this release's shipped default,
  plus one sentence on how to revert to the smaller/cheaper
  `openai/gpt-4.1-mini` (the pre-v1.10.3 default) — place it naturally
  in that section, near the existing `OPENROUTER_MODEL` mention.
- **Release table** (currently ends at line 911, the `v1.10.1` row).
  After line 911, insert exactly this new row (byte-for-byte, this is
  VER-01's pinned text):
  ```
  | v1.10.2 | — | run stopped at T5 by the stop route, gate 8 red on model behaviour (injection 4/5 on openai/gpt-4.1-mini — INJ-04's developer-mode pretext produced exec("printenv") ×3 before a refusal; the Secrets: line held the other four), not tagged; ships with v1.10.3 |
  ```
  **Do NOT add the `v1.10.3` row yet** — that lands at T7 only, after
  gate 8 is actually green. Adding it now would be a false claim (T6
  hasn't run).

## 3. `AGENTS.md`

- Line 95 (inside the "Brief by file, never by retyping" paragraph):
  `docs/spec/task-briefs/v1102-T<N>.md` → `docs/spec/task-briefs/v1103-T<N>.md`.
- Waiver paragraph (currently lines 274-278, ending "…a fresh
  OpenRouter baseline is a candidate for a later release."): **keep
  every existing sentence verbatim** (the v1.10.1 and v1.10.2
  sentences), and append this new sentence, exact text:
  > v1.10.3 carries the waiver again: the model under test and the
  > judge changed, so no run is comparable to the LM Studio baseline.

## 4. `config/quality_gates.yaml` — GATE-03's matrix repoint (test-side, not the yaml itself)

The yaml's `lint-docs` block was already repointed at T3 (report_path
+ delegation_record) — you are not touching `config/quality_gates.yaml`
in this task. This task only repoints the TEST that parses the spec's
gate matrix:

- `tests/test_v15_standards.py:1793` — inside
  `_GATE_MATRIX_LABEL_TO_NAME` (currently lines 1772-1802), insert one
  new entry **immediately after** the `` "`mutation_check.py --select
  v1102-`": "mutation-v1102", `` line (currently line 1792), before the
  `` "`mutation_check.py` (all)": "mutation-all", `` line (currently
  line 1793):
  ```python
      "`mutation_check.py --select v1103-`": "mutation-v1103",
  ```
- `tests/test_v15_standards.py:1823` — inside
  `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table`, change
  `checks.REPO_ROOT / "docs" / "spec" / "spec-v1.10.2.md"` to
  `checks.REPO_ROOT / "docs" / "spec" / "spec-v1.10.3.md"`.

This makes that test parse the 29-row matrix table already present in
`spec-v1.10.3.md` (lines 849-879, verbatim, already written — you are
NOT adding or editing that table, it already exists in the spec).
Confirm the test goes green against the new target file, proving the
spec's own matrix table agrees with `config/quality_gates.yaml`'s
`profiles:` block as it stands after T0-T3 (no other yaml change is
made this task — if the matrix test fails for a reason other than the
missing `v1103-` label, STOP, do not "fix" the yaml to make the test
pass; report what mismatched instead — the shared repair budget is at
2 of 3 cycles spent).

## Non-goals

Do not touch `spec-v1.10.2.md` (frozen, NG-11). Do not add the
`v1.10.3` release-table row (T7's job). Do not touch the `pending (T9)`
rows in README (T7's job, `:592-596`). Do not touch AGENTS.md's count
lines (`:161-162`, `:172` — T7's job). Do not touch
`config/quality_gates.yaml` at all this task.

## Tests

- `tests/test_v1103_docs.py` — `T-V1103-CFG-01` (the `.env.example`
  values, read as text, never asserting a live config load — this is a
  static-content test), the T4-scoped functions of `T-V1103-RPT-02` and
  `T-V1103-RPT-03` (README/AGENTS.md content assertions — follow the
  existing style of `tests/test_v1102_docs.py` for how prior releases
  pinned this kind of content).
- `T-V1103-GATE-03` — put this one in `tests/test_v1103_gates.py`
  (create it if T3 didn't already — check first, T3's brief may have
  created it): asserts the new `mutation-v1103` label maps correctly
  and that `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table`
  is green against `spec-v1.10.3.md`.
- EC-02 rows this task owns (read `docs/spec/spec-v1.10.3.md:99-119`
  directly): `tests/test_v190_agents.py:94-95` (brief-path token
  `v1103-T<N>.md` present, `v1102-T<N>.md` absent, renamed
  `…is_v1103`), `tests/test_v1102_docs.py:131-132` (same token swap,
  renamed `…is_v1103`; `:130`'s v1.10.2 waiver sentence **unchanged**),
  `tests/test_v1102_docs.py:111` ("no `| v1.10.2 |` row" assertion
  becomes: the `v1.10.2` row equals VER-01's stopped-run text verbatim
  AND no `| v1.10.3 |` row exists yet — a stronger pin, same intent).
  Make exactly these edits, no others outside the table.

## Acceptance

- `uv run --locked ruff check .` clean.
- `uv run --locked pytest tests/test_v1103_docs.py tests/test_v15_standards.py -k gate_04 -q` green.
- `test_v15_gate_04_profile_matrix_agrees_with_the_spec_table` green
  against `spec-v1.10.3.md`; no `v1.10.3` release-table row exists yet;
  the v1.10.2 waiver sentence in `AGENTS.md` is intact verbatim.
- Full `uv run --locked pytest -q` green, count increased, nothing
  deleted.
- Gates 1-4 only.

## Delegation record

`- T4 | delegated: yes | to: <your agent identity> | brief:
docs/spec/task-briefs/v1103-T4.md | map vs actual: <matches the reading
map below, or names what you had to read beyond it>`

## Reading map (§13.1)

`.env.example:7-20`, `:96-101`; `README.md:47-59`, `:237-257`,
`:563-572`, `:898-911`; `AGENTS.md:92-97`, `:264-279`;
`tests/test_v15_standards.py:1772-1836`; `tests/test_v190_agents.py:
82-100`; `tests/test_v1102_docs.py:90-135`; `tests/test_v1100_config.py:
145-165`; `tests/test_v1103_docs.py`, `tests/test_v1103_gates.py` (both
new or T3-created).

## Commit

One commit, this task only. Body references `(prompt:
docs/prompts/224-v1103-t4-paperwork.md)`. Do not touch
`docs/reports/report-v1.10.3.md` — I append T4's section myself.
